---
phase: 19-productionized-loop-metric-loop
plan: 03
type: execute
wave: 2
depends_on: ["19-01"]
files_modified:
  - scripts/loop_runner.py
autonomous: true
requirements: [LOOP-01, LOOP-02, LOOP-03, METRIC-01, METRIC-02, METRIC-03]
user_setup:
  - service: google-places
    why: "The ingest stage calls live Google Places Text Search (operator-run gate, not CI)"
    env_vars:
      - name: GOOGLE_PLACES_API_KEY
        source: "Google Cloud Console → APIs & Services → Credentials"
  - service: openai
    why: "Paraphrase generation + embed-v2 + retrieval embeddings call the OpenAI API"
    env_vars:
      - name: OPENAI_API_KEY
        source: "OpenAI dashboard → API keys"
must_haves:
  truths:
    - "loop_runner coerces DATABASE_URL=sandbox → get_settings.cache_clear() → close_db_pool() → asserts resolved target == sandbox BEFORE any coverage_agent import, gap_mine_main call, or proposal mutation (D-07 LOCKED CONSTRAINT)"
    - "loop_runner asserts settings.embedding_table == 'place_embeddings_v2' after cache-clear, before any semantic_search (D-07 LOCKED CONSTRAINT)"
    - "The miner→metric handoff is a deterministic one-gap set-diff on pending query_text: len(new)==1 → that gap; len(new)==0 → honest cold-start exit 0; len(new)>1 → EXIT_INFRA (D-08)"
    - "N paraphrases for the chosen gap are LLM-generated and DURABLY FROZEN to disk (frozen_paraphrases_runner.json written BEFORE the ingest subprocess call), non-circularity-checked against the seed, so post-ingest data cannot game them and the freeze survives a mid-ingest crash (D-04)"
    - "Target set = the place_embeddings_v2 DB-diff (new embedded IDs); before_hit@k = 0 by construction; empty after-ingest v2 diff → EXIT_INFRA(2) (D-02, D-03)"
    - "A DISTINCT places_raw before/after DB-diff guards provisioning: empty places_raw diff → EXIT_INFRA(2) with the D-02 'loop ran but ingested zero new rows' message (separate from the v2 'rows ingested but none embedded' signal); new_place_count = len(new_raw_ids) is logged to MLflow (D-02)"
    - "BOTH hit@k (gated) AND recall@k (logged) are computed; the gate = decide_loop_exit(strict-positive-delta AND after >= floor) (D-03, D-05)"
    - "MLflow logs the fixture-vs-real-demand flag, frozen paraphrases, v2 diff IDs, before/after hit@k, recall@k, delta, floor, counts under coverage_agent; exit 0/1/2 (D-01, D-06)"
    - "scripts/loop_falsifier.py and its hardcoded GAP are untouched — loop_runner is a NEW sibling (D-07)"
  artifacts:
    - path: "scripts/loop_runner.py"
      provides: "the staged productionized loop orchestrator (gap-mine→ingest→embed→score)"
      contains: "def main"
      min_lines: 250
  key_links:
    - from: "scripts/loop_runner.py"
      to: "app.loop.falsifier_core"
      via: "import compute_hit_rate, compute_recall_at_k, db_diff, decide_loop_exit, FLOOR, K, N, EXIT_*"
      pattern: "from app.loop.falsifier_core import"
    - from: "scripts/loop_runner.py"
      to: "scripts.coverage_agent.gap_mine_main"
      via: "deferred import AFTER sandbox coercion; called with ['--top-n','1']"
      pattern: "from scripts.coverage_agent import gap_mine_main"
    - from: "scripts/loop_runner.py"
      to: "place_embeddings_v2 DB-diff"
      via: "_snapshot_ids_from_url before/after ingest+embed"
      pattern: "place_embeddings_v2"
---

<objective>
Build `scripts/loop_runner.py` — a NEW sibling to `loop_falsifier.py` that owns the
productionized loop's stage ordering (D-07): coerce+cache_clear+close_pool+assert-sandbox
(MUST come first — clearing stale proposals is a sandbox WRITE and is unsafe before the
coercion, D-07) → embedding-table assert → gap-mine (clear-stale-pending → snapshot →
gap_mine_main → set-diff parse gap) → LLM-generate+freeze paraphrases (durably persisted
BEFORE ingest) → before-snapshot → ingest subprocess → embed-v2 subprocess → v2 DB-diff
(+ places_raw diff guard) → after-snapshot → hit@k/recall@k + floor gate → MLflow → exit
0/1/2. It encodes the three LOCKED CONSTRAINTS (coercion-ordering, embedding-table assertion,
empty-diff→INFRA) and the deterministic one-gap set-diff handoff (D-08). All gate/diff/floor
logic delegates to the pure `falsifier_core` functions from 19-01.

Purpose: This is the capstone glue that turns the individually-shipped pieces (gap-mine,
ingest, embed, metric) into ONE Make-targeted loop on a POPULATED baseline, measuring a real
before→after lift where new gap places compete against the existing corpus — the honest
productionized counterpart to Phase 16's empty-sandbox mechanism falsifier.

Output: `scripts/loop_runner.py` with `main()` + pure-orchestration helpers, reusing
`loop_falsifier`'s coercion/snapshot/subprocess/MLflow patterns (copied, NOT imported from
the falsifier). `loop_falsifier.py` stays byte-for-byte unchanged.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-productionized-loop-metric-loop/19-CONTEXT.md
@.planning/phases/19-productionized-loop-metric-loop/19-PATTERNS.md
@.planning/phases/19-productionized-loop-metric-loop/19-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build loop_runner sandbox-coercion + gap-handoff + paraphrase-freeze stages</name>
  <files>scripts/loop_runner.py</files>
  <read_first>
    - scripts/loop_falsifier.py L36-61 (module-scope import contract: ONLY stdlib + falsifier_core at module scope; everything settings-touching deferred into main())
    - scripts/loop_falsifier.py L152-171 (resolve_prod_url — copy verbatim) and L133-149 (assert_resolved_target — copy verbatim)
    - scripts/loop_falsifier.py L405-466 (the EXACT 4-step coercion sequence: read SANDBOX_DATABASE_URL → resolve prod → check_prod_safety → os.environ["DATABASE_URL"]=sandbox → get_settings.cache_clear() → close_db_pool() → assert resolve_database_url AND get_settings().resolved_database_url both == sandbox)
    - scripts/loop_falsifier.py L285-299 (run_subprocess_or_infra — copy verbatim) and L650-665 (_snapshot_ids_from_url — copy verbatim)
    - scripts/coverage_agent.py L799-913 (gap_mine_main contract: returns int, uses get_conn() pool, accepts ['--top-n','1'], cold-start returns 0) and L472-490 (gap_to_seed_query format "{cuisine} restaurants in {neighborhood} San Francisco")
    - scripts/sandbox_guard.py L33-55 (assert_sandbox_write_target — call on the SAME conn as the stale-clear write)
    - scripts/loop_falsifier.py L242-251 (the reject-stale-pending UPDATE idiom)
    - app/agent/critique/vibe.py (make_judge — the existing LLM handle for generating paraphrases; coverage_agent uses vibe.make_judge() at L399/L939) OR app/llm_factory.py L330 build_chat_model(llm_provider, chat_model, temperature) — pick make_judge for consistency with coverage_agent
    - configs/falsifier_paraphrases.json (the frozen-artifact shape: seed_query, generation_prompt, non_circularity_note, paraphrases[]) and 19-PATTERNS.md L723-742 (the per-run artifact shape extending it with gap_neighborhood/gap_cuisine/generation_timestamp/generation_model)
    - app/loop/falsifier_core.py L298-329 (check_non_circularity — exact-string check vs seed) and L209-295 (check_prod_safety)
    - .planning/phases/19-productionized-loop-metric-loop/19-PATTERNS.md L24-291 (the full loop_runner pattern assembly) + D-07/D-08 LOCKED CONSTRAINTS in 19-CONTEXT.md
  </read_first>
  <action>
    Create `scripts/loop_runner.py` with the SAME module-scope import discipline as
    loop_falsifier: at module scope import ONLY stdlib (`contextlib`, `datetime`, `json`,
    `os`, `subprocess`, `sys`, `typing.Any`), `mlflow`, `dotenv.dotenv_values`, and from
    `app.loop.falsifier_core`: `EXIT_PASS, EXIT_FAIL, EXIT_INFRA, K, N, FLOOR, GuardResult,
    check_non_circularity, check_prod_safety, compute_hit_rate, compute_recall_at_k, db_diff,
    decide_loop_exit, is_strictly_positive_delta`. Copy `resolve_prod_url`,
    `assert_resolved_target`, `run_subprocess_or_infra`, `_snapshot_ids_from_url` VERBATIM
    from loop_falsifier (do NOT import them from it — copy so the falsifier stays untouched).
    In `main()`, execute IN THIS ORDER: (Step 1) read `SANDBOX_DATABASE_URL` → EXIT_INFRA if
    unset; (Step 2) `resolve_prod_url(sandbox_url)` then `check_prod_safety` with
    `allow_remote = bool(os.environ.get("SANDBOX_ALLOW_REMOTE"))` → EXIT_INFRA on violation;
    (Step 3) `os.environ["DATABASE_URL"] = sandbox_url`, then deferred-import `get_settings`
    + `close_db_pool`, call `get_settings.cache_clear()` + `close_db_pool()`; (Step 4) assert
    `resolve_database_url(os.environ) == sandbox_url` AND
    `get_settings().resolved_database_url == sandbox_url` → EXIT_INFRA on mismatch; (Step 4b)
    NEW embedding-table assertion: `settings = get_settings(); if settings.embedding_table !=
    "place_embeddings_v2": EXIT_INFRA` (D-07 LOCKED CONSTRAINT, 19-PATTERNS L614-628). ONLY
    NOW (Step 5) deferred-import `gap_mine_main` from `scripts.coverage_agent`,
    `semantic_search` from `app.tools.retrieval`, `get_conn` from `app.db`, and
    `assert_sandbox_write_target` from `scripts.sandbox_guard`. (Step 6 — D-08 gap handoff)
    Open a sandbox conn via `get_conn()`, `assert_sandbox_write_target(conn)`, then: reject
    stale pending rows (the UPDATE...SET status='rejected' WHERE status='pending' idiom from
    loop_falsifier L242-251 — clear ALL stale pending so the diff is clean), snapshot
    `pending_before = {query_text WHERE status='pending'}`, call `gap_mine_main(["--top-n",
    "1"])`, snapshot `pending_after`, compute `new = pending_after - pending_before`
    (set-diff on query_text, NOT created_at). If `len(new) == 0` → print honest cold-start
    verdict and `raise SystemExit(EXIT_PASS)` (no-op, covers no-demand-gap AND
    already-deduped). If `len(new) > 1` → EXIT_INFRA (shouldn't happen with --top-n 1). If
    `len(new) == 1` → that `query_text` IS the gap seed; parse `(neighborhood, cuisine)` by
    reversing the `gap_to_seed_query` format `"{cuisine} restaurants in {neighborhood} San
    Francisco"`. (Step 6b — BASELINE/MINER GAP CONTRACT, WARNING 2) If
    `LOOP_GAP_NEIGHBORHOOD` / `LOOP_GAP_CUISINE` are set (the same env the populated baseline
    in 19-02 was provisioned with), assert the miner-parsed `(neighborhood, cuisine)` equals
    `(os.environ["LOOP_GAP_NEIGHBORHOOD"], os.environ["LOOP_GAP_CUISINE"])` → EXIT_INFRA on
    mismatch with a message that the baseline excluded bucket X but the miner chose bucket Y,
    so the metric would silently measure the wrong thing (the baseline-excluded gap MUST equal
    the miner-chosen gap, D-02/D-08). If the env vars are unset, skip the assertion (the
    operator accepts responsibility to provision the baseline with the same bucket the miner
    will choose — documented as a preflight in the runbook). (Step 7 — D-04 paraphrase freeze) Generate N
    paraphrases of the gap intent via `make_judge()` (or build_chat_model) with a generation
    prompt mirroring configs/falsifier_paraphrases.json's; build the per-run artifact dict
    (seed_query, generation_prompt, non_circularity_note, gap_neighborhood, gap_cuisine,
    generation_timestamp=utcnow isoformat, generation_model, paraphrases[]); run
    `check_non_circularity(paraphrases, [seed_query])` → EXIT_INFRA on violation; then DURABLY
    FREEZE the artifact to disk by writing it as `frozen_paraphrases_runner.json` to the
    run/artifact dir (e.g. a `loop_runner_artifacts/` dir or a tempdir whose path is threaded
    forward) with `json.dump(...)` — this write MUST happen BEFORE the before-snapshot/ingest
    stage in Task 2 so the paraphrases are frozen before any post-ingest data exists and the
    freeze survives a mid-ingest crash (D-04). Keep the artifact dict + its on-disk path in
    memory so Task 2's end-of-run `log_to_mlflow` re-references the SAME frozen artifact (the
    MLflow log is a re-reference, NOT the freeze point — the disk write is the freeze point).
    Stop this task here — Task 2 picks up at the before-snapshot. Include a `fixture_mode`
    bool determined by whether `DEMAND_DATABASE_URL` is set/non-empty (real demand) vs unset
    (seeded-sandbox fixture, D-01) and thread it forward.
  </action>
  <verify>
    <automated>poetry run python -c "import ast; ast.parse(open('scripts/loop_runner.py').read())" && poetry run ruff check scripts/loop_runner.py</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/loop_runner.py` parses (`python -c "import ast; ast.parse(...)"` exits 0) and `ruff check` is clean
    - Module scope imports ONLY stdlib + mlflow + dotenv + falsifier_core: `grep -n "^from scripts\|^from app\." scripts/loop_runner.py` returns NO matches outside `app.loop.falsifier_core` (everything else deferred)
    - The coercion sequence order is correct: in the file, the `get_settings.cache_clear()` line index < the first `from scripts.coverage_agent import gap_mine_main` line index < any `gap_mine_main(` call (the D-07 ordering — assert via grep line numbers)
    - `scripts/loop_runner.py` contains the embedding-table assertion: `grep -c 'embedding_table != "place_embeddings_v2"' scripts/loop_runner.py` >= 1
    - The gap handoff keys on the set-diff `pending_after - pending_before` (`grep -c "pending_after\|pending_before" scripts/loop_runner.py` >= 2) and has the three len(new) branches (0→exit 0, 1→parse, >1→INFRA)
    - BASELINE/MINER GAP CONTRACT present (WARNING 2): `grep -c "LOOP_GAP_NEIGHBORHOOD\|LOOP_GAP_CUISINE" scripts/loop_runner.py` >= 1 and the parsed `(neighborhood, cuisine)` is asserted equal to the `LOOP_GAP_*` env (when set) → EXIT_INFRA on mismatch
    - DURABLE PARAPHRASE FREEZE before ingest (D-04, BLOCKER 4): `scripts/loop_runner.py` writes `frozen_paraphrases_runner.json` to disk (`grep -c "frozen_paraphrases_runner.json" scripts/loop_runner.py` >= 1) and the `json.dump`/write line index < the first ingest subprocess (`ingest_places_sf.py`) invocation line index — verify by grep line numbers (the freeze is a real disk write BEFORE ingest, not merely held in memory)
    - `git diff --quiet scripts/loop_falsifier.py` exits 0 (falsifier untouched — D-07)
  </acceptance_criteria>
  <done>loop_runner.py exists with the locked coercion→cache_clear→close_pool→assert-sandbox→embedding-table-assert sequence BEFORE any miner import, the deterministic one-gap set-diff handoff with all three len(new) branches, and the frozen-before-ingest paraphrase artifact with non-circularity check; loop_falsifier untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Build loop_runner snapshot→ingest→embed→score→MLflow→exit stages</name>
  <files>scripts/loop_runner.py</files>
  <read_first>
    - scripts/loop_falsifier.py L488-598 (the before-snapshot → ingest subprocess → embed-v2 subprocess → v2 DB-diff → after-snapshot → gate sequence to mirror; Phase 19 ALWAYS uses new_v2_ids as the target set, D-03)
    - scripts/loop_falsifier.py L536-577 (subprocess invocation with child_env={**os.environ,"DATABASE_URL":sandbox_url}; the embed_added_count==0 → EXIT_INFRA loud-fail — D-02)
    - scripts/db/init.sql L32 (`places_raw.place_id TEXT PRIMARY KEY`) — so `_snapshot_ids_from_url(sandbox_url, "places_raw")` works the same way as the v2 snapshot; Phase 19 adds a DISTINCT places_raw before/after diff as the "no new rows ingested" infra guard (D-02), separate from the v2 "ingested but not embedded" guard
    - scripts/loop_falsifier.py L335-385 (log_to_mlflow — artifacts-before-metrics IN-05 pattern to mirror) and 19-PATTERNS.md L223-261 (the loop_runner log_to_mlflow additions: before/after hit@k, recall@k, floor, fixture_mode param, frozen_paraphrases_runner.json + demand_gaps.json artifacts, run name loop-runner-{neighborhood}-{cuisine})
    - scripts/loop_falsifier.py L600-647 (decide_exit call + verdict print + SystemExit — Phase 19 swaps to decide_loop_exit with floor+recall and prints both hit@k and recall@k in the verdict)
    - app/loop/falsifier_core.py compute_hit_rate / compute_recall_at_k / decide_loop_exit / FLOOR (the pure functions from 19-01 — read their final signatures from 19-01-SUMMARY.md)
    - app/tools/retrieval.py semantic_search(query, k=K) -> list[PlaceHit] (sync; .place_id field)
    - 19-CONTEXT.md D-05 (floor resolution: env LOOP_HIT_RATE_FLOOR / CLI --floor overrides the module FLOOR default; first run gates strict-positive-delta only since FLOOR default is 0.0)
  </read_first>
  <action>
    Continue `main()` after Task 1's paraphrase freeze. (Step 8 — before-snapshot) Capture
    `before_v2_ids = _snapshot_ids_from_url(sandbox_url, "place_embeddings_v2")` AND
    `before_raw_ids = _snapshot_ids_from_url(sandbox_url, "places_raw")` (the places_raw
    snapshot feeds the D-02 "no new rows ingested" guard in Step 10, distinct from the v2
    "ingested but not embedded" guard). Probe the frozen paraphrases: `before_topk = [[h.place_id for h in semantic_search(p, k=K)] for p in
    paraphrases]`. (Step 9 — ingest+embed) `child_env = {**os.environ, "DATABASE_URL":
    sandbox_url}`; `run_subprocess_or_infra([sys.executable, "scripts/ingest_places_sf.py"],
    child_env)`; `run_subprocess_or_infra([sys.executable, "-m",
    "scripts.embed_places_pgvector_v2"], child_env)`. (Step 10 — DB-diffs + provisioning guards) FIRST the places_raw diff (the
    "no new rows ingested" provisioning guard, D-02): `after_raw_ids =
    _snapshot_ids_from_url(sandbox_url, "places_raw")`; `new_raw_ids =
    db_diff(before_raw_ids, after_raw_ids)`; `new_place_count = len(new_raw_ids)`. GUARD A: if
    `new_place_count == 0` → print the D-02 "loop ran but ingested ZERO new places_raw rows —
    this is a provisioning/INFRA error, not a metric FAIL" message and
    `raise SystemExit(EXIT_INFRA)` (this is the distinct "loop ran but no new rows" infra
    signal). THEN the v2 diff (the SCORING target set, D-03): `after_v2_ids =
    _snapshot_ids_from_url(sandbox_url, "place_embeddings_v2")`; `new_v2_ids =
    db_diff(before_v2_ids, after_v2_ids)`; `embed_added_count = len(new_v2_ids)`. GUARD B: if
    `embed_added_count == 0` → print the D-02 "rows ingested but NONE embedded is an INFRA
    error" message and `raise SystemExit(EXIT_INFRA)` (do NOT proceed to burn after-snapshot
    API calls). NB: the v2 diff (`new_v2_ids`) is the SCORING target set per D-03; the raw diff
    (`new_raw_ids`) is ONLY for GUARD A + the `new_place_count` MLflow log — do NOT score
    against `new_raw_ids`. (Step 11 — after-snapshot + score) `after_topk = [...semantic_search...]`;
    `before_hit = compute_hit_rate(before_topk, new_v2_ids)` (0.0 by construction since new
    IDs didn't exist before — D-03); `after_hit = compute_hit_rate(after_topk, new_v2_ids)`;
    `recall = compute_recall_at_k(after_topk, new_v2_ids)`; `delta = after_hit.hit_rate -
    before_hit.hit_rate`. (Step 12 — floor resolution) `floor = float(os.environ.get(
    "LOOP_HIT_RATE_FLOOR", FLOOR))` (or a CLI --floor arg via argparse that defaults to that);
    (Step 13 — gate) `exit_code = decide_loop_exit(before_hit.hit_rate, after_hit.hit_rate,
    floor, guard_violation=None, embed_added_count=embed_added_count)`. (Step 14 — MLflow)
    Write a `log_to_mlflow` helper mirroring loop_falsifier's IN-05 artifacts-before-metrics:
    set experiment "coverage_agent", run name `f"loop-runner-{neighborhood}-{cuisine}"`; params
    (gap_neighborhood, gap_cuisine, seed_query, k=K, n=N, floor, fixture_mode); ALL log_dict
    artifacts (the frozen paraphrase run artifact as "frozen_paraphrases_runner.json",
    before/after snapshots, "db_diff_v2_place_ids.json" with sorted(new_v2_ids)) BEFORE any
    log_metric; then metrics (before_hit_at_k, after_hit_at_k, hit_rate_delta,
    recall_at_k=recall.recall, new_place_count, embed_added_count); on any MLflow exception →
    EXIT_INFRA. (Step 15 — verdict) print a `{'='*60}` banner reporting hit@k delta, recall@k,
    floor, and fixture_mode, mapping EXIT_PASS/FAIL/INFRA to PASS/FAIL/INFRA labels, then
    `raise SystemExit(exit_code)`. Add the `if __name__ == "__main__": main()` guard.
  </action>
  <verify>
    <automated>poetry run python -c "import ast; ast.parse(open('scripts/loop_runner.py').read())" && poetry run ruff check scripts/loop_runner.py && poetry run mypy scripts/loop_runner.py</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/loop_runner.py` parses, `ruff check` clean, `mypy scripts/loop_runner.py` reports no errors
    - Target set is the v2 diff: `grep -c "new_v2_ids\|place_embeddings_v2" scripts/loop_runner.py` >= 3 and `compute_hit_rate` / `compute_recall_at_k` are both called against `new_v2_ids`
    - BOTH provisioning guards exist (BLOCKER 3): the places_raw "no new rows" guard `grep -c "new_place_count == 0\|new_raw_ids" scripts/loop_runner.py` >= 1 → EXIT_INFRA, AND the v2 "ingested but not embedded" guard `grep -c "embed_added_count == 0" scripts/loop_runner.py` >= 1 → EXIT_INFRA; they are DISTINCT signals
    - `new_place_count` is DEFINED (not a NameError): `grep -c "new_place_count = len(new_raw_ids)" scripts/loop_runner.py` >= 1 and it is logged to MLflow in Step 14
    - `places_raw` is snapshotted before AND after ingest: `grep -c '_snapshot_ids_from_url(sandbox_url, "places_raw")' scripts/loop_runner.py` >= 2 (before + after); the raw diff is NOT used as the scoring target (only `new_v2_ids` is passed to compute_hit_rate/compute_recall_at_k)
    - BOTH metrics logged: `grep -c "after_hit_at_k\|recall_at_k" scripts/loop_runner.py` >= 2; floor + fixture_mode are MLflow params (`grep -c "floor\|fixture_mode" scripts/loop_runner.py` >= 2)
    - In log_to_mlflow, every `log_dict` line index < every `log_metric` line index (artifacts-before-metrics IN-05 — verify by grep line numbers)
    - `decide_loop_exit` is the gate (`grep -c "decide_loop_exit(" scripts/loop_runner.py` >= 1) — NOT a reimplemented inline gate
    - `git diff --quiet scripts/loop_falsifier.py` exits 0
  </acceptance_criteria>
  <done>loop_runner.main() completes the before-snapshot→ingest→embed→v2-diff→after-snapshot→hit@k+recall@k→floor-gate→MLflow→exit 0/1/2 pipeline; scores against the v2 DB-diff with before=0 by construction; empty-diff→INFRA; both metrics + floor + fixture_mode logged artifacts-before-metrics; falsifier untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| loop_runner → sandbox Postgres | proposal mutation + ingest + embed writes cross here; MUST be sandbox only |
| loop_runner → prod user_query_log | demand READ via DEMAND_DATABASE_URL (gap-mine), strictly read-only |
| loop_runner in-process pool → resolved DB | the lru_cache/pool footgun can resolve a CACHED prod target if coercion ordering is violated |
| loop_runner → Google Places + OpenAI | live keys for ingest, embed, paraphrase-gen, retrieval (operator-run) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-03-01 | Tampering | accidental write to prod places_raw | mitigate | Step 3-4 coerce DATABASE_URL=sandbox → cache_clear → close_db_pool → assert resolve_database_url AND get_settings().resolved_database_url both == sandbox BEFORE any miner import/proposal mutation (D-07); assert_sandbox_write_target runs a live SELECT current_database() on the SAME conn as the stale-clear |
| T-19-03-02 | Spoofing | lru_cache/pool resolving CACHED prod target | mitigate | get_settings.cache_clear() + close_db_pool() immediately after coercion; both the free-function AND cached-settings resolution paths asserted == sandbox (the D-07 LOCKED CONSTRAINT footgun, proven in Phase 16) |
| T-19-03-03 | Tampering | semantic_search scoring against wrong view | mitigate | Step 4b asserts settings.embedding_table == 'place_embeddings_v2' after cache-clear, before any semantic_search call (D-07 LOCKED CONSTRAINT) |
| T-19-03-04 | Information Disclosure | demand read mutating prod | mitigate | gap-mine reads prod user_query_log via DEMAND_DATABASE_URL strictly read-only; all writes (proposals/ingest/embed) target the coerced sandbox DATABASE_URL only |
| T-19-03-05 | Repudiation | gap-attribution claimed as guaranteed | accept | ingest persists every Google result with no bucket post-filter (D-02 caveat); loop_runner LOGS the v2 diff IDs + fixture_mode flag so attribution is AUDITABLE, not claimed-guaranteed |
| T-19-03-06 | Information Disclosure | live Google/OpenAI keys | accept | Operator-run gate only; CI never runs loop_runner (19-04 unit-tests the decision logic at zero key cost) |
| T-19-03-SC | Tampering | npm/pip/cargo installs | accept | No new package installs (reuses existing ingest/embed/gap-mine scripts + poetry env); no `## Package Legitimacy Audit` required — no install tasks in this plan |
</threat_model>

<verification>
- `scripts/loop_runner.py` parses, `ruff check` clean, `mypy scripts/loop_runner.py` clean.
- Module-scope imports limited to stdlib + mlflow + dotenv + falsifier_core (all app.*/scripts.* deferred into main() after coercion — D-07).
- Coercion → cache_clear → close_pool → assert-sandbox → embedding-table-assert all precede the first gap_mine_main import/call (grep line-number ordering).
- Gap handoff is the deterministic set-diff on query_text with three len(new) branches (D-08).
- Target set = place_embeddings_v2 DB-diff; before=0 by construction; empty v2-diff → EXIT_INFRA. A DISTINCT places_raw diff guards provisioning (empty raw diff → EXIT_INFRA, "no new rows ingested"); new_place_count = len(new_raw_ids) is defined and logged.
- MLflow artifacts-before-metrics; both hit@k + recall@k + floor + fixture_mode logged.
- `git diff --quiet scripts/loop_falsifier.py` exits 0.
</verification>

<success_criteria>
- The full staged pipeline runs in the locked order with all three LOCKED CONSTRAINTS encoded (coercion-ordering, embedding-table assertion, empty-diff→INFRA).
- The miner→metric handoff is deterministic and keyed on pending query_text set-diff (D-08).
- N paraphrases are LLM-generated and frozen to a run artifact BEFORE ingest, non-circularity-checked (D-04).
- hit@k is gated via decide_loop_exit(strict-positive-delta AND after >= floor); recall@k is logged (D-03/D-05).
- fixture-vs-real-demand flag distinguishes seeded-fixture from real-demand runs (D-01).
- loop_falsifier.py and its hardcoded GAP are untouched (D-07).
</success_criteria>

<output>
Create `.planning/phases/19-productionized-loop-metric-loop/19-03-SUMMARY.md` when done.
</output>
