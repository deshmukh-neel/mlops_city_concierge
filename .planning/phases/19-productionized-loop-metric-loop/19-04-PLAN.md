---
phase: 19-productionized-loop-metric-loop
plan: 04
type: execute
wave: 3
depends_on: ["19-01", "19-03"]
files_modified:
  - Makefile
  - docs/loop_runner.md
  - tests/unit/test_loop_runner_orchestrator.py
  - app/loop/falsifier_core.py
  - CLAUDE.md
  - AGENTS.md
  - .github/copilot-instructions.md
autonomous: false
requirements: [LOOP-01, LOOP-02, LOOP-03, METRIC-01]
user_setup:
  - service: google-places
    why: "make loop runs the live operator gate (ingest stage)"
    env_vars:
      - name: GOOGLE_PLACES_API_KEY
        source: "Google Cloud Console → APIs & Services → Credentials"
  - service: openai
    why: "make loop runs paraphrase-gen + embed + retrieval"
    env_vars:
      - name: OPENAI_API_KEY
        source: "OpenAI dashboard → API keys"
must_haves:
  truths:
    - "make loop wraps loop_runner.py with a three-key env-guard (SANDBOX_DATABASE_URL + GOOGLE_PLACES_API_KEY + OPENAI_API_KEY) mirroring make loop-falsifier (D-06)"
    - "CI unit-tests the runner DECISION logic (floor handling, no-gap/cold-start, one-gap handoff, stale-proposal rejection, exit-code mapping) at zero API cost — CI does NOT run the live loop (D-06)"
    - "docs/loop_runner.md is an operator runbook documenting the gate, the fixture-vs-real-demand honesty caveat (D-01), the gap-attribution-is-audited-not-guaranteed caveat (D-02), and exit codes 0/1/2"
    - "The committed FLOOR is calibrated: after the operator records the actual populated after_hit@k, the FLOOR default is ratcheted to the highest defensible value with the justifying run documented (D-05)"
  artifacts:
    - path: "Makefile"
      provides: "make loop target with three-key guard"
      contains: "loop:"
    - path: "docs/loop_runner.md"
      provides: "operator runbook + honesty caveats + exit codes + calibration record"
      contains: "fixture"
    - path: "tests/unit/test_loop_runner_orchestrator.py"
      provides: "zero-API-cost decision-logic + handoff unit tests (mock all externals)"
      contains: "class Test"
  key_links:
    - from: "Makefile loop target"
      to: "scripts/loop_runner.py"
      via: "$(POETRY_RUN) python scripts/loop_runner.py after the three-key guard"
      pattern: "loop_runner.py"
    - from: "tests/unit/test_loop_runner_orchestrator.py"
      to: "app.loop.falsifier_core.decide_loop_exit + scripts.loop_runner gap-handoff helpers"
      via: "import + mock psycopg2/mlflow/semantic_search/gap_mine_main"
      pattern: "import"
---

<objective>
Wire the operator-run gate and lock down its honesty: add `make loop` (three-key env-guard,
D-06), write `docs/loop_runner.md` (runbook + the fixture-vs-real-demand caveat D-01 + the
gap-attribution-audited-not-guaranteed caveat D-02 + exit codes), add the zero-API-cost
orchestrator decision-logic unit tests (D-06 — CI never runs the live loop), and run the
floor-calibration checkpoint (D-05 — record the real populated after_hit@k, ratchet the
committed FLOOR, document the justifying run). Sync the three AI-instruction files for the
new `make loop` target.

Purpose: D-06 mandates the operator-run-gate / CI-unit-tested split (mirrors Phase 16
exactly): the live loop costs real API calls and needs a sandbox + live keys, so CI must
test the pure decision logic only. D-05 makes the floor a calibrated outcome — the first run
gates on strict-positive-delta (FLOOR default 0.0), then the floor ratchets to the highest
defensible value once the real after_hit@k is known. This plan closes the loop on both.

Output: `make loop`; `docs/loop_runner.md`; `tests/unit/test_loop_runner_orchestrator.py`;
a ratcheted `FLOOR` in `falsifier_core.py` with the justifying run in the runbook; CLAUDE.md
/ AGENTS.md / copilot-instructions.md synced.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-productionized-loop-metric-loop/19-CONTEXT.md
@.planning/phases/19-productionized-loop-metric-loop/19-PATTERNS.md
@.planning/phases/19-productionized-loop-metric-loop/19-01-SUMMARY.md
@.planning/phases/19-productionized-loop-metric-loop/19-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add make loop target + zero-cost orchestrator decision-logic unit tests</name>
  <files>Makefile, tests/unit/test_loop_runner_orchestrator.py</files>
  <read_first>
    - Makefile L53-60 (sandbox-provision env-guard) and L237-246 (loop-falsifier target) — mirror BOTH: the loop-falsifier `$(POETRY_RUN) python scripts/...` form AND the sandbox-provision `@[ -n ... ] || { echo ERROR; exit 1; }` guard, extended to all THREE keys
    - .planning/phases/19-productionized-loop-metric-loop/19-PATTERNS.md L474-497 (the exact make loop target with the three-key guard to add)
    - tests/unit/test_loop_falsifier_orchestrator.py (the EXACT mocking idiom: unittest.mock MagicMock/patch; TestDecideExit L137-202; TestResolvedTargetCheck L243-260; the _make_mock_conn helper L455-460 for psycopg2 conn mocking) — mirror this file's structure for the new test
    - app/loop/falsifier_core.py decide_loop_exit + FLOOR (from 19-01) — the pure gate under test
    - scripts/loop_runner.py (from 19-03) — the gap-handoff set-diff logic + cold-start branches + embedding-table assertion to unit-test by mocking gap_mine_main/get_conn/semantic_search/mlflow
    - CLAUDE.md "Key Conventions" (pytest asyncio_mode=auto; conftest patches env; integration tests skipped unless APP_ENV=integration; no manual ruff step — pre-commit handles it)
  </read_first>
  <action>
    In `Makefile`, add a `.PHONY: loop` target after `loop-falsifier` (L246) per
    19-PATTERNS.md L474-497: a three-key guard — three separate `@[ -n "$${VAR:-}" ] || {
    echo "ERROR: VAR is not set."; exit 1; }` blocks for SANDBOX_DATABASE_URL,
    GOOGLE_PLACES_API_KEY, OPENAI_API_KEY — then `$(POETRY_RUN) python scripts/loop_runner.py`.
    Give it a `##` help comment: "LOOP-01..03+METRIC: productionized loop runner — full
    gap-mine→ingest→embed→score cycle (requires SANDBOX_DATABASE_URL + GOOGLE_PLACES_API_KEY +
    OPENAI_API_KEY); see docs/loop_runner.md". Create
    `tests/unit/test_loop_runner_orchestrator.py` mirroring
    test_loop_falsifier_orchestrator.py — ALL DB/network/subprocess/mlflow/LLM mocked, zero
    API cost. Cover: (1) decide_loop_exit floor handling — positive delta above floor → PASS,
    positive delta below floor → FAIL, floor==0 reduces to strict-positive-delta, non-positive
    delta → FAIL, guard violation → INFRA, embed_added_count==0 → INFRA; (2) the gap-handoff
    set-diff: len(new)==0 → cold-start exit 0, len(new)==1 → parses (neighborhood, cuisine)
    from the seed via the gap_to_seed_query reverse-format, len(new)>1 → EXIT_INFRA (mock
    gap_mine_main + a mock conn whose pending-query SELECT returns controlled before/after
    sets); (3) stale-proposal rejection runs BEFORE the pending_before snapshot; (4) the
    embedding-table assertion exits INFRA when settings.embedding_table != 'place_embeddings_v2'
    (mock get_settings); (5) exit-code mapping 0/1/2 to PASS/FAIL/INFRA labels. Use the
    `_make_mock_conn` cursor-context idiom from the falsifier orchestrator test. For any
    branch hard to reach via main(), assert against the extracted pure helper or a source
    string (the falsifier test uses this fallback at L421-447) — but PREFER exercising the
    real helper functions. Do NOT add a manual ruff step (pre-commit handles it).
  </action>
  <verify>
    <automated>poetry run pytest tests/unit/test_loop_runner_orchestrator.py -x -q && make -n loop SANDBOX_DATABASE_URL=x GOOGLE_PLACES_API_KEY=y OPENAI_API_KEY=z</automated>
  </verify>
  <acceptance_criteria>
    - `Makefile` contains `loop:` with three separate guards: `grep -c 'SANDBOX_DATABASE_URL:-}\|GOOGLE_PLACES_API_KEY:-}\|OPENAI_API_KEY:-}' Makefile` shows the loop target adds all three (>= 3 new guard lines in the loop block)
    - `make -n loop SANDBOX_DATABASE_URL=x GOOGLE_PLACES_API_KEY=y OPENAI_API_KEY=z` prints `python scripts/loop_runner.py` without running it
    - `tests/unit/test_loop_runner_orchestrator.py` exists, imports `unittest.mock`, and makes NO real DB/network/LLM/subprocess calls (mocks gap_mine_main, get_conn, semantic_search, mlflow, subprocess)
    - The test covers all 6 decide_loop_exit cases AND the 3 len(new) handoff branches AND the embedding-table-assert INFRA case
    - `poetry run pytest tests/unit/test_loop_runner_orchestrator.py -q` exits 0 — zero API cost
  </acceptance_criteria>
  <done>make loop exists with the three-key guard and runs loop_runner.py; the orchestrator decision-logic + handoff + exit-mapping are unit-tested at zero API cost (CI never runs the live loop — D-06).</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Operator floor-calibration run (live keys + sandbox — human-only)</name>
  <files>app/loop/falsifier_core.py, docs/loop_runner.md</files>
  <read_first>
    - docs/loop_falsifier.md (the existing operator runbook to mirror for docs/loop_runner.md)
    - 19-CONTEXT.md D-05 (first run gates strict-positive-delta only; ratchet FLOOR to highest defensible value AFTER observing populated after_hit@k; document floor + justifying run) and D-01/D-02 (the fixture + attribution honesty caveats the runbook must carry)
    - app/loop/falsifier_core.py FLOOR (the constant to ratchet)
  </read_first>
  <action>
    HUMAN-ONLY operator step — Claude cannot run it (needs live Google Places + OpenAI keys
    and a provisioned sandbox DB). The operator provisions the populated baseline, runs
    `make loop` once to observe the real populated-corpus after_hit@k, ratchets the committed
    FLOOR to the highest defensible value at-or-below that observed after_hit@k (D-05), and
    drafts docs/loop_runner.md (gate purpose, exit codes 0/1/2, env prerequisites, the
    fixture-vs-real-demand caveat D-01, the gap-attribution-audited-not-guaranteed caveat D-02,
    and a "Floor calibration" section recording the justifying MLflow run). See
    <how-to-verify> for the exact commands; Task 3 commits whatever the operator approves here.
  </action>
  <what-built>
    make loop (operator gate) + loop_runner.py + populated-baseline provisioning. This
    checkpoint runs the live loop ONCE to observe the real populated-corpus after_hit@k, then
    ratchets the committed FLOOR and writes the runbook. This step is human-only because it
    needs live Google Places + OpenAI keys and a provisioned sandbox DB — Claude cannot run it.
  </what-built>
  <how-to-verify>
    1. Provision the populated baseline (choose the gap bucket, set LOOP_GAP_NEIGHBORHOOD /
       LOOP_GAP_CUISINE): `export SANDBOX_DATABASE_URL=postgresql://postgres:cityconcierge@127.0.0.1:5433/city_concierge_sandbox`
       then `make sandbox-provision-populated`.
    2. Export GOOGLE_PLACES_API_KEY + OPENAI_API_KEY (and DEMAND_DATABASE_URL if running real
       demand; leave unset to use the seeded fixture — note which mode you ran).
    3. Run `make loop`. Record the printed before_hit@k (should be 0.000 by v2-diff
       construction), after_hit@k, recall@k, delta, embed_added_count, fixture_mode, and the
       exit code. Confirm exit 0 (PASS) on a strict-positive delta with FLOOR still 0.0.
    4. Set the committed `FLOOR` in app/loop/falsifier_core.py to the highest defensible value
       at or below the observed after_hit@k (D-05 — e.g. if after_hit@k = 0.6, a defensible
       floor might be 0.4; do not set it above the observed value or the gate fails by
       construction next run).
    5. Write docs/loop_runner.md: the gate purpose, exit codes 0/1/2, the env-var
       prerequisites, the fixture-vs-real-demand honesty caveat (D-01), the
       gap-attribution-is-audited-not-guaranteed caveat (D-02), and a "Floor calibration"
       section recording the justifying run (MLflow run name/id, gap bucket, observed
       after_hit@k, the chosen FLOOR, and the date).
  </how-to-verify>
  <verify>
    <human-check>Operator confirms make loop exited 0 (PASS) on a strict-positive delta with FLOOR=0.0, and reports before/after hit@k, recall@k, delta, exit code, the chosen FLOOR value, and the MLflow run id.</human-check>
  </verify>
  <done>The operator has run make loop once, recorded the populated after_hit@k + recall@k, chosen the calibrated FLOOR at-or-below the observed after_hit@k, and captured the justifying MLflow run id for Task 3 to commit.</done>
  <resume-signal>Paste the run's before/after hit@k, recall@k, delta, exit code, the chosen FLOOR value, and the MLflow run id; then type "approved" (or describe any FAIL/INFRA so we can diagnose before ratcheting).</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Commit the calibrated FLOOR + runbook + sync AI-instruction docs</name>
  <files>app/loop/falsifier_core.py, docs/loop_runner.md, CLAUDE.md, AGENTS.md, .github/copilot-instructions.md, tests/unit/test_falsifier_core_recall.py</files>
  <read_first>
    - app/loop/falsifier_core.py FLOOR (set to the value approved in Task 2; do NOT guess — use the operator-provided value)
    - docs/loop_runner.md (the runbook drafted in Task 2 — finalize it with the approved FLOOR + justifying run)
    - CLAUDE.md (the "Commands" Makefile block + the "Adaptive data loop (Phase 18 / v2.3)" section — add a `make loop` line and a brief Phase 19 note)
    - AGENTS.md + .github/copilot-instructions.md (the sync targets — CLAUDE.md "sync notice" requires all three mirror each other)
    - tests/unit/test_falsifier_core_recall.py (from 19-01 — if the FLOOR default changed, ensure no test hardcodes FLOOR==0.0 in a way that now breaks; update the floor-default test to assert the new committed value)
  </read_first>
  <action>
    Set `FLOOR` in `app/loop/falsifier_core.py` to the exact value approved in Task 2 (update
    its docstring comment to reference docs/loop_runner.md and the justifying run). Finalize
    `docs/loop_runner.md` with the approved FLOOR and the justifying-run record (MLflow run
    id, gap bucket, observed after_hit@k, date). In `CLAUDE.md`, add a `make loop` line to the
    Commands Makefile block and a one-line Phase 19 note under the adaptive-data-loop section
    describing the productionized loop + the operator-run gate (CI tests decision logic only).
    Mirror the SAME edits into `AGENTS.md` and `.github/copilot-instructions.md` (the
    three-file sync notice). If 19-01's floor-default unit test asserted FLOOR==0.0, update it
    to the new committed value so the suite stays green. Run the full unit suite to confirm
    nothing regressed.
  </action>
  <verify>
    <automated>poetry run pytest tests/unit/test_falsifier_core_recall.py tests/unit/test_loop_runner_orchestrator.py -q && poetry run python -c "from app.loop.falsifier_core import FLOOR; print(FLOOR)"</automated>
  </verify>
  <acceptance_criteria>
    - `app/loop/falsifier_core.py` FLOOR equals the operator-approved value from Task 2 (not 0.0 unless the operator explicitly kept it at 0.0)
    - `docs/loop_runner.md` contains a "Floor calibration" section with the MLflow run id, observed after_hit@k, the chosen FLOOR, and the date; AND the fixture-vs-real-demand + gap-attribution caveats
    - `make loop` appears in all THREE of CLAUDE.md, AGENTS.md, .github/copilot-instructions.md (the sync notice satisfied) — `grep -l "make loop\b\|loop_runner" CLAUDE.md AGENTS.md .github/copilot-instructions.md` lists all three
    - `poetry run pytest tests/unit/test_falsifier_core_recall.py tests/unit/test_loop_runner_orchestrator.py -q` exits 0 (no test hardcodes the stale FLOOR)
  </acceptance_criteria>
  <done>The calibrated FLOOR is committed with its justifying run documented in docs/loop_runner.md; CLAUDE.md/AGENTS.md/copilot-instructions.md are synced for make loop; the unit suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → make loop (live gate) | live Google/OpenAI keys + sandbox DB cross here; operator-run only |
| CI → unit tests | tests mock all externals; no key, no DB, no network crosses this boundary |
| docs → reader | the runbook must not OVERCLAIM (fixture vs real-demand; audited vs guaranteed attribution) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-04-01 | Information Disclosure | live keys in make loop | accept | make loop is operator-run; the three-key guard fails fast if a key is missing; CI never invokes it (only the mocked unit tests run) |
| T-19-04-02 | Repudiation | runbook overclaiming results | mitigate | docs/loop_runner.md MUST document the fixture-vs-real-demand caveat (D-01) and the gap-attribution-audited-not-guaranteed caveat (D-02); the calibration section records the exact justifying run so the FLOOR is defensible, not asserted |
| T-19-04-03 | Tampering | FLOOR set above observed after_hit@k | mitigate | Task 2 instructs setting FLOOR at-or-below the observed after_hit@k; Task 3 acceptance asserts FLOOR equals the operator-approved value and the suite stays green (a too-high floor would fail the gate by construction next run) |
| T-19-04-SC | Tampering | npm/pip/cargo installs | accept | No new package installs (Makefile target + docs + tests over existing scripts); no `## Package Legitimacy Audit` required — no install tasks in this plan |
</threat_model>

<verification>
- `poetry run pytest tests/unit/test_loop_runner_orchestrator.py tests/unit/test_falsifier_core_recall.py -q` passes — zero API cost.
- `make -n loop SANDBOX_DATABASE_URL=x GOOGLE_PLACES_API_KEY=y OPENAI_API_KEY=z` prints the runner command; missing any key → ERROR + exit 1.
- `docs/loop_runner.md` carries the D-01 + D-02 honesty caveats and the calibration record.
- The committed FLOOR matches the operator-approved value; CLAUDE.md/AGENTS.md/copilot-instructions.md synced.
- CI does NOT run the live loop (only the mocked unit tests) — D-06 split preserved.
</verification>

<success_criteria>
- make loop runs the operator gate with a three-key env-guard (D-06).
- The runner decision logic (floor, cold-start, one-gap handoff, stale rejection, exit mapping) is unit-tested at zero API cost; CI never runs the live loop (D-06).
- The FLOOR is a calibrated outcome — ratcheted to the highest defensible value with the justifying run documented (D-05).
- docs/loop_runner.md keeps the story honest (fixture-vs-real-demand D-01; attribution-audited-not-guaranteed D-02).
- The three AI-instruction files are synced for make loop.
</success_criteria>

<output>
Create `.planning/phases/19-productionized-loop-metric-loop/19-04-SUMMARY.md` when done.
</output>
