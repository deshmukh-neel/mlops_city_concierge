---
phase: 19-productionized-loop-metric-loop
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/loop/falsifier_core.py
  - tests/unit/test_falsifier_core_recall.py
autonomous: true
requirements: [METRIC-01, METRIC-02, METRIC-03]
must_haves:
  truths:
    - "compute_recall_at_k returns recall = #distinct new IDs found across paraphrases' top-k / total new IDs (D-03)"
    - "BOTH a hit@k scorer (already exists) AND a recall@k scorer (new) are pure, stdlib-only, and unit-tested at zero API cost (D-03, D-06)"
    - "A runtime-tunable FLOOR constant exists; the populated-corpus gate decision combines strict-positive-delta AND after_hit@k >= floor (D-05)"
    - "decide_loop_exit returns EXIT_PASS only when delta is strictly positive AND after_rate >= floor; EXIT_INFRA on guard/empty-diff; EXIT_FAIL otherwise (D-05, D-06)"
  artifacts:
    - path: "app/loop/falsifier_core.py"
      provides: "compute_recall_at_k + RecallAtKResult + FLOOR + decide_loop_exit (net-new, pure)"
      contains: "def compute_recall_at_k"
    - path: "tests/unit/test_falsifier_core_recall.py"
      provides: "zero-API-cost unit tests for recall@k, FLOOR, and decide_loop_exit"
      contains: "class TestComputeRecallAtK"
  key_links:
    - from: "tests/unit/test_falsifier_core_recall.py"
      to: "app.loop.falsifier_core"
      via: "import compute_recall_at_k, RecallAtKResult, FLOOR, decide_loop_exit"
      pattern: "from app.loop.falsifier_core import"
---

<objective>
Add the populated-dataset scoring primitives to `app/loop/falsifier_core.py`: a pure
`compute_recall_at_k` sibling of the existing `compute_hit_rate` (D-03), a
runtime-tunable `FLOOR` constant (D-05), and a pure `decide_loop_exit` gate function
that combines strict-positive-delta with the floor check (D-05/D-06). All four are
stdlib-only, side-effect-free, and unit-tested at zero API cost — the pure-core half of
the operator-run/core-unit-tested split (D-06).

Purpose: The current `compute_hit_rate` measures hit@k against an EMPTY-sandbox DB-diff;
Phase 19 measures a before→after lift on a POPULATED baseline and needs (a) recall depth
alongside the headline hit@k, and (b) a calibrated floor gate. Keeping all gate/diff/floor
logic in pure functions lets the orchestrator (19-03) and CI unit-test it without DB,
network, or LLM cost.

Output: `compute_recall_at_k`, `RecallAtKResult`, `FLOOR`, `decide_loop_exit` added to
`falsifier_core.py`; a new `tests/unit/test_falsifier_core_recall.py` covering them.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-productionized-loop-metric-loop/19-CONTEXT.md
@.planning/phases/19-productionized-loop-metric-loop/19-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add compute_recall_at_k + RecallAtKResult to falsifier_core</name>
  <files>app/loop/falsifier_core.py, tests/unit/test_falsifier_core_recall.py</files>
  <read_first>
    - app/loop/falsifier_core.py (the file being modified; mirror compute_hit_rate L71-106 EXACTLY: same IN-02 `len(topk) <= K` assertion with `# noqa: S101`, same empty-input-returns-zero guard, frozen dataclass result, K=5 module constant)
    - app/loop/falsifier_core.py L49-56 (HitRateResult dataclass shape to mirror for RecallAtKResult)
    - tests/unit/test_loop_falsifier_core.py L58-138 (TestComputeHitRate — the exact test idiom to mirror)
    - .planning/phases/19-productionized-loop-metric-loop/19-PATTERNS.md L336-408 (the exact compute_recall_at_k body + RecallAtKResult dataclass to add)
  </read_first>
  <behavior>
    - All new IDs found across paraphrases → recall == 1.0 (found_count == total_count)
    - Partial coverage: 2 of 4 new IDs appear in some paraphrase's top-k → recall == 0.5
    - Empty newly_ingested_ids → RecallAtKResult(found_count=0, total_count=0, recall=0.0), no ZeroDivisionError
    - A top-k list longer than K raises AssertionError (IN-02, single source of truth for the retrieval window)
    - Distinct counting: the SAME new ID found in multiple paraphrases counts ONCE toward found_count
  </behavior>
  <action>
    Add a frozen `@dataclass RecallAtKResult` with fields `found_count: int`,
    `total_count: int`, `recall: float` directly after the existing `HitRateResult`
    dataclass in `app/loop/falsifier_core.py`. Add
    `def compute_recall_at_k(per_paraphrase_topk: list[list[str]], newly_ingested_ids: set[str]) -> RecallAtKResult`
    in the "Metric functions" section right after `compute_hit_rate`. Body per
    19-PATTERNS.md L374-407: `total_count = len(newly_ingested_ids)`; if `total_count == 0`
    return `RecallAtKResult(0, 0, 0.0)`; assert `len(topk) <= K` for each list with the
    SAME `# noqa: S101` message style as `compute_hit_rate` (IN-02); accumulate
    `found = set()`; `for topk: found |= set(topk) & newly_ingested_ids`;
    `found_count = len(found)`; return `RecallAtKResult(found_count, total_count, found_count / total_count)`.
    Keep stdlib-only — NO mlflow/psycopg2/app.db/semantic_search imports (the module's
    import contract). recall keys on DISTINCT IDs via the set union, NOT a per-paraphrase
    sum.
  </action>
  <verify>
    <automated>poetry run pytest tests/unit/test_falsifier_core_recall.py::TestComputeRecallAtK -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `app/loop/falsifier_core.py` contains `def compute_recall_at_k(` and `class RecallAtKResult`
    - `grep -n "import mlflow\|import psycopg2\|from app.db\|semantic_search" app/loop/falsifier_core.py` returns NO new matches (import contract preserved)
    - `poetry run python -c "from app.loop.falsifier_core import compute_recall_at_k, RecallAtKResult"` exits 0
    - `poetry run pytest tests/unit/test_falsifier_core_recall.py::TestComputeRecallAtK -q` exits 0 with all 5 behavior cases passing
  </acceptance_criteria>
  <done>compute_recall_at_k + RecallAtKResult exist, are pure (no new heavy imports), and the TestComputeRecallAtK class passes all 5 behavior cases.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add FLOOR constant + pure decide_loop_exit gate function</name>
  <files>app/loop/falsifier_core.py, tests/unit/test_falsifier_core_recall.py</files>
  <read_first>
    - app/loop/falsifier_core.py L24-41 (module-constants section: K, N, EXIT_PASS/FAIL/INFRA — add FLOOR here)
    - app/loop/falsifier_core.py L119-126 (is_strictly_positive_delta — reused unchanged inside decide_loop_exit)
    - scripts/loop_falsifier.py L302-332 (decide_exit — the existing gate idiom to mirror; Phase 19 adds floor + drops the `before_rate != 0.0 → INFRA` branch because the populated baseline keeps before_hit@k = 0 by v2-diff construction, D-03)
    - .planning/phases/19-productionized-loop-metric-loop/19-PATTERNS.md L296-318 (the floor-aware decide_exit variant guidance)
    - tests/unit/test_loop_falsifier_orchestrator.py L137-202 (TestDecideExit — the exact unit-test idiom to mirror for decide_loop_exit)
  </read_first>
  <behavior>
    - guard_violation present (ok=False) → EXIT_INFRA (highest priority)
    - embed_added_count == 0 → EXIT_INFRA (D-02 empty-diff loud-fail)
    - strictly-positive delta AND after_rate >= floor → EXIT_PASS
    - strictly-positive delta BUT after_rate < floor → EXIT_FAIL (below the calibrated bar, D-05)
    - non-positive delta → EXIT_FAIL
    - floor == 0.0 (first-run default) reduces to strict-positive-delta-only (D-05)
  </behavior>
  <action>
    Add a module constant `FLOOR: float = 0.0` in the constants section (L24-41) with a
    docstring comment naming it the runtime-tunable populated-corpus quality floor (D-05),
    that the first run uses strict-positive-delta only and the orchestrator overrides it via
    env `LOOP_HIT_RATE_FLOOR` / CLI `--floor`, and that 19-04 ratchets the committed default
    after the calibration run. Add a pure
    `def decide_loop_exit(before_rate: float, after_rate: float, floor: float, guard_violation: GuardResult | None, embed_added_count: int) -> int`.
    Priority order: if `guard_violation is not None and not guard_violation.ok` → `EXIT_INFRA`;
    if `embed_added_count == 0` → `EXIT_INFRA`; if
    `is_strictly_positive_delta(before_rate, after_rate) and after_rate >= floor` → `EXIT_PASS`;
    else → `EXIT_FAIL`. Reuse the existing `is_strictly_positive_delta` (do NOT reimplement
    the delta math). Do NOT include the `before_rate != 0.0 → EXIT_INFRA` branch from
    `loop_falsifier.decide_exit` — the populated baseline legitimately scores before_hit@k = 0
    against the v2-diff target set (D-03), so a non-zero before would be caught upstream, not
    here. Keep `loop_falsifier.py` and its `decide_exit` UNTOUCHED (this is a NEW sibling fn).
  </action>
  <verify>
    <automated>poetry run pytest tests/unit/test_falsifier_core_recall.py::TestDecideLoopExit -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `app/loop/falsifier_core.py` contains `FLOOR: float = 0.0` and `def decide_loop_exit(`
    - `decide_loop_exit` references `is_strictly_positive_delta` (not a reimplemented delta) — `grep -A12 "def decide_loop_exit" app/loop/falsifier_core.py` shows the call
    - `scripts/loop_falsifier.py` is byte-for-byte unchanged: `git diff --quiet scripts/loop_falsifier.py` exits 0
    - `poetry run pytest tests/unit/test_falsifier_core_recall.py::TestDecideLoopExit -q` exits 0 covering all 6 behavior cases (the floor>after_rate FAIL branch and floor==0 strict-delta-only branch are both asserted)
  </acceptance_criteria>
  <done>FLOOR + decide_loop_exit exist as pure functions; loop_falsifier.py untouched; TestDecideLoopExit passes all 6 cases including the below-floor FAIL and floor==0 reduction.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → falsifier_core | Pure functions receive caller-supplied lists/sets/floats; no DB/network/LLM crosses this boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01-01 | Tampering | falsifier_core import contract | mitigate | Acceptance criterion greps for new mlflow/psycopg2/app.db/semantic_search imports; the module stays stdlib-only so it cannot reach prod DB or spend API budget |
| T-19-01-02 | Information Disclosure | decide_loop_exit gate logic | accept | Pure boolean/int gate over numeric rates; no secrets, no PII, no I/O — nothing to disclose |
| T-19-01-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan (only stdlib + existing dataclasses); no `## Package Legitimacy Audit` needed because no install tasks exist |
</threat_model>

<verification>
- `poetry run pytest tests/unit/test_falsifier_core_recall.py -q` passes (all classes).
- `poetry run mypy app/loop/falsifier_core.py` reports no new errors (typed signatures, frozen dataclass).
- `git diff --quiet scripts/loop_falsifier.py` exits 0 (falsifier untouched — D-07).
- No DB/network/LLM is reachable from `app/loop/falsifier_core.py` (import-contract grep clean).
</verification>

<success_criteria>
- `compute_recall_at_k` + `RecallAtKResult` mirror `compute_hit_rate` house style (IN-02 assertion, empty-input guard, frozen dataclass).
- `FLOOR` is a runtime-tunable module constant defaulting to 0.0 (strict-positive-delta first run, D-05).
- `decide_loop_exit` is pure, combines strict-positive-delta AND `after_rate >= floor`, and maps guard/empty-diff to EXIT_INFRA.
- All new logic is unit-tested at zero API cost (D-06 pure-core half).
</success_criteria>

<output>
Create `.planning/phases/19-productionized-loop-metric-loop/19-01-SUMMARY.md` when done.
</output>
