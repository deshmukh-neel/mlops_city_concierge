---
phase: 15-gate-promotion-baseline-regen
plan: 04
type: execute
wave: 4
depends_on: ["15-03"]
files_modified:
  - docs/promotion_decision.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
autonomous: true
requirements: [PROMO-01, PROMO-02, PROMO-03]
must_haves:
  truths:
    - "docs/promotion_decision.md is finalized as the single v2.2 milestone-closing record: it holds the A2 retest delta, the root-cause findings, the gate-promotion decisions, the baseline regen provenance, and the PROMO-03 latency report in one coherent document"
    - "docs/promotion_decision.md CROSS-LINKS both docs/decisiveness_arm_verdicts.md and docs/replay_arm_verdicts.md as immutable inputs; it does NOT append to or modify either closed verdict doc"
    - "The INST-05 verdict sentence in the Milestone Closing Summary is DATA-DEPENDENT on the measured Plan 02 A2 retest outcome read from docs/promotion_decision.md (no arm cleared INST-05 if A2 did not clear 0.6; forced-experiment-cleared-but-not-prod-default if A2 cleared only under FORCED_COMMIT_STEP=6; flag-off-cleared if the prod-default config itself cleared) — the honest-null framing remains the EXPECTED outcome but is NOT hardcoded as certain"
    - "The locked-regardless statements stay: gpt-4o-mini anchor ratified; ARCH-FUT-01 deferred as tracked debt (not executed); prod-default FORCED_COMMIT_STEP=6 flip flagged as a separate likely-deferred decision (not implemented); enforced gate thresholds live in configs/eval_gates.yaml (measured rates may appear in the doc but the yaml is the source of truth)"
    - "Milestone-close bookkeeping flips PROMO-01/02/03 to Complete in the REQUIREMENTS.md traceability table rows, marks Phase 15 complete in ROADMAP.md, and updates STATE.md — committed straight to main (no PR) after a clean-worktree / focused-commit preflight"
  artifacts:
    - path: "docs/promotion_decision.md"
      provides: "the finalized v2.2 milestone-audit anchor doc with all five Phase-15 sections + cross-links to both immutable verdict docs + the data-dependent INST-05 verdict"
      contains: "decisiveness_arm_verdicts.md"
      min_lines: 80
  key_links:
    - from: "docs/promotion_decision.md"
      to: "docs/decisiveness_arm_verdicts.md + docs/replay_arm_verdicts.md"
      via: "markdown cross-links (immutable inputs, not appended)"
      pattern: "replay_arm_verdicts.md"
    - from: ".planning/REQUIREMENTS.md traceability"
      to: "PROMO-01/02/03 rows = Complete"
      via: "status flip committed to main (no PR) after a focused-commit preflight"
      pattern: "PROMO-0"
---

<objective>
Finalize docs/promotion_decision.md as the single v2.2 milestone-closing record (D-15-03) — stitching together the root-cause findings (Plan 01), the A2 retest delta (Plan 02), the gate-promotion decisions, baseline regen provenance, and the PROMO-03 latency report (Plan 03) into one coherent audit anchor that cross-links both immutable verdict docs without appending to them, with a DATA-DEPENDENT INST-05 verdict driven by the measured Plan 02 outcome. Then run milestone-close bookkeeping: flip PROMO-01/02/03 to Complete and update ROADMAP/REQUIREMENTS/STATE straight to main (no PR) after a clean-worktree / focused-commit preflight.

Purpose: This is the last step of the D-15-09 pipeline. docs/promotion_decision.md must be the single doc a reviewer reads to understand how the v2.2 milestone closed: the INST-05 verdict (data-dependent on the A2 retest), anchor ratified, ARCH-FUT-01 deferred, what is enforced vs logged, and the latency budget reality. The honest-null framing remains the EXPECTED outcome — but the verdict sentence must reflect the actual measured A2 result, not a pre-written certainty, because the A2 retest outcome is not known until Plan 02 completes. The closed verdict docs (decisiveness + replay) stay immutable — cross-linked as inputs, never appended (mirroring the Phase-14 closed-record discipline). The bookkeeping commits go straight to main per the established convention, after a preflight that confirms only the intended files are staged.
Output: finalized docs/promotion_decision.md + updated ROADMAP/REQUIREMENTS/STATE.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/15-gate-promotion-baseline-regen/15-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Finalize docs/promotion_decision.md with cross-links + a DATA-DEPENDENT INST-05 verdict</name>
  <files>docs/promotion_decision.md</files>
  <read_first>
    - docs/promotion_decision.md (all sections written by Plans 01-03: root-cause, fix disposition, A2 retest delta + the measured A2 Retest Delta outcome, Run #2 provenance, gate promotion decisions, baseline regen provenance, PROMO-03 latency report). The A2 Retest Delta section is the SOURCE OF TRUTH for the data-dependent verdict below.
    - docs/decisiveness_arm_verdicts.md (Phase-13 DEC-05 record — immutable; read the header + INST-05 definition + A1/A2/A3 numbers for accurate cross-linking; do NOT append or modify)
    - docs/replay_arm_verdicts.md (Phase-14 REPLAY-05 record — immutable; read the closing verdict + ARCH-FUT-01 evaluation + the "USER CHECKPOINT RESOLVED 2026-06-12" block for accurate cross-linking; do NOT append or modify)
    - configs/eval_gates.yaml (the source of truth for enforced gate thresholds — link to it, do NOT duplicate the numbers into the doc)
    - .planning/phases/15-gate-promotion-baseline-regen/15-CONTEXT.md (D-15-03: new doc is the single milestone-closing record; cross-links both closed verdict docs as immutable inputs; the v2.2 milestone-audit anchor; D-15-01: A2 is a measured input, the verdict follows the data)
  </read_first>
  <action>Finalize docs/promotion_decision.md so it reads as a single coherent v2.2 milestone-closing record. Add a top-of-doc header/intro stating its role (the v2.2 milestone-audit anchor, D-15-03) and an explicit "## Inputs (immutable cross-links)" section that markdown-links docs/decisiveness_arm_verdicts.md (Phase-13 DEC-05 plateau record) and docs/replay_arm_verdicts.md (Phase-14 REPLAY-05 record incl. the ARCH-FUT-01 evaluation and resolved user checkpoint) as immutable inputs — with a one-line note that this doc does NOT append to or modify either. Ensure the five required content blocks are present and coherent (root-cause findings, A2 retest delta, gate-promotion decisions, baseline regen provenance, PROMO-03 latency report) — fold/rename sections from Plans 01-03 as needed for a clean read. CLARIFY (do not contradict prior sections): measured commit rates and deltas MAY appear in this doc, but the enforced gate THRESHOLDS live in configs/eval_gates.yaml as the source of truth — link to the yaml rather than re-asserting the enforced numbers. Add a "## Milestone Closing Summary" whose INST-05 verdict sentence is DATA-DEPENDENT on the measured A2 Retest Delta (read from this same doc): (a) if the A2 retest did NOT clear 0.6 -> "no arm cleared the INST-05 falsifier (honest null result)"; (b) if A2 cleared experimentally under FORCED_COMMIT_STEP=6 but the flag-off prod config did NOT -> "the milestone's prod-default config did not clear INST-05; the forced-commit experiment cleared it but is not the prod default (flagged, deferred per D-15-07)"; (c) if the flag-off prod config ITSELF cleared -> record that outcome honestly. The honest-null framing (case a) is the EXPECTED outcome and the document should frame it as such, but the verdict sentence MUST reflect the actual measured result, not a hardcoded certainty. Keep the locked-regardless statements unchanged: gpt-4o-mini anchor RATIFIED; ARCH-FUT-01 DEFERRED as tracked technical debt with the Phases 13-14 evidence chain as trigger criteria (NOT executed); a prod-default flip to FORCED_COMMIT_STEP=6 is a SEPARATE, likely-deferred decision flagged but NOT implemented; and what is enforced vs logged per Phase 15 (linking eval_gates.yaml). Do NOT touch docs/decisiveness_arm_verdicts.md or docs/replay_arm_verdicts.md — verify with `git diff --stat` that neither closed verdict doc changed.</action>
  <verify>
    <automated>grep -q "decisiveness_arm_verdicts.md" docs/promotion_decision.md && grep -q "replay_arm_verdicts.md" docs/promotion_decision.md && grep -qi "INST-05" docs/promotion_decision.md && (git diff --stat docs/decisiveness_arm_verdicts.md docs/replay_arm_verdicts.md | grep -q . && echo "FAIL: verdict doc changed" || echo "OK: verdict docs immutable")</automated>
  </verify>
  <acceptance_criteria>
    - docs/promotion_decision.md contains an "## Inputs (immutable cross-links)" (or equivalent) section that markdown-links BOTH docs/decisiveness_arm_verdicts.md and docs/replay_arm_verdicts.md and states it does not append to them
    - docs/promotion_decision.md contains all five Phase-15 content blocks coherently: root-cause findings, A2 retest delta, gate-promotion decisions, baseline regen provenance, PROMO-03 latency report
    - docs/promotion_decision.md has a "## Milestone Closing Summary" whose INST-05 verdict sentence matches the MEASURED A2 Retest Delta outcome (case a no-arm-cleared / case b forced-experiment-cleared-not-prod-default / case c flag-off-cleared) — NOT a hardcoded "no arm cleared" string asserted before the measurement
    - The Closing Summary keeps the locked-regardless statements: anchor ratified, ARCH-FUT-01 deferred (not executed), prod-default flip flagged-not-implemented
    - The doc clarifies that enforced gate thresholds live in configs/eval_gates.yaml (links to it); measured rates may appear but the yaml is the source of truth — gate threshold numbers are not duplicated as the authority
    - `git diff --stat docs/decisiveness_arm_verdicts.md docs/replay_arm_verdicts.md` shows NO changes (both immutable)
  </acceptance_criteria>
  <done>docs/promotion_decision.md is the finalized, coherent v2.2 milestone-audit anchor that cross-links both immutable verdict docs and records a DATA-DEPENDENT INST-05 verdict plus the ratified anchor and deferred ARCH-FUT-01 — without modifying any closed record.</done>
</task>

<task type="auto">
  <name>Task 2: Milestone-close bookkeeping (ROADMAP / REQUIREMENTS / STATE) straight to main, after a focused-commit preflight</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <read_first>
    - .planning/ROADMAP.md (Phase 15 entry + the Progress table row "15. Gate Promotion + Baseline Regen | v2.2 | 0/TBD | Not started"; the v2.2 milestone line)
    - .planning/REQUIREMENTS.md (traceability table rows "| PROMO-01 | Phase 15 | Pending |" etc.; the PROMO requirement checkboxes "- [ ] **PROMO-01**: ...")
    - .planning/STATE.md (frontmatter status/progress; Current Position; Decisions log)
    - docs/promotion_decision.md (the finalized record — the source of the closing facts, incl. the data-dependent INST-05 verdict)
  </read_first>
  <action>Update the three planning docs to reflect Phase 15 completion. In .planning/REQUIREMENTS.md: flip the traceability table rows so each reads `| PROMO-01 | Phase 15 | Complete |`, `| PROMO-02 | Phase 15 | Complete |`, `| PROMO-03 | Phase 15 | Complete |` (change Pending -> Complete on those three rows specifically), and check the three PROMO requirement boxes (`- [x] **PROMO-01**: ...` etc.). Add a one-line outcome note per requirement where the format allows (baselines regenerated honest n=5 / gates promoted-or-logged per measurement / latency report written with ratify-anchor recommendation). In .planning/ROADMAP.md: mark "Phase 15: Gate Promotion + Baseline Regen" complete (checkbox + completed date), update the Progress table row to the actual plan count (4/4) and Status Complete, and update the v2.2 milestone line / Last-updated footer to note the milestone closed (use the data-dependent INST-05 verdict from docs/promotion_decision.md — anchor ratified, ARCH-FUT-01 deferred). In .planning/STATE.md: update the frontmatter status + progress, the Current Position block, and append a Decisions-log line summarizing the Phase 15 outcome (the measured A2 retest result, gate promotion outcome, baselines regenerated, ARCH-FUT-01 deferred). PREFLIGHT before committing: run `git status --short` and confirm ONLY the three intended planning files (.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md) are staged for the bookkeeping commit — no unrelated changes swept in. Confirm the two closed verdict docs are UNCHANGED (`git diff --stat docs/decisiveness_arm_verdicts.md docs/replay_arm_verdicts.md` empty). Then commit these bookkeeping changes straight to main with single-line focused commit messages (no branch, no PR) per the established bookkeeping convention (pre-commit handles ruff; do not run ruff manually). Reference docs/promotion_decision.md as the canonical Phase-15 record in the commit body if useful.</action>
  <verify>
    <automated>grep -Eq '\|\s*PROMO-01\s*\|\s*Phase 15\s*\|\s*Complete\s*\|' .planning/REQUIREMENTS.md && grep -Eq '\|\s*PROMO-02\s*\|\s*Phase 15\s*\|\s*Complete\s*\|' .planning/REQUIREMENTS.md && grep -Eq '\|\s*PROMO-03\s*\|\s*Phase 15\s*\|\s*Complete\s*\|' .planning/REQUIREMENTS.md && (git diff --stat docs/decisiveness_arm_verdicts.md docs/replay_arm_verdicts.md | grep -q . && echo "FAIL: verdict doc changed" || echo "OK: verdict docs immutable")</automated>
  </verify>
  <acceptance_criteria>
    - .planning/REQUIREMENTS.md traceability rows show `| PROMO-01 | Phase 15 | Complete |`, `| PROMO-02 | Phase 15 | Complete |`, `| PROMO-03 | Phase 15 | Complete |` (asserted by the regex grep above, not just that the strings "PROMO-01"/"Complete" appear somewhere); the three PROMO requirement checkboxes are checked
    - .planning/ROADMAP.md: Phase 15 marked complete (checkbox + date); Progress table row shows 4/4 and Status Complete; the v2.2 milestone line/footer notes the close using the data-dependent INST-05 verdict (anchor ratified, ARCH-FUT-01 deferred)
    - .planning/STATE.md: frontmatter status/progress updated, Current Position reflects Phase 15 complete, and a Decisions-log line records the measured Phase-15 outcome
    - PREFLIGHT confirmed: `git status --short` showed only the three intended planning files staged; the two closed verdict docs are unchanged (`git diff --stat docs/decisiveness_arm_verdicts.md docs/replay_arm_verdicts.md` empty)
    - Bookkeeping changes are committed straight to main (no branch, no PR) per convention; commit messages are single-line and focused
    - docs/promotion_decision.md remains the canonical Phase-15 record (referenced, not duplicated, in the planning docs)
  </acceptance_criteria>
  <done>The milestone-close bookkeeping is committed to main after a focused-commit preflight: PROMO-01/02/03 traceability rows flipped to Complete, Phase 15 marked done in ROADMAP, STATE updated with the measured Phase-15 outcome — closing the v2.2 milestone on its data-dependent INST-05 verdict with no unrelated changes swept in.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan finalizes a docs file and updates planning bookkeeping (ROADMAP/REQUIREMENTS/STATE). No new request surface, no external input, no live spend, no auth/network/secret handling. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-06 | Tampering | closed verdict docs immutability | mitigate | The plan explicitly verifies via `git diff --stat` that docs/decisiveness_arm_verdicts.md and docs/replay_arm_verdicts.md are NOT modified; the finalized record cross-links them as immutable inputs only. |
| T-15-08 | Tampering | straight-to-main bookkeeping scope | mitigate | A focused-commit preflight (`git status --short`) confirms ONLY the three intended planning files are staged before the no-PR commit, so unrelated working-tree changes are not swept into main. |
| T-15-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan; no `## Package Legitimacy Audit` required. |
</threat_model>

<verification>
- docs/promotion_decision.md finalized with all five content blocks + immutable cross-links + a data-dependent INST-05 verdict; both verdict docs unchanged
- REQUIREMENTS.md PROMO-01/02/03 traceability rows = Complete (asserted by regex); ROADMAP Phase 15 marked done (4/4); STATE updated
- Focused-commit preflight confirmed before committing straight to main per convention
</verification>

<success_criteria>
The v2.2 milestone closes cleanly: docs/promotion_decision.md is the single coherent audit anchor cross-linking both immutable verdict docs, the INST-05 verdict is data-dependent on the measured A2 retest (honest-null as the expected but not hardcoded outcome), the ratified anchor + deferred ARCH-FUT-01 are recorded, and PROMO-01/02/03 are flipped to Complete in the planning docs after a focused-commit preflight.
</success_criteria>

<output>
Create `.planning/phases/15-gate-promotion-baseline-regen/15-04-SUMMARY.md` when done.
</output>
</content>
