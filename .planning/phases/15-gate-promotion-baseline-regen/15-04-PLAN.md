---
phase: 15-gate-promotion-baseline-regen
plan: 04
type: execute
wave: 4
depends_on: ["15-03"]
files_modified:
  - docs/promotion_decision.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
autonomous: true
requirements: [PROMO-01, PROMO-02, PROMO-03]
must_haves:
  truths:
    - "docs/promotion_decision.md is finalized as the single v2.2 milestone-closing record: it holds the A2 retest delta, the root-cause findings, the gate-promotion decisions, the baseline regen provenance, and the PROMO-03 latency report in one coherent document"
    - "docs/promotion_decision.md CROSS-LINKS both docs/decisiveness_arm_verdicts.md and docs/replay_arm_verdicts.md as immutable inputs; it does NOT append to or modify either closed verdict doc"
    - "The doc records the honest null-result framing: no arm cleared INST-05; gpt-4o-mini anchor ratified; ARCH-FUT-01 deferred as tracked debt; prod-default FORCED_COMMIT_STEP=6 flip flagged as a separate likely-deferred decision (not implemented)"
    - "Milestone-close bookkeeping flips PROMO-01/02/03 to Complete in REQUIREMENTS.md traceability, marks Phase 15 complete in ROADMAP.md, and updates STATE.md — committed straight to main (no PR) per the bookkeeping convention"
  artifacts:
    - path: "docs/promotion_decision.md"
      provides: "the finalized v2.2 milestone-audit anchor doc with all five Phase-15 sections + cross-links to both immutable verdict docs"
      contains: "decisiveness_arm_verdicts.md"
      min_lines: 80
  key_links:
    - from: "docs/promotion_decision.md"
      to: "docs/decisiveness_arm_verdicts.md + docs/replay_arm_verdicts.md"
      via: "markdown cross-links (immutable inputs, not appended)"
      pattern: "replay_arm_verdicts.md"
    - from: ".planning/REQUIREMENTS.md traceability"
      to: "PROMO-01/02/03 = Complete"
      via: "status flip committed to main (no PR)"
      pattern: "PROMO-0"
---

<objective>
Finalize docs/promotion_decision.md as the single v2.2 milestone-closing record (D-15-03) — stitching together the root-cause findings (Plan 01), the A2 retest delta (Plan 02), the gate-promotion decisions, baseline regen provenance, and the PROMO-03 latency report (Plan 03) into one coherent audit anchor that cross-links both immutable verdict docs without appending to them. Then run milestone-close bookkeeping: flip PROMO-01/02/03 to Complete and update ROADMAP/REQUIREMENTS/STATE straight to main (no PR).

Purpose: This is the last step of the D-15-09 pipeline. docs/promotion_decision.md must be the single doc a reviewer reads to understand how the v2.2 milestone closed: honest null result, anchor ratified, ARCH-FUT-01 deferred, what is enforced vs logged, and the latency budget reality. The closed verdict docs (decisiveness + replay) stay immutable — cross-linked as inputs, never appended (mirroring the Phase-14 closed-record discipline). The bookkeeping commits go straight to main per the established convention.
Output: finalized docs/promotion_decision.md + updated ROADMAP/REQUIREMENTS/STATE.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/15-gate-promotion-baseline-regen/15-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Finalize docs/promotion_decision.md as the milestone-closing record with cross-links</name>
  <files>docs/promotion_decision.md</files>
  <read_first>
    - docs/promotion_decision.md (all sections written by Plans 01-03: root-cause, fix disposition, A2 retest delta, Run #2 provenance, gate promotion decisions, baseline regen provenance, PROMO-03 latency report)
    - docs/decisiveness_arm_verdicts.md (Phase-13 DEC-05 record — immutable; read the header + INST-05 definition + A1/A2/A3 numbers for accurate cross-linking; do NOT append or modify)
    - docs/replay_arm_verdicts.md (Phase-14 REPLAY-05 record — immutable; read the closing verdict + ARCH-FUT-01 evaluation + the "USER CHECKPOINT RESOLVED 2026-06-12" block for accurate cross-linking; do NOT append or modify)
    - .planning/phases/15-gate-promotion-baseline-regen/15-CONTEXT.md (D-15-03: new doc is the single milestone-closing record; cross-links both closed verdict docs as immutable inputs; the v2.2 milestone-audit anchor)
  </read_first>
  <action>Finalize docs/promotion_decision.md so it reads as a single coherent v2.2 milestone-closing record. Add a top-of-doc header/intro stating its role (the v2.2 milestone-audit anchor, D-15-03) and an explicit "## Inputs (immutable cross-links)" section that markdown-links docs/decisiveness_arm_verdicts.md (Phase-13 DEC-05 plateau record) and docs/replay_arm_verdicts.md (Phase-14 REPLAY-05 record incl. the ARCH-FUT-01 evaluation and resolved user checkpoint) as immutable inputs — with a one-line note that this doc does NOT append to or modify either. Ensure the five required content blocks are present and coherent (root-cause findings, A2 retest delta, gate-promotion decisions, baseline regen provenance, PROMO-03 latency report) — fold/rename sections from Plans 01-03 as needed for a clean read; do NOT duplicate gate numbers that live in configs/eval_gates.yaml (link to it). Add a "## Milestone Closing Summary" stating: no arm cleared the INST-05 falsifier (honest null result); gpt-4o-mini anchor RATIFIED; ARCH-FUT-01 DEFERRED as tracked technical debt with the Phases 13-14 evidence chain as trigger criteria (NOT executed); a prod-default flip to FORCED_COMMIT_STEP=6 is a SEPARATE, likely-deferred decision flagged but NOT implemented; and what is enforced vs logged per Phase 15. Do NOT touch docs/decisiveness_arm_verdicts.md or docs/replay_arm_verdicts.md — verify with `git diff --stat` that neither closed verdict doc changed.</action>
  <verify>
    <automated>grep -q "decisiveness_arm_verdicts.md" docs/promotion_decision.md && grep -q "replay_arm_verdicts.md" docs/promotion_decision.md && (git diff --stat docs/decisiveness_arm_verdicts.md docs/replay_arm_verdicts.md | grep -q . && echo "FAIL: verdict doc changed" || echo "OK: verdict docs immutable")</automated>
  </verify>
  <acceptance_criteria>
    - docs/promotion_decision.md contains an "## Inputs (immutable cross-links)" (or equivalent) section that markdown-links BOTH docs/decisiveness_arm_verdicts.md and docs/replay_arm_verdicts.md and states it does not append to them
    - docs/promotion_decision.md contains all five Phase-15 content blocks coherently: root-cause findings, A2 retest delta, gate-promotion decisions, baseline regen provenance, PROMO-03 latency report
    - docs/promotion_decision.md has a "## Milestone Closing Summary" recording: honest null result, anchor ratified, ARCH-FUT-01 deferred (not executed), prod-default flip flagged-not-implemented
    - `git diff --stat docs/decisiveness_arm_verdicts.md docs/replay_arm_verdicts.md` shows NO changes (both immutable)
    - Gate numbers are NOT duplicated into the doc (it links to configs/eval_gates.yaml as the source of truth)
  </acceptance_criteria>
  <done>docs/promotion_decision.md is the finalized, coherent v2.2 milestone-audit anchor that cross-links both immutable verdict docs and records the honest null result, ratified anchor, and deferred ARCH-FUT-01 — without modifying any closed record.</done>
</task>

<task type="auto">
  <name>Task 2: Milestone-close bookkeeping (ROADMAP / REQUIREMENTS / STATE) straight to main</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <read_first>
    - .planning/ROADMAP.md (Phase 15 entry + the Progress table row "15. Gate Promotion + Baseline Regen | v2.2 | 0/TBD | Not started"; the v2.2 milestone line)
    - .planning/REQUIREMENTS.md (traceability table: PROMO-01/02/03 = Phase 15 Pending; the PROMO requirement checkboxes)
    - .planning/STATE.md (frontmatter status/progress; Current Position; Decisions log)
    - docs/promotion_decision.md (the finalized record — the source of the closing facts)
  </read_first>
  <action>Update the three planning docs to reflect Phase 15 completion. In .planning/REQUIREMENTS.md: flip PROMO-01, PROMO-02, PROMO-03 to Complete in the traceability table (with a one-line status note: baselines regenerated honest n=5 / gates promoted-or-logged per measurement / latency report written with ratify-anchor recommendation) and check the three PROMO requirement boxes. In .planning/ROADMAP.md: mark "Phase 15: Gate Promotion + Baseline Regen" complete (checkbox + completed date), update the Progress table row to the actual plan count (4/4) and Status Complete, and update the v2.2 milestone line / Last-updated footer to note the milestone closed with an honest null result (anchor ratified, ARCH-FUT-01 deferred). In .planning/STATE.md: update the frontmatter status + progress, the Current Position block, and append a Decisions-log line summarizing the Phase 15 outcome (A2 retest result, gate promotion outcome, baselines regenerated, ARCH-FUT-01 deferred). Commit these bookkeeping changes straight to main with single-line focused commit messages (no branch, no PR) per the established bookkeeping convention (pre-commit handles ruff; do not run ruff manually). Reference docs/promotion_decision.md as the canonical Phase-15 record in the commit body if useful.</action>
  <verify>
    <automated>grep -q "PROMO-01" .planning/REQUIREMENTS.md && grep -iq "Complete" .planning/REQUIREMENTS.md && grep -q "15" .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - .planning/REQUIREMENTS.md traceability: PROMO-01, PROMO-02, PROMO-03 all show Complete (Phase 15) with a one-line outcome note; the three PROMO checkboxes are checked
    - .planning/ROADMAP.md: Phase 15 marked complete (checkbox + date); Progress table row shows 4/4 and Status Complete; the v2.2 milestone line/footer notes the honest-null close (anchor ratified, ARCH-FUT-01 deferred)
    - .planning/STATE.md: frontmatter status/progress updated, Current Position reflects Phase 15 complete, and a Decisions-log line records the Phase-15 outcome
    - Bookkeeping changes are committed straight to main (no branch, no PR) per convention; commit messages are single-line and focused
    - docs/promotion_decision.md remains the canonical Phase-15 record (referenced, not duplicated, in the planning docs)
  </acceptance_criteria>
  <done>The milestone-close bookkeeping is committed to main: PROMO-01/02/03 flipped to Complete, Phase 15 marked done in ROADMAP, STATE updated with the Phase-15 outcome — closing the v2.2 milestone on an honest null result.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan finalizes a docs file and updates planning bookkeeping (ROADMAP/REQUIREMENTS/STATE). No new request surface, no external input, no live spend, no auth/network/secret handling. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-06 | Tampering | closed verdict docs immutability | mitigate | The plan explicitly verifies via `git diff --stat` that docs/decisiveness_arm_verdicts.md and docs/replay_arm_verdicts.md are NOT modified; the finalized record cross-links them as immutable inputs only. |
| T-15-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan; no `## Package Legitimacy Audit` required. |
</threat_model>

<verification>
- docs/promotion_decision.md finalized with all five content blocks + immutable cross-links; both verdict docs unchanged
- REQUIREMENTS.md PROMO-01/02/03 = Complete; ROADMAP Phase 15 marked done (4/4); STATE updated
- Bookkeeping committed straight to main per convention
</verification>

<success_criteria>
The v2.2 milestone closes cleanly: docs/promotion_decision.md is the single coherent audit anchor cross-linking both immutable verdict docs, the honest null result + ratified anchor + deferred ARCH-FUT-01 are recorded, and PROMO-01/02/03 are flipped to Complete in the planning docs.
</success_criteria>

<output>
Create `.planning/phases/15-gate-promotion-baseline-regen/15-04-SUMMARY.md` when done.
</output>
