---
phase: 15-gate-promotion-baseline-regen
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/promotion_decision.md
  - app/agent/graph.py
  # CONDITIONAL (D-15-08 trivial fix only): if a graph.py fix ships, the regression
  # test below MUST also land in the same plan so the executor's commit and the
  # check_baselines_fresh.py staleness watch-set both account for it. If the fix is
  # DEFERRED (the likely outcome), neither graph.py nor this test file is touched.
  - tests/unit/test_graph_forced_commit.py
autonomous: true
requirements: [PROMO-02]
must_haves:
  truths:
    - "A written root-cause section exists in docs/promotion_decision.md that explains why gpt-5-mini commits at 1.000 on omakase but 0.000 on refinement_cheaper, grounded ONLY in existing Phase-13/14 run JSONs (zero new live spend)"
    - "The diagnostic reports the ACTUAL per-step telemetry values read from named existing run JSONs (viable_candidates_per_step, rule8_met_per_step, first_commit_call_step, forced_commit_step, commit_forced) — including any value that contradicts the expected pattern — NOT pre-baked expected arrays"
    - "The diagnostic states whether the forced-commit path can fire on refinement_cheaper at step 6 given the all_slots_viable gate, using the recorded rule8_met_per_step evidence actually present in the JSONs"
    - "The diagnostic names exactly which run dirs / cell JSONs were inspected (and which were not), so the 'across every arm' claim is scoped to the evidence actually examined"
    - "A D-15-08 fix disposition is recorded: either a one-flag/one-line/low-risk fix is shipped behind the A2 retest (with its regression test), OR the cause is documented as a finding and DEFERRED as tracked debt (NOT re-opening ARCH-FUT-01)"
  artifacts:
    - path: "docs/promotion_decision.md"
      provides: "NEW milestone-closing record scaffold with the refinement_cheaper root-cause section filled (actual telemetry values) and the D-15-08 fix disposition recorded"
      contains: "Root-Cause"
      min_lines: 40
  key_links:
    - from: "eval_reports/2026-06-12T07-27-03Z (A2 run dir) gpt-5-mini refinement + omakase JSONs"
      to: "docs/promotion_decision.md root-cause section"
      via: "actual cited viable_candidates_per_step / rule8_met_per_step / forced_commit_step values read from the JSONs"
      pattern: "rule8_met_per_step|viable_candidates_per_step"
---

<objective>
Produce the refinement_cheaper root-cause diagnostic (D-15-08) from EXISTING Phase-13/14 run JSONs with ZERO live API spend, write it into the NEW docs/promotion_decision.md milestone-closing record, and record the fix disposition: ship a one-flag/one-line/low-risk fix behind the A2 retest, OR document the cause and defer it as tracked debt. This is step 1 of the D-15-09 fixed pipeline — it MUST land before any live run so the A2 retest (Plan 02) measures the FINAL post-fix code.

Purpose: The milestone produced an honest null result. The single open diagnostic question is WHY gpt-5-mini commits 1.000 on omakase but 0.000 on refinement_cheaper across every arm. The existing run JSONs already carry the telemetry needed to answer it (viable_candidates_per_step, rule8_met_per_step, forced_commit_step) — no new spend is required, and the diagnose-before-retest ordering prevents wasting a live run against the <=4-run cap. The diagnostic MUST be data-driven: report the values actually present in the JSONs, not a predetermined conclusion.
Output: docs/promotion_decision.md created with the root-cause section + the D-15-08 fix disposition; optionally a trivial graph.py fix (with its regression test) if and only if it meets the one-flag/one-line/low-risk/clearly-isolated bar.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-gate-promotion-baseline-regen/15-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Diagnose the refinement_cheaper 0.000 pattern from existing run JSONs (zero live spend, data-driven)</name>
  <files>docs/promotion_decision.md</files>
  <read_first>
    - eval_reports/2026-06-12T07-27-03Z/openai--gpt-5-mini--refinement_cheaper--run-0.json (and run-1..4) — the A2 run dir gpt-5-mini refinement cells; read queries[0].deterministic.viable_candidates_per_step, .rule8_met_per_step, .first_commit_call_step, .forced_commit_step, .commit_forced, .arm_flags (the telemetry is nested under queries[i].deterministic, NOT at top level)
    - eval_reports/2026-06-12T07-27-03Z/openai--gpt-5-mini--omakase_mission_open_ended--run-0.json (and run-1..4) — the same fields on the scenario where gpt-5-mini commits, for contrast
    - app/agent/graph.py (lines ~313-335 flag reads: FORCED_COMMIT_STEP / VIABILITY_CONTRACT_ENABLED / PARALLEL_TOOL_EXECUTION_ENABLED / REPLAY_MULTI_MESSAGE_ENABLED / REPLAY_CONTENT_BLOCKS_ENABLED; ~628-695 forced-commit branch) — the all_slots_viable(state, LOW_SIMILARITY_THRESHOLD) gate condition the forced path requires before firing
    - app/agent/viability.py — all_slots_viable / best_viable_candidate_per_slot definitions (the gate's viability condition)
    - docs/decisiveness_arm_verdicts.md — the Phase-13 A2 verdict + CR-01 annotation (forced mechanism never fired; 0.500 was model-initiated)
    - docs/replay_arm_verdicts.md — the closing ARCH-FUT-01 evaluation paragraph naming refinement_cheaper as the blocking pattern (immutable; read for context, do NOT append)
  </read_first>
  <action>Write a one-off zero-spend analysis (an inline python reading loop or a throwaway script — your discretion; do NOT add a committed script) over the named A2 run-dir JSONs to tabulate, per gpt-5-mini run, the ACTUAL queries[i].deterministic.viable_candidates_per_step and queries[i].deterministic.rule8_met_per_step arrays recorded for BOTH scenarios. Report the values you actually read — do NOT pre-fill expected arrays. The arrays below are illustrative SHAPE examples only, to show the format; the executor MUST paste the real values from the JSONs even if they differ or contradict the expected pattern. (Illustrative shape, not a finding: an omakase run might show rule8_met_per_step flipping to True at/after step 6 like [False,False,False,False,False,True,True,True] while a refinement run might stay all-False like [False,False,False,False]; if the actual JSON shows something else, report the actual.) Establish the contrast from the actual data and answer the two D-15-08 diagnostic questions explicitly: (1) does the refinement prompt structure prevent viable candidates from appearing at step 6 — yes/no with the recorded viable_candidates_per_step counts as evidence; (2) are all slots truly viable per the gate's all_slots_viable condition at step 6 on refinement — answer from the actual rule8_met_per_step values. Also report the forced-path telemetry actually present: state the recorded forced_commit_step and commit_forced values alongside arm_flags.forced_commit_step — if (as in the Phase-13 A2 dir) forced_commit_step=None and commit_forced=False despite arm_flags.forced_commit_step=6, that confirms the forced path never fired (CR-01), which is exactly what the A2 retest in Plan 02 verifies on the post-fix synthesizer; if the data shows otherwise, report that instead. In the doc, NAME exactly which run dirs / cell JSONs you inspected (e.g. "inspected eval_reports/2026-06-12T07-27-03Z gpt-5-mini omakase + refinement run-0..4") and which you did NOT (e.g. Phase-14 REPLAY run dirs), so the "across every arm" framing is scoped to the evidence examined. Do NOT run any live matrix, eval-matrix, or eval-agent command — this task reads committed artifacts only.</action>
  <verify>
    <automated>test -f docs/promotion_decision.md && grep -q "rule8_met_per_step" docs/promotion_decision.md && grep -q "viable_candidates_per_step" docs/promotion_decision.md</automated>
  </verify>
  <acceptance_criteria>
    - docs/promotion_decision.md exists and contains a "## Root-Cause: refinement_cheaper = 0.000" (or equivalently-titled) section
    - The section reports the ACTUAL telemetry values read from named existing run JSONs under eval_reports/2026-06-12T07-27-03Z (at least viable_candidates_per_step, rule8_met_per_step, first_commit_call_step, forced_commit_step, commit_forced), and explicitly flags any value that contradicts the expected omakase-commits/refinement-fails pattern rather than omitting it
    - The section states the omakase-vs-refinement contrast FROM THE ACTUAL DATA and answers both D-15-08 diagnostic questions with the recorded counts as evidence
    - The section records the forced-path telemetry actually present (forced_commit_step / commit_forced vs arm_flags.forced_commit_step) and, if it confirms the Phase-13 forced path never fired, ties that to the Plan 02 A2 retest motivation
    - The section NAMES which run dirs / cell JSONs were inspected and which were not (scoping the "across every arm" claim to examined evidence)
    - ZERO live spend: capture a before/after listing of the eval_reports/ directory (e.g. `ls -1 eval_reports/ | sort > /tmp/eval_before.txt` before the task and `ls -1 eval_reports/ | sort | diff /tmp/eval_before.txt -` after) and confirm NO new run dir appeared. (NOTE: eval_reports/ is gitignored, so `git status --porcelain eval_reports/` is NOT a valid zero-spend check — use the directory-listing snapshot.)
  </acceptance_criteria>
  <done>The root-cause section is written into docs/promotion_decision.md, grounded entirely in the actual values of existing run JSONs, answering why refinement_cheaper plateaus at 0.000 and recording the forced-path firing state observed in Phase 13.</done>
</task>

<task type="auto">
  <name>Task 2: Decide and record the D-15-08 fix disposition (trivial-fix-or-defer boundary)</name>
  <files>docs/promotion_decision.md, app/agent/graph.py + tests/unit/test_graph_forced_commit.py (edited ONLY if a trivial fix qualifies)</files>
  <read_first>
    - app/agent/graph.py (lines ~628-695 forced-commit branch; the all_slots_viable gate at ~650) — the only legitimate trivial-fix site
    - app/agent/viability.py — all_slots_viable / best_viable_candidate_per_slot; LOW_SIMILARITY_THRESHOLD usage
    - tests/unit/test_graph_forced_commit.py — the existing CR-01 forced-commit regression suite (extend here if a fix ships, mirroring its non-committing-mock pattern)
    - docs/promotion_decision.md (the root-cause section written in Task 1)
    - .planning/phases/15-gate-promotion-baseline-regen/15-CONTEXT.md (D-15-08 boundary: ship ONLY a one-flag/one-line/low-risk/clearly-isolated change; anything larger is documented + deferred as tracked debt; do NOT re-open ARCH-FUT-01)
  </read_first>
  <action>Apply the D-15-08 boundary to the Task-1 finding. The default is diagnostic-only. Ship a fix ONLY if it is genuinely one-flag/one-line/low-risk/clearly-isolated — e.g. the gate's viability condition is computed wrong, or FORCED_COMMIT_STEP=6 fires after the model has already terminated. If the finding is that refinement_cheaper never reaches all_slots_viable at step 6 (a scenario/candidate-availability property, NOT a one-line gate bug), that is a NON-trivial cause: record it as a documented finding and DEFER it as tracked technical debt with the Phases 13-14 evidence chain as the trigger — explicitly note this does NOT re-open or execute ARCH-FUT-01 (user-ratified deferral). If and only if a trivial fix qualifies, edit app/agent/graph.py with the minimal change, keep it behind the existing FORCED_COMMIT_STEP arm flag (so flag-off prod behavior is byte-identical and the anchor is unaffected), and add/extend a unit test in tests/unit/test_graph_forced_commit.py asserting the corrected forced-path firing on a mock that never calls commit_itinerary (mirroring the existing CR-01 regression test). Write a "## D-15-08 Fix Disposition" section in docs/promotion_decision.md recording the decision (SHIPPED with the one-line description AND the test name, or DEFERRED with the tracked-debt note) so Plan 02's A2 retest runs against the final post-fix code. Because any graph.py edit trips the check_baselines_fresh.py staleness watch-set, note in the disposition section that baseline regen (Plan 03) is sequenced LAST against final merged code per D-15-05.</action>
  <verify>
    <automated>grep -qi "Fix Disposition" docs/promotion_decision.md && (grep -qi "DEFERRED\|SHIPPED" docs/promotion_decision.md) && (if ! git diff --quiet -- app/agent/graph.py; then make test; fi)</automated>
  </verify>
  <acceptance_criteria>
    - docs/promotion_decision.md contains a "## D-15-08 Fix Disposition" section stating either SHIPPED (with the one-line change and the test name in tests/unit/test_graph_forced_commit.py) or DEFERRED (with the tracked-debt note and the explicit "does NOT re-open ARCH-FUT-01" statement)
    - If DEFERRED: no change to app/agent/graph.py or tests/unit/test_graph_forced_commit.py (`git diff --stat app/agent/graph.py tests/unit/test_graph_forced_commit.py` empty) and the disposition explains why the cause exceeds the one-flag/one-line/low-risk bar
    - If SHIPPED: the graph.py change is gated by the existing FORCED_COMMIT_STEP flag (flag-off path byte-identical), a unit test in tests/unit/test_graph_forced_commit.py fires the forced path on a non-committing mock, `make test-unit` passes, and the disposition records that baseline regen is sequenced last (D-15-05)
    - In BOTH cases ARCH-FUT-01 is NOT executed and the prod default is NOT flipped to FORCED_COMMIT_STEP=6
    - `make test` passes full-suite (DB-pool contamination risk — run the full suite, not just the changed file)
  </acceptance_criteria>
  <done>The fix disposition is recorded and honored: either a trivial flag-gated fix with a regression test shipped, or the cause documented and deferred as tracked debt, with ARCH-FUT-01 untouched and the prod default unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan reads existing eval-run JSONs and writes a docs file. An optional trivial graph.py fix is internal forced-commit-step logic behind an existing env flag — no new request surface, no external input, no auth/network/secret handling. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Tampering | app/agent/graph.py optional D-15-08 fix | accept | Internal forced-commit-step branch behind the existing FORCED_COMMIT_STEP flag; flag-off prod path stays byte-identical (verified by unit test in tests/unit/test_graph_forced_commit.py). No new attack surface — eval/docs/config-only phase. |
| T-15-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan; no `## Package Legitimacy Audit` required. |
</threat_model>

<verification>
- docs/promotion_decision.md exists with a root-cause section (actual telemetry values) and a fix-disposition section
- The root-cause section is grounded in named existing run JSONs (zero new eval_reports/ dir, verified by before/after directory listing)
- If a fix shipped: `make test` passes and the flag-off path is byte-identical
- ARCH-FUT-01 not executed; prod default not flipped
</verification>

<success_criteria>
The refinement_cheaper root cause is diagnosed from existing telemetry with zero live spend (actuals reported, not pre-baked), written into the new milestone-closing doc, and the trivial-fix-or-defer decision is recorded and honored — clearing the way for the Plan 02 live runs to execute against final post-fix code.
</success_criteria>

<output>
Create `.planning/phases/15-gate-promotion-baseline-regen/15-01-SUMMARY.md` when done.
</output>
</content>
</invoke>
