---
phase: 15-gate-promotion-baseline-regen
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/promotion_decision.md
  - app/agent/graph.py
autonomous: true
requirements: [PROMO-02]
must_haves:
  truths:
    - "A written root-cause section exists in docs/promotion_decision.md that explains why gpt-5-mini commits at 1.000 on omakase but 0.000 on refinement_cheaper, grounded ONLY in existing Phase-13/14 run JSONs (zero new live spend)"
    - "The diagnostic cites concrete per-step telemetry fields (viable_candidates_per_step, rule8_met_per_step, first_commit_call_step, forced_commit_step, commit_forced) from named existing run JSONs"
    - "The diagnostic states whether the forced-commit path can fire on refinement_cheaper at step 6 given the all_slots_viable gate, using the recorded rule8_met_per_step evidence"
    - "A D-15-08 fix disposition is recorded: either a one-flag/one-line/low-risk fix is shipped behind the A2 retest, OR the cause is documented as a finding and DEFERRED as tracked debt (NOT re-opening ARCH-FUT-01)"
  artifacts:
    - path: "docs/promotion_decision.md"
      provides: "NEW milestone-closing record scaffold with the refinement_cheaper root-cause section filled and the D-15-08 fix disposition recorded"
      contains: "Root-Cause"
      min_lines: 40
  key_links:
    - from: "eval_reports/2026-06-12T07-27-03Z (A2 run dir) gpt-5-mini refinement + omakase JSONs"
      to: "docs/promotion_decision.md root-cause section"
      via: "cited viable_candidates_per_step / rule8_met_per_step / forced_commit_step values"
      pattern: "rule8_met_per_step|viable_candidates_per_step"
---

<objective>
Produce the refinement_cheaper root-cause diagnostic (D-15-08) from EXISTING Phase-13/14 run JSONs with ZERO live API spend, write it into the NEW docs/promotion_decision.md milestone-closing record, and record the fix disposition: ship a one-flag/one-line/low-risk fix behind the A2 retest, OR document the cause and defer it as tracked debt. This is step 1 of the D-15-09 fixed pipeline — it MUST land before any live run so the A2 retest (Plan 02) measures the FINAL post-fix code.

Purpose: The milestone produced an honest null result. The single open diagnostic question is WHY gpt-5-mini commits 1.000 on omakase but 0.000 on refinement_cheaper across every arm. The existing run JSONs already carry the telemetry needed to answer it (viable_candidates_per_step, rule8_met_per_step, forced_commit_step) — no new spend is required, and the diagnose-before-retest ordering prevents wasting a live run against the ≤4-run cap.
Output: docs/promotion_decision.md created with the root-cause section + the D-15-08 fix disposition; optionally a trivial graph.py fix if and only if it meets the one-flag/one-line/low-risk/clearly-isolated bar.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-gate-promotion-baseline-regen/15-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Diagnose the refinement_cheaper 0.000 pattern from existing run JSONs (zero live spend)</name>
  <files>docs/promotion_decision.md</files>
  <read_first>
    - eval_reports/2026-06-12T07-27-03Z/openai--gpt-5-mini--refinement_cheaper--run-0.json (and run-1..4) — the A2 run dir gpt-5-mini refinement cells; read deterministic.viable_candidates_per_step, deterministic.rule8_met_per_step, deterministic.first_commit_call_step, deterministic.forced_commit_step, deterministic.commit_forced, deterministic.arm_flags
    - eval_reports/2026-06-12T07-27-03Z/openai--gpt-5-mini--omakase_mission_open_ended--run-0.json (and run-1..4) — the same fields on the scenario where gpt-5-mini commits, for contrast
    - app/agent/graph.py (lines ~313-330 flag reads, ~628-695 forced-commit branch) — the all_slots_viable(state, LOW_SIMILARITY_THRESHOLD) gate condition the forced path requires before firing
    - app/agent/viability.py — all_slots_viable / best_viable_candidate_per_slot definitions (the gate's viability condition)
    - docs/decisiveness_arm_verdicts.md — the Phase-13 A2 verdict + CR-01 annotation (forced mechanism never fired; 0.500 was model-initiated)
    - docs/replay_arm_verdicts.md — the closing ARCH-FUT-01 evaluation paragraph naming refinement_cheaper as the blocking pattern (immutable; read for context, do NOT append)
  </read_first>
  <action>Write a one-off zero-spend analysis (an inline python reading loop or a throwaway script — your discretion; do NOT add a committed script) over the named A2 run-dir JSONs to tabulate, per gpt-5-mini run, the recorded deterministic.viable_candidates_per_step and deterministic.rule8_met_per_step arrays for BOTH scenarios. Establish the contrast: on omakase rule8_met_per_step flips to True at/after step 6 in the recorded runs (e.g. run-0 omakase = [False,False,False,False,False,True,True,True]) while on refinement_cheaper rule8_met_per_step stays all-False (e.g. run-0 refinement = [False,False,False,False]) and viable_candidates_per_step never reaches one-viable-per-slot at step 6. Answer the two D-15-08 diagnostic questions explicitly: (1) does the refinement prompt structure prevent viable candidates from appearing at step 6 — yes/no with the recorded counts as evidence; (2) are all slots truly viable per the gate's all_slots_viable condition at step 6 on refinement — no, with rule8_met_per_step=all-False as the proof. Note the second decisive finding: even on omakase where rule8_met_per_step=True at step 6, the recorded forced_commit_step=None and commit_forced=False with arm_flags.forced_commit_step=6 — confirming the Phase-13 forced path never fired (CR-01), which is exactly what the A2 retest in Plan 02 verifies on the post-fix synthesizer. Do NOT run any live matrix, eval-matrix, or eval-agent command — this task reads committed artifacts only.</action>
  <verify>
    <automated>test -f docs/promotion_decision.md && grep -q "rule8_met_per_step" docs/promotion_decision.md && grep -q "viable_candidates_per_step" docs/promotion_decision.md</automated>
  </verify>
  <acceptance_criteria>
    - docs/promotion_decision.md exists and contains a "## Root-Cause: refinement_cheaper = 0.000" (or equivalently-titled) section
    - The section cites at least four of the named telemetry fields (viable_candidates_per_step, rule8_met_per_step, first_commit_call_step, forced_commit_step, commit_forced) with concrete values from named existing run JSONs under eval_reports/2026-06-12T07-27-03Z
    - The section states the omakase-vs-refinement contrast (rule8 met at step 6 on omakase, never met on refinement) and answers both D-15-08 diagnostic questions
    - The section records that the Phase-13 A2 forced path never fired (forced_commit_step=None despite arm_flags.forced_commit_step=6), motivating the Plan 02 A2 retest
    - NO new eval_reports/ run dir was created by this task (zero live spend): `git status --porcelain eval_reports/ | grep -q . && echo "FAIL: new run dir" || echo "OK: zero spend"` prints OK
  </acceptance_criteria>
  <done>The root-cause section is written into docs/promotion_decision.md, grounded entirely in existing run JSONs, answering why refinement_cheaper plateaus at 0.000 and confirming the forced path was inoperative in Phase 13.</done>
</task>

<task type="auto">
  <name>Task 2: Decide and record the D-15-08 fix disposition (trivial-fix-or-defer boundary)</name>
  <files>docs/promotion_decision.md, app/agent/graph.py (edited ONLY if a trivial fix qualifies)</files>
  <read_first>
    - app/agent/graph.py (lines ~628-695 forced-commit branch; the all_slots_viable gate at ~650) — the only legitimate trivial-fix site
    - app/agent/viability.py — all_slots_viable / best_viable_candidate_per_slot; LOW_SIMILARITY_THRESHOLD usage
    - docs/promotion_decision.md (the root-cause section written in Task 1)
    - .planning/phases/15-gate-promotion-baseline-regen/15-CONTEXT.md (D-15-08 boundary: ship ONLY a one-flag/one-line/low-risk/clearly-isolated change; anything larger is documented + deferred as tracked debt; do NOT re-open ARCH-FUT-01)
  </read_first>
  <action>Apply the D-15-08 boundary to the Task-1 finding. The default is diagnostic-only. Ship a fix ONLY if it is genuinely one-flag/one-line/low-risk/clearly-isolated — e.g. the gate's viability condition is computed wrong, or FORCED_COMMIT_STEP=6 fires after the model has already terminated. If the finding is that refinement_cheaper never reaches all_slots_viable at step 6 (a scenario/candidate-availability property, NOT a one-line gate bug), that is a NON-trivial cause: record it as a documented finding and DEFER it as tracked technical debt with the Phases 13-14 evidence chain as the trigger — explicitly note this does NOT re-open or execute ARCH-FUT-01 (user-ratified deferral). If and only if a trivial fix qualifies, edit app/agent/graph.py with the minimal change, keep it behind the existing FORCED_COMMIT_STEP arm flag (so flag-off prod behavior is byte-identical and the anchor is unaffected), and add/extend a unit test asserting the corrected forced-path firing on a mock that never calls commit_itinerary (mirroring the Phase 13-08 CR-01 regression test). Write a "## D-15-08 Fix Disposition" section in docs/promotion_decision.md recording the decision (SHIPPED with the one-line description, or DEFERRED with the tracked-debt note) so Plan 02's A2 retest runs against the final post-fix code. Because any graph.py edit trips the check_baselines_fresh.py staleness watch-set, note in the disposition section that baseline regen (Plan 03) is sequenced LAST against final merged code per D-15-05.</action>
  <verify>
    <automated>grep -qi "Fix Disposition" docs/promotion_decision.md && (grep -qi "DEFERRED\|SHIPPED" docs/promotion_decision.md) && (if ! git diff --quiet -- app/agent/graph.py; then make test; fi)</automated>
  </verify>
  <acceptance_criteria>
    - docs/promotion_decision.md contains a "## D-15-08 Fix Disposition" section stating either SHIPPED (with the one-line change and the test name) or DEFERRED (with the tracked-debt note and the explicit "does NOT re-open ARCH-FUT-01" statement)
    - If DEFERRED: no change to app/agent/graph.py (`git diff --stat app/agent/graph.py` empty) and the disposition explains why the cause exceeds the one-flag/one-line/low-risk bar
    - If SHIPPED: the graph.py change is gated by the existing FORCED_COMMIT_STEP flag (flag-off path byte-identical), a unit test fires the forced path on a non-committing mock, `make test-unit` passes, and the disposition records that baseline regen is sequenced last (D-15-05)
    - In BOTH cases ARCH-FUT-01 is NOT executed and the prod default is NOT flipped to FORCED_COMMIT_STEP=6
    - `make test` passes full-suite (DB-pool contamination risk — run the full suite, not just the changed file)
  </acceptance_criteria>
  <done>The fix disposition is recorded and honored: either a trivial flag-gated fix with a regression test shipped, or the cause documented and deferred as tracked debt, with ARCH-FUT-01 untouched and the prod default unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan reads existing eval-run JSONs and writes a docs file. An optional trivial graph.py fix is internal forced-commit-step logic behind an existing env flag — no new request surface, no external input, no auth/network/secret handling. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Tampering | app/agent/graph.py optional D-15-08 fix | accept | Internal forced-commit-step branch behind the existing FORCED_COMMIT_STEP flag; flag-off prod path stays byte-identical (verified by unit test). No new attack surface — eval/docs/config-only phase. |
| T-15-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan; no `## Package Legitimacy Audit` required. |
</threat_model>

<verification>
- docs/promotion_decision.md exists with a root-cause section and a fix-disposition section
- The root-cause section is grounded in named existing run JSONs (zero new eval_reports/ dir)
- If a fix shipped: `make test` passes and the flag-off path is byte-identical
- ARCH-FUT-01 not executed; prod default not flipped
</verification>

<success_criteria>
The refinement_cheaper root cause is diagnosed from existing telemetry with zero live spend, written into the new milestone-closing doc, and the trivial-fix-or-defer decision is recorded and honored — clearing the way for the Plan 02 live runs to execute against final post-fix code.
</success_criteria>

<output>
Create `.planning/phases/15-gate-promotion-baseline-regen/15-01-SUMMARY.md` when done.
</output>
