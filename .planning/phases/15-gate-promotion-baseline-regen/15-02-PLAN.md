---
phase: 15-gate-promotion-baseline-regen
plan: 02
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - docs/promotion_decision.md
autonomous: false
requirements: [PROMO-01, PROMO-02]
user_setup:
  - service: openai
    why: "Live n=5 matrix runs for gpt-4o-mini anchor + gpt-5-mini (also covers shared embeddings quota)"
    env_vars:
      - name: OPENAI_API_KEY
        source: "OpenAI Dashboard -> API keys"
  - service: deepseek
    why: "Live n=5 matrix runs for deepseek-reasoner"
    env_vars:
      - name: DEEPSEEK_API_KEY
        source: "DeepSeek Dashboard -> API keys"
must_haves:
  truths:
    - "Run #1 (A2 retest) is invoked with an EXPLICIT clean environment that sets ONLY FORCED_COMMIT_STEP=6 and unsets every other experiment flag in the command itself (env -u VIABILITY_CONTRACT_ENABLED -u PARALLEL_TOOL_EXECUTION_ENABLED -u REPLAY_MULTI_MESSAGE_ENABLED -u REPLAY_CONTENT_BLOCKS_ENABLED FORCED_COMMIT_STEP=6 make eval-matrix-arm RUNS=5), measured at n=5 temp=1.0 sequential via configs/eval_matrix_arm.yaml over openai/gpt-4o-mini + openai/gpt-5-mini + deepseek/deepseek-reasoner across omakase_mission_open_ended + refinement_cheaper"
    - "Run #2 (flag-off prod-config baseline source) is invoked with an EXPLICIT clean environment that unsets ALL FIVE experiment flags in the command itself (env -u FORCED_COMMIT_STEP -u VIABILITY_CONTRACT_ENABLED -u PARALLEL_TOOL_EXECUTION_ENABLED -u REPLAY_MULTI_MESSAGE_ENABLED -u REPLAY_CONTENT_BLOCKS_ENABLED make eval-matrix-arm RUNS=5) — proving by construction that no inherited shell flag contaminates the honest baseline source — same 3 models x 2 scenarios x n=5"
    - "Each n=5 run is preceded by an n=1 smoke whose arm_flags self-description dict is INSPECTED (not merely produced) before the full spend, with the inspected dict pasted into docs/promotion_decision.md (Run #1 smoke shows forced_commit_step=6 with viability_contract/parallel_tool/replay flags false; Run #2 smoke shows forced_commit_step=0 with every experiment flag false)"
    - "The A2 retest verifies via forced_commit_step / commit_forced telemetry whether the forced-commit path ACTUALLY FIRED this time; if all_slots_viable never becomes true on refinement_cheaper so the forced path does not fire, that non-fire is recorded as a meaningful finding (it confirms the Plan 01 root cause), not treated as a failed/void retest"
    - "Anchor non-regression is checked on BOTH runs using committed_itinerary_rate median (the same metric check_eval_gates.py enforces): gpt-4o-mini median holds >= its prior committed baseline (no A3-style regression); any regression is flagged explicitly and stops promotion"
    - "Total FULL n=5 live matrix runs stays at 2 within the prior <=4-run cap (n=1 smokes are guards, not full runs — see run-cap accounting in the objective); a partial run (n_scored < n_requested) can never become a baseline source and is handled via the explicit partial-run decision path; no billing top-ups"
  artifacts:
    - path: "docs/promotion_decision.md"
      provides: "A2 Retest Delta section (Run #1 three-delta table + pasted falsifier output + forced-path-fired-or-non-fire finding + inspected smoke arm_flags dict) and a Run #2 flag-off provenance note recording the run dir + summary.json path + inspected smoke dict for Plan 03"
      contains: "A2 Retest"
  key_links:
    - from: "clean-env FORCED_COMMIT_STEP=6 make eval-matrix-arm RUNS=5 (Run #1) + clean-env all-flags-unset make eval-matrix-arm RUNS=5 (Run #2)"
      to: "docs/promotion_decision.md A2 Retest Delta + Run #2 provenance"
      via: "run dirs + eval-falsifier-arm output pasted verbatim + inspected arm_flags smoke verification"
      pattern: "eval_reports"
---

<objective>
Execute the TWO full live matrix runs against the FINAL post-fix code (Plan 01 landed first), each with an EXPLICIT clean-environment invocation so no inherited shell flag can leak: Run #1 = A2 retest with ONLY FORCED_COMMIT_STEP=6 set and all other experiment flags unset (experiment — feeds the verdict delta, NOT written as baselines), and Run #2 = flag-off prod-config with ALL FIVE experiment flags explicitly unset in the command (the honest baseline source for Plan 03). Smoke n=1 with arm_flags INSPECTION before each n=5 spend. Verify whether the forced-commit path fired on Run #1 (a non-fire is itself a valid finding), confirm anchor non-regression on BOTH runs via committed_itinerary_rate median, and record the A2 Retest Delta + Run #2 provenance in docs/promotion_decision.md. Stay within the <=4-run cap; no billing top-ups.

Purpose: This is the measurement core of Phase 15 (D-15-02). Two SEPARATE runs are mandatory: the experiment run (FORCED_COMMIT_STEP=6) violates the baselines=prod-config invariant and must never be written as a baseline; the flag-off run is the only honest baseline source. PROVENANCE IS THE TOP RISK: `make eval-matrix-arm RUNS=5` does NOT by itself prove flags are unset — an inherited FORCED_COMMIT_STEP (or any other arm flag) from the shell would silently contaminate the flag-off baseline source and invalidate D-15-07 provenance. So both runs use an explicit `env -u ...` clean-environment invocation that sets/unsets every experiment flag by construction, with the smoke arm_flags dict INSPECTED (not just produced) as the second line of defense. The A2 retest answers whether the now-working forced path (Plan 01 / Phase 13-08) lifts refinement_cheaper above 0.000 — but per D-15-01 the A2 outcome is a MEASURED INPUT, never a hard gate: Phase 15 completes regardless. Anchor non-regression on both runs is mandatory (A3's anchor regression is the cautionary precedent).
Output: docs/promotion_decision.md A2 Retest Delta section + Run #2 flag-off provenance (run dir + summary.json path + inspected smoke dict) for Plan 03 to consume.

RUN-CAP ACCOUNTING (resolves the ambiguity): the <=4-run cap (D-15-02) counts FULL n=5 live matrix runs / real spend. The n=1 smokes are CI-style guards that exist to catch a misconfigured flag BEFORE the n=5 spend — they do NOT count against the <=4-run cap. This plan therefore consumes 2 of the <=4 cap (Run #1 full + Run #2 full); the two n=1 smokes are not counted. (If a smoke itself reveals a flag problem and you must re-smoke, that re-smoke also does not count against the cap — fix the env and re-smoke until the arm_flags dict is correct, THEN spend the n=5.) No billing top-ups.

PARTIAL-RUN DECISION PATH (D-11-14): if either full run returns partial cells (any cell's n_scored < n_requested), that run can NEVER become a baseline source. Record the partial honestly in docs/promotion_decision.md, then choose ONE: (a) RETRY within the cap if budget remains (one retry of that run counts as a new full run against the <=4 cap); (b) MARK BLOCKED and stop (open a blocker; do not write baselines) if the cap is exhausted or the partial recurs; (c) RECORD-PARTIAL-AND-SKIP-REGEN — for Run #2 only, if a partial cannot be cleared, record the partial and explicitly defer baseline regen (Plan 03) with the partial as the documented reason. State which branch was taken in the doc.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/15-gate-promotion-baseline-regen/15-CONTEXT.md
@docs/baseline_regen.md
@configs/eval_matrix_arm.yaml
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Checkpoint: Authorize live spend against the &lt;=4-run cap</name>
  <files>(none — gate before any spend)</files>
  <action>Pause for the user to confirm provider keys are live, the embeddings probe + DB are reachable, Plan 01 is merged, and the &lt;=4-run cap has budget, BEFORE authorizing the two n=5 live runs in Tasks 1-2. This checkpoint precedes any API spend; do not run eval-matrix-arm until approved.</action>
  <what-built>Plan 01 landed the root-cause diagnostic and the D-15-08 fix disposition. Both full live runs in this plan spend real OpenAI + DeepSeek API money and run sequentially at n=5 against the post-fix code. Each is preceded by a no-counted n=1 smoke whose arm_flags dict is inspected first.</what-built>
  <how-to-verify>
    1. Confirm Plan 01 is merged: `git log --oneline -5` shows the 15-01 root-cause commit; docs/promotion_decision.md has the root-cause + fix-disposition sections.
    2. Confirm provider keys are live: `echo $OPENAI_API_KEY` and `echo $DEEPSEEK_API_KEY` are non-empty.
    3. Confirm the embeddings sanity probe passes (docs/baseline_regen.md Preconditions step 1) and the DB is reachable (`psql "$DATABASE_URL" -c "SELECT 1"`).
    4. Confirm the <=4-run cap budget: this plan spends 2 FULL n=5 runs (smokes are not counted); no prior Phase-15 full runs exist.
    Approve to spend against the <=4-run cap, or describe blockers.
  </how-to-verify>
  <resume-signal>Type "approved" to authorize the two n=5 live runs, or describe blockers (missing keys, exhausted quota, DB down).</resume-signal>
  <verify>
    <human-check>User confirms keys live, probe + DB reachable, Plan 01 merged, <=4-run cap has budget</human-check>
  </verify>
  <done>User has authorized the two n=5 live runs against the <=4-run cap.</done>
</task>

<task type="auto">
  <name>Task 1: Run #1 — A2 retest (clean-env, ONLY FORCED_COMMIT_STEP=6) smoke-first at n=5, record forced-fire-or-non-fire</name>
  <files>docs/promotion_decision.md (eval_reports/{run1_ts}/ is produced, not edited)</files>
  <read_first>
    - docs/baseline_regen.md (Preconditions: embeddings probe, DB reachable, keys live; Step 1 probe-providers)
    - configs/eval_matrix_arm.yaml (the committed 3-model x 2-scenario arm config — reuse verbatim)
    - Makefile (eval-matrix-arm target ~233, eval-falsifier-arm target ~248, probe-providers ~160)
    - app/agent/graph.py (lines ~326-335 — the five experiment flags read at graph-build time: FORCED_COMMIT_STEP, VIABILITY_CONTRACT_ENABLED, PARALLEL_TOOL_EXECUTION_ENABLED, REPLAY_MULTI_MESSAGE_ENABLED, REPLAY_CONTENT_BLOCKS_ENABLED — the authoritative flag-name list to unset)
    - scripts/eval_agent.py (arm_flags self-description dict ~928-938: keys viability_contract / forced_commit_step / parallel_tool / viability_threshold_override / replay_multi_message / replay_content_blocks; forced_commit_step/commit_forced telemetry ~926-927)
    - scripts/eval_falsifier.py (exit-code 0/1/2 contract; --run-dir + --matrix-config; _commit_split_from_run_dir forced/model-initiated split)
    - scripts/check_eval_gates.py (committed_itinerary_rate -> scorers.committed_itinerary_rate.median is the gate metric; the same metric used for anchor non-regression)
    - docs/decisiveness_arm_verdicts.md (Phase-13 A2 = 0.500 model-initiated; CR-01 forced-never-fired baseline for the delta)
    - docs/promotion_decision.md (Plan 01 root-cause + fix disposition)
  </read_first>
  <action>Run the D-10-14 mandatory pre-matrix probe (`make probe-providers`) and the embeddings sanity probe per docs/baseline_regen.md. Then run the Run #1 SMOKE in a clean environment that sets ONLY the A2 flag and explicitly unsets the other four experiment flags: `env -u VIABILITY_CONTRACT_ENABLED -u PARALLEL_TOOL_EXECUTION_ENABLED -u REPLAY_MULTI_MESSAGE_ENABLED -u REPLAY_CONTENT_BLOCKS_ENABLED FORCED_COMMIT_STEP=6 make eval-matrix-arm RUNS=1`. INSPECT (do not merely produce) the arm_flags self-description dict in the smoke run JSON (queries[0].deterministic.arm_flags): confirm forced_commit_step=6 and viability_contract=false, parallel_tool=false, replay_multi_message=false, replay_content_blocks=false — BEFORE the full spend, and paste the inspected dict into docs/promotion_decision.md. If the dict shows any other flag set, STOP, fix the environment, and re-smoke (re-smokes do not count against the cap). Then run the full Run #1 with the SAME clean-env invocation at n=5: `env -u VIABILITY_CONTRACT_ENABLED -u PARALLEL_TOOL_EXECUTION_ENABLED -u REPLAY_MULTI_MESSAGE_ENABLED -u REPLAY_CONTENT_BLOCKS_ENABLED FORCED_COMMIT_STEP=6 make eval-matrix-arm RUNS=5` (3 models x 2 scenarios x n=5, temp=1.0, sequential). Grade it: `make eval-falsifier-arm RUN_DIR=eval_reports/{run1_ts} MATRIX_CONFIG=configs/eval_matrix_arm.yaml`. CRITICAL VERIFICATION: inspect the gpt-5-mini run JSONs' queries[i].deterministic.forced_commit_step and .commit_forced fields — report whether the forced path fired (forced_commit_step is a non-null step index) this time, unlike Phase 13 where it stayed None. A NON-FIRE is a valid, meaningful result: if all_slots_viable never becomes true on refinement_cheaper (rule8_met_per_step stays all-False), the forced path correctly does not fire and that CONFIRMS the Plan 01 root cause — record it as a finding, NOT as a failed or void retest. Apply the partial-run decision path from the objective if any cell is partial. Write the "## A2 Retest Delta" section in docs/promotion_decision.md: the per-model three-delta table (pooled committed_itinerary_rate, delta vs Phase-13 A2 0.500, delta vs flag-off floor 0.000) for gpt-5-mini / gpt-4o-mini anchor / deepseek-reasoner, the model-initiated-vs-forced split, the pasted falsifier output verbatim, the inspected smoke arm_flags dict, and an explicit statement of whether the forced path fired (or the non-fire finding) and whether the A2 retest cleared 0.6. Confirm anchor non-regression using committed_itinerary_rate median: gpt-4o-mini median holds >= its prior committed baseline; if it regressed, flag it explicitly and STOP (no promotion off a regressed anchor). Do NOT run write_baselines.py on this run — the FORCED_COMMIT_STEP=6 flag violates the baselines=prod-config invariant (this is the experiment run, not the baseline source).</action>
  <verify>
    <automated>grep -qi "A2 Retest" docs/promotion_decision.md && grep -qi "forced" docs/promotion_decision.md</automated>
  </verify>
  <acceptance_criteria>
    - The Run #1 SMOKE and full run were both invoked with the explicit clean-env form (`env -u VIABILITY_CONTRACT_ENABLED -u PARALLEL_TOOL_EXECUTION_ENABLED -u REPLAY_MULTI_MESSAGE_ENABLED -u REPLAY_CONTENT_BLOCKS_ENABLED FORCED_COMMIT_STEP=6 make eval-matrix-arm RUNS=...`)
    - The Run #1 smoke arm_flags dict was INSPECTED and pasted into docs/promotion_decision.md, showing forced_commit_step=6 and viability_contract=false, parallel_tool=false, replay_multi_message=false, replay_content_blocks=false BEFORE the n=5 spend
    - The full Run #1 ran 3 models x 2 scenarios x n=5 via configs/eval_matrix_arm.yaml at temp=1.0
    - docs/promotion_decision.md has a "## A2 Retest Delta" section with the per-model three-delta table and the pasted `make eval-falsifier-arm` output verbatim
    - The section explicitly states whether the forced-commit path FIRED via queries[i].deterministic.forced_commit_step / .commit_forced telemetry; if it did NOT fire because all_slots_viable never held on refinement, that non-fire is recorded as a meaningful finding confirming the Plan 01 root cause
    - Anchor non-regression CONFIRMED for gpt-4o-mini using committed_itinerary_rate median (>= prior baseline); a regression is flagged explicitly and halts promotion
    - If any cell was partial (n_scored < n_requested), the partial-run decision path branch taken (retry / mark-blocked / record-partial) is stated in the doc
    - write_baselines.py was NOT run on the Run #1 (experiment) dir
  </acceptance_criteria>
  <done>Run #1 (A2 retest) is measured at n=5 in a clean environment, the forced-path-fired-or-non-fire question is answered from telemetry as a meaningful finding either way, the three-delta table + falsifier output + inspected smoke dict are recorded, and anchor non-regression is confirmed — all without writing this experiment run as a baseline.</done>
</task>

<task type="auto">
  <name>Task 2: Run #2 — flag-off prod-config (baseline source) clean-env all-flags-unset, smoke-first at n=5, anchor non-regression</name>
  <files>docs/promotion_decision.md (eval_reports/{run2_ts}/ is produced, not edited)</files>
  <read_first>
    - docs/baseline_regen.md (Preconditions + Steps 1-4: probe, snapshot, run matrix; the regen-last ordering)
    - configs/eval_matrix_arm.yaml (same config; flags differ, shape identical)
    - app/agent/graph.py (lines ~326-335 — the five experiment flags to unset for the prod-default config)
    - scripts/eval_agent.py (arm_flags self-description — Run #2 must show all experiment flags UNSET)
    - scripts/eval_falsifier.py (anchor non-regression check; --baselines-dir floor)
    - scripts/check_eval_gates.py (committed_itinerary_rate -> scorers.committed_itinerary_rate.median is the metric for anchor non-regression)
    - configs/eval_baselines/omakase_mission_open_ended.json + refinement_cheaper.json (current committed cells — the anchor floor and the cells Plan 03 will overwrite)
    - docs/promotion_decision.md (Plan 01 sections + Task 1 A2 Retest Delta)
  </read_first>
  <action>Run the Run #2 SMOKE in a clean environment that explicitly unsets ALL FIVE experiment flags so no inherited shell value can leak into the honest baseline source: `env -u FORCED_COMMIT_STEP -u VIABILITY_CONTRACT_ENABLED -u PARALLEL_TOOL_EXECUTION_ENABLED -u REPLAY_MULTI_MESSAGE_ENABLED -u REPLAY_CONTENT_BLOCKS_ENABLED make eval-matrix-arm RUNS=1`. INSPECT (do not merely produce) the arm_flags self-description dict (queries[0].deterministic.arm_flags): confirm forced_commit_step=0 and viability_contract=false, parallel_tool=false, replay_multi_message=false, replay_content_blocks=false — this is the prod-default CI config and the honest baseline source — and paste the inspected dict into docs/promotion_decision.md. If any flag shows set, STOP, fix the environment, and re-smoke (re-smokes do not count against the cap). Then run the full Run #2 with the SAME clean-env all-flags-unset invocation at n=5: `env -u FORCED_COMMIT_STEP -u VIABILITY_CONTRACT_ENABLED -u PARALLEL_TOOL_EXECUTION_ENABLED -u REPLAY_MULTI_MESSAGE_ENABLED -u REPLAY_CONTENT_BLOCKS_ENABLED make eval-matrix-arm RUNS=5` (3 models x 2 scenarios x n=5, temp=1.0, sequential, all flags unset by construction). Grade it: `make eval-falsifier-arm RUN_DIR=eval_reports/{run2_ts} MATRIX_CONFIG=configs/eval_matrix_arm.yaml`. Confirm anchor non-regression using committed_itinerary_rate median: gpt-4o-mini median holds >= its prior committed baseline; if it regressed below the 0.8 gate, STOP per docs/baseline_regen.md "gpt-4o-mini committed_itinerary_rate below 0.8" branch (do not write baselines, open a blocker). Apply the partial-run decision path from the objective if any cell is partial (a partial Run #2 can never become a baseline source). Record the Run #2 provenance in docs/promotion_decision.md: the run dir path, the summary.json path, the INSPECTED smoke arm_flags dict, the per-model flag-off committed_itinerary_rate medians, and an explicit note that THIS run (flags unset by construction) — not Run #1 — is the source write_baselines.py reads in Plan 03 (D-15-05 regen-last). Do NOT run write_baselines.py yet (D-15-05: baselines are written LAST against final merged code in Plan 03). Note the {run2_ts} timestamp clearly so Plan 03 can reference summary.json.</action>
  <verify>
    <automated>grep -qi "Run #2\|flag-off" docs/promotion_decision.md && grep -qi "summary.json" docs/promotion_decision.md</automated>
  </verify>
  <acceptance_criteria>
    - The Run #2 SMOKE and full run were both invoked with the explicit clean-env all-flags-unset form (`env -u FORCED_COMMIT_STEP -u VIABILITY_CONTRACT_ENABLED -u PARALLEL_TOOL_EXECUTION_ENABLED -u REPLAY_MULTI_MESSAGE_ENABLED -u REPLAY_CONTENT_BLOCKS_ENABLED make eval-matrix-arm RUNS=...`)
    - The Run #2 smoke arm_flags dict was INSPECTED and pasted into docs/promotion_decision.md, showing forced_commit_step=0 and ALL experiment flags false (the prod-default config) BEFORE the n=5 spend
    - The full Run #2 ran 3 models x 2 scenarios x n=5 via configs/eval_matrix_arm.yaml at temp=1.0 with all experiment flags unset by construction
    - docs/promotion_decision.md records the Run #2 run dir path AND the summary.json path, with an explicit note that this flag-off run is the write_baselines.py source for Plan 03 (NOT Run #1)
    - Anchor non-regression CONFIRMED using committed_itinerary_rate median: gpt-4o-mini median >= its prior committed baseline (>= 0.8 gate); a sub-0.8 result halts per the docs/baseline_regen.md stop branch
    - If any cell was partial, the partial-run decision branch taken is stated and Run #2 is not used as a baseline source
    - write_baselines.py was NOT run in this plan (regen is sequenced last in Plan 03, D-15-05)
    - Total Phase-15 FULL n=5 live runs after this plan = 2, within the <=4-run cap (n=1 smokes not counted); no billing top-ups occurred
  </acceptance_criteria>
  <done>Run #2 (flag-off prod-config) is measured at n=5 in a clean environment with all experiment flags verified unset by construction AND by inspection, the run dir + summary.json provenance is recorded for Plan 03, and anchor non-regression is confirmed — establishing the honest baseline source without writing baselines yet.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local -> provider APIs | Live eval runs call OpenAI + DeepSeek with the operator's own keys. No new request surface is added to the product; this is the existing eval harness invoked with env flags. No external untrusted input crosses into the agent beyond the committed scenario queries. |
| shell env -> baseline provenance | An inherited experiment env var (FORCED_COMMIT_STEP or any arm flag) could silently contaminate the flag-off baseline source. Mitigated by explicit `env -u ...` clean-environment invocation on BOTH runs + INSPECTED arm_flags smoke verification before the n=5 spend. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-02 | Information disclosure | provider API keys in env | accept | Keys are operator-supplied at run time via env vars (OPENAI_API_KEY, DEEPSEEK_API_KEY); the harness already redacts probe fixtures (Phase-10 fail-closed redaction). No new key handling introduced. |
| T-15-03 | Denial of service | <=4-run cap / billing | mitigate | Hard <=4-run cap on FULL n=5 runs (D-15-02); n=1 smokes are guards, not counted; no billing top-ups; smoke before each n=5 spend; blocking human checkpoint before any live run. |
| T-15-07 | Tampering | flag-off baseline provenance (env leak) | mitigate | Both runs use explicit `env -u FORCED_COMMIT_STEP/VIABILITY_CONTRACT_ENABLED/PARALLEL_TOOL_EXECUTION_ENABLED/REPLAY_MULTI_MESSAGE_ENABLED/REPLAY_CONTENT_BLOCKS_ENABLED` clean-environment invocation (Run #1 sets only FORCED_COMMIT_STEP=6; Run #2 unsets all five), proving flag state by construction; the smoke arm_flags dict is INSPECTED (not merely produced) and pasted into the doc before any n=5 spend as the second line of defense. |
| T-15-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan; no `## Package Legitimacy Audit` required. |
</threat_model>

<verification>
- Two FULL n=5 runs completed, each via an explicit clean-env invocation (Run #1 sets only FORCED_COMMIT_STEP=6; Run #2 unsets all five flags), each smoke-INSPECTED
- A2 Retest Delta section records the three-delta table, forced-path-fired-or-non-fire finding, inspected smoke dict, and falsifier output
- Run #2 provenance (run dir + summary.json + inspected smoke dict) recorded as the Plan 03 baseline source
- Anchor non-regression confirmed on BOTH runs via committed_itinerary_rate median
- <=4-run cap respected (smokes not counted); partial-run decision path applied if needed; write_baselines.py not yet run
</verification>

<success_criteria>
Both live runs are measured against final post-fix code in explicit clean environments (no env-var leak by construction, confirmed by inspected smoke dicts), the A2 retest's forced-path-fired-or-non-fire question is answered from telemetry as a meaningful finding, the flag-off baseline source is established and recorded, and the anchor held on both runs — within the <=4-run cap and with no baselines written yet.
</success_criteria>

<output>
Create `.planning/phases/15-gate-promotion-baseline-regen/15-02-SUMMARY.md` when done.
</output>
</content>
