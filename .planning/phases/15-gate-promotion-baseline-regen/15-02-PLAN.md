---
phase: 15-gate-promotion-baseline-regen
plan: 02
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - docs/promotion_decision.md
autonomous: false
requirements: [PROMO-01, PROMO-02]
user_setup:
  - service: openai
    why: "Live n=5 matrix runs for gpt-4o-mini anchor + gpt-5-mini (also covers shared embeddings quota)"
    env_vars:
      - name: OPENAI_API_KEY
        source: "OpenAI Dashboard -> API keys"
  - service: deepseek
    why: "Live n=5 matrix runs for deepseek-reasoner"
    env_vars:
      - name: DEEPSEEK_API_KEY
        source: "DeepSeek Dashboard -> API keys"
must_haves:
  truths:
    - "Run #1 (A2 retest) sets FORCED_COMMIT_STEP=6 and is measured at n=5 temp=1.0 sequential via configs/eval_matrix_arm.yaml over openai/gpt-4o-mini + openai/gpt-5-mini + deepseek/deepseek-reasoner across omakase_mission_open_ended + refinement_cheaper"
    - "Run #2 (flag-off prod-config) has ALL experiment flags UNSET and is the honest baseline source for write_baselines.py — same 3 models x 2 scenarios x n=5"
    - "Each n=5 run is preceded by an n=1 smoke whose arm_flags self-description dict is verified before the full spend (Run #1 smoke shows forced_commit_step=6; Run #2 smoke shows forced_commit_step=0/all flags false)"
    - "The A2 retest verifies via forced_commit_step / commit_forced telemetry that the forced-commit path ACTUALLY FIRED this time (CR-01 left it never-firing in Phase 13)"
    - "Anchor non-regression is checked on BOTH runs: gpt-4o-mini committed-rate holds >= its prior baseline (no A3-style regression); any regression is flagged explicitly and stops promotion"
    - "Total live spend stays within the prior <=4-run cap with no billing top-ups; partials are recorded honestly and never written as baselines"
  artifacts:
    - path: "docs/promotion_decision.md"
      provides: "A2 Retest Delta section (Run #1 three-delta table + pasted falsifier output + forced-path-fired confirmation) and a Run #2 flag-off provenance note recording the run dir + summary.json path for Plan 03"
      contains: "A2 Retest"
  key_links:
    - from: "FORCED_COMMIT_STEP=6 make eval-matrix-arm RUNS=5 (Run #1) + flags-unset make eval-matrix-arm RUNS=5 (Run #2)"
      to: "docs/promotion_decision.md A2 Retest Delta + Run #2 provenance"
      via: "run dirs + eval-falsifier-arm output pasted verbatim + arm_flags smoke verification"
      pattern: "eval_reports"
---

<objective>
Execute the TWO live matrix runs against the FINAL post-fix code (Plan 01 landed first): Run #1 = A2 retest with FORCED_COMMIT_STEP=6 (experiment — feeds the verdict delta, NOT written as baselines), and Run #2 = flag-off prod-config with all experiment flags UNSET (the honest baseline source for Plan 03). Smoke n=1 with arm_flags verification before each n=5 spend. Verify the forced-commit path actually fired on Run #1, confirm anchor non-regression on BOTH runs, and record the A2 Retest Delta + Run #2 provenance in docs/promotion_decision.md. Stay within the <=4-run cap; no billing top-ups.

Purpose: This is the measurement core of Phase 15 (D-15-02). Two SEPARATE runs are mandatory: the experiment run (FORCED_COMMIT_STEP=6) violates the baselines=prod-config invariant and must never be written as a baseline; the flag-off run is the only honest baseline source. The A2 retest answers whether the now-working forced path (Plan 01 / Phase 13-08) lifts refinement_cheaper above 0.000 — but per D-15-01 the A2 outcome is a MEASURED INPUT, never a hard gate: Phase 15 completes regardless. Anchor non-regression on both runs is mandatory (A3's anchor regression is the cautionary precedent).
Output: docs/promotion_decision.md A2 Retest Delta section + Run #2 flag-off provenance (run dir + summary.json path) for Plan 03 to consume.

RUN BUDGET: Run #1 + Run #2 = 2 of the <=4 total live matrix runs (D-15-02). No billing top-ups: a partial is recorded honestly and never written as a baseline (D-11-14).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/15-gate-promotion-baseline-regen/15-CONTEXT.md
@docs/baseline_regen.md
@configs/eval_matrix_arm.yaml
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Checkpoint: Authorize live spend against the &lt;=4-run cap</name>
  <files>(none — gate before any spend)</files>
  <action>Pause for the user to confirm provider keys are live, the embeddings probe + DB are reachable, Plan 01 is merged, and the &lt;=4-run cap has budget, BEFORE authorizing the two n=5 live runs in Tasks 1-2. This checkpoint precedes any API spend; do not run eval-matrix-arm until approved.</action>
  <what-built>Plan 01 landed the root-cause diagnostic and the D-15-08 fix disposition. Both live runs in this plan spend real OpenAI + DeepSeek API money and run sequentially at n=5 against the post-fix code.</what-built>
  <how-to-verify>
    1. Confirm Plan 01 is merged: `git log --oneline -5` shows the 15-01 root-cause commit; docs/promotion_decision.md has the root-cause + fix-disposition sections.
    2. Confirm provider keys are live: `echo $OPENAI_API_KEY` and `echo $DEEPSEEK_API_KEY` are non-empty.
    3. Confirm the embeddings sanity probe passes (docs/baseline_regen.md Preconditions step 1) and the DB is reachable (`psql "$DATABASE_URL" -c "SELECT 1"`).
    4. Confirm the <=4-run cap budget: this plan spends 2 runs; no prior Phase-15 live runs exist.
    Approve to spend against the <=4-run cap, or describe blockers.
  </how-to-verify>
  <resume-signal>Type "approved" to authorize the two n=5 live runs, or describe blockers (missing keys, exhausted quota, DB down).</resume-signal>
  <verify>
    <human-check>User confirms keys live, probe + DB reachable, Plan 01 merged, <=4-run cap has budget</human-check>
  </verify>
  <done>User has authorized the two n=5 live runs against the <=4-run cap.</done>
</task>

<task type="auto">
  <name>Task 1: Run #1 — A2 retest (FORCED_COMMIT_STEP=6) smoke-first at n=5, confirm forced path fired</name>
  <files>docs/promotion_decision.md (eval_reports/{run1_ts}/ is produced, not edited)</files>
  <read_first>
    - docs/baseline_regen.md (Preconditions: embeddings probe, DB reachable, keys live; Step 1 probe-providers)
    - configs/eval_matrix_arm.yaml (the committed 3-model x 2-scenario arm config — reuse verbatim)
    - Makefile (eval-matrix-arm target ~234, eval-falsifier-arm target ~249, probe-providers ~161)
    - scripts/eval_agent.py (arm_flags self-description ~928-931; forced_commit_step/commit_forced telemetry ~926-928)
    - scripts/eval_falsifier.py (exit-code 0/1/2 contract; --run-dir + --matrix-config; _commit_split_from_run_dir forced/model-initiated split)
    - docs/decisiveness_arm_verdicts.md (Phase-13 A2 = 0.500 model-initiated; CR-01 forced-never-fired baseline for the delta)
    - docs/promotion_decision.md (Plan 01 root-cause + fix disposition)
  </read_first>
  <action>Run the D-10-14 mandatory pre-matrix probe (`make probe-providers`) and the embeddings sanity probe per docs/baseline_regen.md. Then run the Run #1 SMOKE: `FORCED_COMMIT_STEP=6 make eval-matrix-arm RUNS=1` and verify the arm_flags self-description dict in the smoke run JSON shows forced_commit_step=6 with viability_contract / parallel_tool / replay flags all false — BEFORE the full spend. Then run the full Run #1: `FORCED_COMMIT_STEP=6 make eval-matrix-arm RUNS=5` (3 models x 2 scenarios x n=5, temp=1.0, sequential). Grade it: `make eval-falsifier-arm RUN_DIR=eval_reports/{run1_ts} MATRIX_CONFIG=configs/eval_matrix_arm.yaml`. CRITICAL VERIFICATION: inspect the gpt-5-mini run JSONs' deterministic.forced_commit_step and deterministic.commit_forced fields — the A2 retest is only meaningful if the forced path DEMONSTRABLY fired this time (forced_commit_step is a non-null step index on at least the runs where rule8 is met), unlike Phase 13 where it stayed None. Write the "## A2 Retest Delta" section in docs/promotion_decision.md: the per-model three-delta table (pooled commit rate, delta vs Phase-13 A2 0.500, delta vs flag-off floor 0.000) for gpt-5-mini / gpt-4o-mini anchor / deepseek-reasoner, the model-initiated-vs-forced split, the pasted falsifier output verbatim, and an explicit statement of whether the forced path fired and whether the A2 retest cleared 0.6. Confirm anchor non-regression: gpt-4o-mini median holds >= its prior baseline; if it regressed, flag it explicitly and STOP (no promotion off a regressed anchor). Do NOT run write_baselines.py on this run — the FORCED_COMMIT_STEP=6 flag violates the baselines=prod-config invariant (this is the experiment run, not the baseline source).</action>
  <verify>
    <automated>grep -qi "A2 Retest" docs/promotion_decision.md && grep -qi "forced" docs/promotion_decision.md</automated>
  </verify>
  <acceptance_criteria>
    - The Run #1 smoke arm_flags dict (pasted into docs/promotion_decision.md) shows forced_commit_step=6 and viability_contract=false, parallel_tool=false, replay flags false
    - The full Run #1 ran 3 models x 2 scenarios x n=5 via configs/eval_matrix_arm.yaml at temp=1.0
    - docs/promotion_decision.md has a "## A2 Retest Delta" section with the per-model three-delta table and the pasted `make eval-falsifier-arm` output verbatim
    - The section explicitly confirms whether the forced-commit path FIRED via the deterministic.forced_commit_step / deterministic.commit_forced telemetry (the whole point of the retest)
    - Anchor non-regression CONFIRMED for gpt-4o-mini (median >= prior baseline); a regression is flagged explicitly and halts promotion
    - write_baselines.py was NOT run on the Run #1 (experiment) dir
  </acceptance_criteria>
  <done>Run #1 (A2 retest) is measured at n=5, the forced-path-fired question is answered from telemetry, the three-delta table + falsifier output are recorded, and anchor non-regression is confirmed — all without writing this experiment run as a baseline.</done>
</task>

<task type="auto">
  <name>Task 2: Run #2 — flag-off prod-config (baseline source) smoke-first at n=5, anchor non-regression</name>
  <files>docs/promotion_decision.md (eval_reports/{run2_ts}/ is produced, not edited)</files>
  <read_first>
    - docs/baseline_regen.md (Preconditions + Steps 1-4: probe, snapshot, run matrix; the regen-last ordering)
    - configs/eval_matrix_arm.yaml (same config; flags differ, shape identical)
    - scripts/eval_agent.py (arm_flags self-description — Run #2 must show all experiment flags UNSET)
    - scripts/eval_falsifier.py (anchor non-regression check; --baselines-dir floor)
    - configs/eval_baselines/omakase_mission_open_ended.json + refinement_cheaper.json (current committed cells — the anchor floor and the cells Plan 03 will overwrite)
    - docs/promotion_decision.md (Plan 01 sections + Task 1 A2 Retest Delta)
  </read_first>
  <action>Run the Run #2 SMOKE: `make eval-matrix-arm RUNS=1` with ALL experiment flags UNSET (no FORCED_COMMIT_STEP, no VIABILITY_CONTRACT_ENABLED, no PARALLEL_TOOL_EXECUTION_ENABLED, no REPLAY_* flags) and verify the arm_flags self-description dict shows forced_commit_step=0 and every experiment flag false — this is the prod-default CI config and the honest baseline source. Then run the full Run #2: `make eval-matrix-arm RUNS=5` (3 models x 2 scenarios x n=5, temp=1.0, sequential, all flags unset). Grade it: `make eval-falsifier-arm RUN_DIR=eval_reports/{run2_ts} MATRIX_CONFIG=configs/eval_matrix_arm.yaml`. Confirm anchor non-regression: gpt-4o-mini median holds >= its prior committed baseline; if it regressed below the 0.8 gate, STOP per docs/baseline_regen.md "gpt-4o-mini committed_itinerary_rate below 0.8" branch (do not write baselines, open a blocker). Record the Run #2 provenance in docs/promotion_decision.md: the run dir path, the summary.json path, the smoke arm_flags dict, the per-model flag-off committed-rate medians, and an explicit note that THIS run (flags unset) — not Run #1 — is the source write_baselines.py reads in Plan 03 (D-15-05 regen-last). Do NOT run write_baselines.py yet (D-15-05: baselines are written LAST against final merged code in Plan 03). Note the {run2_ts} timestamp clearly so Plan 03 can reference summary.json.</action>
  <verify>
    <automated>grep -qi "Run #2\|flag-off" docs/promotion_decision.md && grep -qi "summary.json" docs/promotion_decision.md</automated>
  </verify>
  <acceptance_criteria>
    - The Run #2 smoke arm_flags dict (pasted into docs/promotion_decision.md) shows forced_commit_step=0 and ALL experiment flags false (the prod-default config)
    - The full Run #2 ran 3 models x 2 scenarios x n=5 via configs/eval_matrix_arm.yaml at temp=1.0 with all experiment flags unset
    - docs/promotion_decision.md records the Run #2 run dir path AND the summary.json path, with an explicit note that this flag-off run is the write_baselines.py source for Plan 03 (NOT Run #1)
    - Anchor non-regression CONFIRMED: gpt-4o-mini median >= its prior committed baseline (>= 0.8 gate); a sub-0.8 result halts per the docs/baseline_regen.md stop branch
    - write_baselines.py was NOT run in this plan (regen is sequenced last in Plan 03, D-15-05)
    - Total Phase-15 live runs after this plan = 2, within the <=4-run cap; no billing top-ups occurred
  </acceptance_criteria>
  <done>Run #2 (flag-off prod-config) is measured at n=5 with all experiment flags verified unset, the run dir + summary.json provenance is recorded for Plan 03, and anchor non-regression is confirmed — establishing the honest baseline source without writing baselines yet.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local -> provider APIs | Live eval runs call OpenAI + DeepSeek with the operator's own keys. No new request surface is added to the product; this is the existing eval harness invoked with env flags. No external untrusted input crosses into the agent beyond the committed scenario queries. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-02 | Information disclosure | provider API keys in env | accept | Keys are operator-supplied at run time via env vars (OPENAI_API_KEY, DEEPSEEK_API_KEY); the harness already redacts probe fixtures (Phase-10 fail-closed redaction). No new key handling introduced. |
| T-15-03 | Denial of service | <=4-run cap / billing | mitigate | Hard <=4-run cap (D-15-02); no billing top-ups; smoke n=1 before each n=5 spend; blocking human checkpoint before any live run. |
| T-15-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan; no `## Package Legitimacy Audit` required. |
</threat_model>

<verification>
- Two n=5 runs completed (Run #1 FORCED_COMMIT_STEP=6, Run #2 all flags unset), each smoke-verified
- A2 Retest Delta section records the three-delta table, forced-path-fired confirmation, and falsifier output
- Run #2 provenance (run dir + summary.json) recorded as the Plan 03 baseline source
- Anchor non-regression confirmed on BOTH runs
- <=4-run cap respected; write_baselines.py not yet run
</verification>

<success_criteria>
Both live runs are measured against final post-fix code, the A2 retest's forced-path-fired question is answered from telemetry, the flag-off baseline source is established and recorded, and the anchor held on both runs — within the <=4-run cap and with no baselines written yet.
</success_criteria>

<output>
Create `.planning/phases/15-gate-promotion-baseline-regen/15-02-SUMMARY.md` when done.
</output>
