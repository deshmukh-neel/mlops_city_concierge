---
phase: 15-gate-promotion-baseline-regen
plan: 03
type: execute
wave: 3
depends_on: ["15-02"]
files_modified:
  - configs/eval_gates.yaml
  - configs/eval_baselines/omakase_mission_open_ended.json
  - configs/eval_baselines/refinement_cheaper.json
  - docs/promotion_decision.md
autonomous: true
requirements: [PROMO-01, PROMO-02, PROMO-03]
must_haves:
  truths:
    - "configs/eval_gates.yaml: gpt-4o-mini keeps its enforced 0.8 hard gate, re-ratified against the fresh Run #2 (flag-off) median"
    - "configs/eval_gates.yaml: gpt-5-mini is promoted to enforced ONLY if the A2 retest cleared 0.6 AND the flag-off Run #2 rate also supports the floor (D-15-07 provenance); otherwise it stays status: logged/aspirational with a Phase-15 rationale citing the measurement"
    - "deepseek-reasoner / anthropic / gemini stay logged-not-gated with Phase-15 rationale notes; no 0.0-floor enforced gate is set on any known-failing config"
    - "write_baselines.py runs LAST against the FINAL merged code reading the Run #2 (flag-off) summary.json; runnable cells only (gpt-4o-mini + gpt-5-mini + deepseek-reasoner x 2 live scenarios); anthropic/gemini deferred-logged; late_night quarantined"
    - "check_baselines_fresh.py exits 0 on the final commit and the regenerated baseline JSONs carry fresh n=5 provenance stamps"
    - "A PROMO-03 latency report decomposed from step_telemetry (llm_call_seconds + tool_exec_seconds per step) states the anchor per-turn total vs the ~30s/turn budget HONESTLY, including the inconvenient ~46s-on-omakase observation, with a written prod-driver recommendation to ratify gpt-4o-mini"
  artifacts:
    - path: "configs/eval_gates.yaml"
      provides: "Phase-15 gate-promotion decisions with provenance-honest rationale fields"
      contains: "D-15"
    - path: "configs/eval_baselines/refinement_cheaper.json"
      provides: "fresh n=5 baseline cells from the Run #2 flag-off summary.json"
      contains: "committed_itinerary_rate"
    - path: "docs/promotion_decision.md"
      provides: "gate-promotion decision record + baseline regen provenance + PROMO-03 latency report"
      contains: "Latency"
  key_links:
    - from: "Run #2 flag-off summary.json (from Plan 02)"
      to: "configs/eval_baselines/*.json"
      via: "scripts/write_baselines.py (runnable cells only; refuses partial/quarantined)"
      pattern: "write_baselines"
    - from: "Run #2 run JSONs step_telemetry (llm_call_seconds + tool_exec_seconds)"
      to: "docs/promotion_decision.md PROMO-03 latency report"
      via: "per-step aggregation vs ~30s/turn budget"
      pattern: "llm_call_seconds|tool_exec_seconds"
---

<objective>
Translate the Plan 02 measurements into the three durable PROMO artifacts, in the D-15-09 order: (PROMO-02) write the gate-promotion decisions into configs/eval_gates.yaml with provenance-honest rationale (D-15-06/07); (PROMO-01) run write_baselines.py LAST against the FINAL merged code reading the Run #2 flag-off summary.json, runnable cells only (D-15-04/05); (PROMO-03) aggregate the latency report from step_telemetry vs the ~30s/turn budget with a written prod-driver recommendation. All three read already-produced artifacts — no new live spend.

Purpose: This plan is where the milestone's enforced-vs-logged gate decisions and honest baselines become committed record. D-15-06 enforces only what is both stable AND measured; D-15-07 demands the gate value come from the config CI actually runs (flag-off Run #2), never from the FORCED_COMMIT_STEP=6 experiment. D-15-05 sequences baselines LAST so the committed JSONs reflect merged behavior and the staleness gate passes. PROMO-03 must decompose latency honestly — including the ~46s-on-omakase anchor observation that is over budget — rather than reporting a flattering single number.
Output: updated configs/eval_gates.yaml, regenerated runnable baseline cells, and the PROMO-03 latency report in docs/promotion_decision.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/15-gate-promotion-baseline-regen/15-CONTEXT.md
@configs/eval_gates.yaml
@docs/baseline_regen.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Promote gates in configs/eval_gates.yaml (D-15-06/07 provenance honesty)</name>
  <files>configs/eval_gates.yaml, docs/promotion_decision.md</files>
  <read_first>
    - configs/eval_gates.yaml (current schema: family / status / rationale / hard / advisory; gpt-4o-mini active@0.8, gpt-5-mini aspirational@0.6, deepseek/anthropic/gemini logged, late_night quarantined)
    - scripts/check_eval_gates.py (what "enforced" means operationally; make eval-gates-check)
    - docs/eval_gates.md (gate semantics; links to the yaml, never duplicates numbers)
    - docs/promotion_decision.md (Plan 02 A2 Retest Delta + Run #2 flag-off medians)
    - .planning/phases/15-gate-promotion-baseline-regen/15-CONTEXT.md (D-15-06 enforce-only-stable-and-measured; D-15-07 gate value must come from flag-off prod-config, NOT the experiment)
  </read_first>
  <action>Edit configs/eval_gates.yaml using the Run #2 flag-off medians (from Plan 02) as the gate-value source. For openai/gpt-4o-mini: keep status: active and the hard committed_itinerary_rate >= 0.8 gate; update the rationale field to re-ratify it against the fresh Run #2 (flag-off) median, citing D-15-06 and the Run #2 timestamp. For openai/gpt-5-mini: apply D-15-07 — promote to status: active with an enforced hard gate ONLY IF (a) the A2 retest cleared >= 0.6 AND (b) the flag-off Run #2 rate also supports the floor; in that case the rationale MUST record that the forced path is the mechanism and that prod would need FORCED_COMMIT_STEP enabled to realize it (gate value sourced from flag-off, unlock rationale from the experiment). If either condition fails (the likely outcome — A2 plateaued at 0.500 before), keep status: aspirational/logged with hard unchanged or null and write a Phase-15 rationale citing the Run #2 flag-off median and the A2 retest number, noting why enforcement is not earned. For deepseek/deepseek-reasoner, anthropic/claude-sonnet-4-6, gemini/gemini-3.1-pro-preview: keep status: logged, hard: null, and append a Phase-15 rationale note (deepseek: data doesn't earn enforcement; anthropic/gemini: deferred per D-12-09, no top-up). Do NOT set a 0.0-floor enforced gate on any known-failing config (meaningless and misrepresents failure as passing). Do NOT flip the prod default to FORCED_COMMIT_STEP=6 — record in docs/promotion_decision.md that a prod-default flip is a SEPARATE, likely-deferred decision (D-15-07) and is NOT implemented here. Run `make eval-gates-check` (or eval-gates-check-baselines) to confirm exit 0 after the edit. Write a "## Gate Promotion Decisions" section in docs/promotion_decision.md summarizing each family's status, the data that earned (or did not earn) enforcement, and the D-15-07 provenance note for gpt-5-mini.</action>
  <verify>
    <automated>make eval-gates-check-baselines; grep -q "D-15" configs/eval_gates.yaml && grep -qi "Gate Promotion" docs/promotion_decision.md</automated>
  </verify>
  <acceptance_criteria>
    - configs/eval_gates.yaml: openai/gpt-4o-mini retains status: active with hard committed_itinerary_rate >= 0.8 and a rationale re-ratified against the Run #2 median (cites D-15-06)
    - configs/eval_gates.yaml: openai/gpt-5-mini is enforced ONLY if A2 cleared 0.6 AND flag-off Run #2 supports the floor; the rationale notes prod would need FORCED_COMMIT_STEP enabled (D-15-07). Otherwise status stays logged/aspirational with a Phase-15 measurement rationale
    - deepseek-reasoner / anthropic / gemini stay status: logged with hard: null and a Phase-15 rationale note; no 0.0-floor enforced gate exists anywhere in the file
    - `make eval-gates-check-baselines` exits 0
    - docs/promotion_decision.md has a "## Gate Promotion Decisions" section and records that a prod-default FORCED_COMMIT_STEP=6 flip is a separate, deferred decision (NOT implemented)
  </acceptance_criteria>
  <done>The gate config reflects honest, provenance-correct Phase-15 promotion decisions: anchor re-ratified, gpt-5-mini enforced only if doubly earned, everything else logged with a note, and no meaningless floors — verified by make eval-gates-check.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate baselines LAST from the Run #2 flag-off summary.json (D-15-04/05 runnable cells only)</name>
  <files>configs/eval_baselines/omakase_mission_open_ended.json, configs/eval_baselines/refinement_cheaper.json, docs/promotion_decision.md</files>
  <read_first>
    - docs/baseline_regen.md (the regen-last runbook: snapshot-baselines, write-baselines from summary.json, eval-gates-check-baselines, the gemini/anthropic deferral branches, the gpt-4o-mini-below-0.8 stop branch)
    - scripts/write_baselines.py (refusal rules: n_scored < n_requested -> exit 1; baseline_eligible: false -> refused; provenance stamps; _observations carry-forward)
    - scripts/check_baselines_fresh.py (staleness watch-set: app/agent/, app/llm_factory.py, configs/eval_matrix*; the regen-last ordering constraint)
    - configs/eval_baselines/omakase_mission_open_ended.json + refinement_cheaper.json (current cells; _observations to carry forward)
    - docs/promotion_decision.md (Plan 02 Run #2 provenance: the run dir + summary.json path)
  </read_first>
  <action>Confirm this runs AFTER all Phase-15 code changes are merged (D-15-05): if Plan 01 shipped a graph.py fix, it must be in the final merged code that produced the Run #2 summary.json — verify the Run #2 was the flag-off run against post-fix code (it was, per Plan 02 sequencing). Run `make snapshot-baselines` first (audit trail per docs/baseline_regen.md Step 2). Then run write_baselines.py against the Run #2 (flag-off) summary.json recorded by Plan 02: `make write-baselines SUMMARY=eval_reports/{run2_ts}/summary.json RUNS=5`. The tool writes runnable cells only and mechanically REFUSES partial cells (n_scored < n_requested) and quarantined scenarios — which is exactly D-15-04 (anthropic/gemini deferred-logged are absent from the arm config so they won't appear; late_night is not in configs/eval_matrix_arm.yaml). Confirm the regenerated configs/eval_baselines/omakase_mission_open_ended.json and refinement_cheaper.json carry fresh generated_at / generated_by provenance stamps and committed_itinerary_rate is present for openai/gpt-4o-mini, openai/gpt-5-mini, deepseek/deepseek-reasoner. Run `make eval-gates-check-baselines` (exit 0; an ASPIRATIONAL miss line for gpt-5-mini is acceptable and does not change exit). Run `python scripts/check_baselines_fresh.py origin/main` and confirm exit 0 on the final commit (the baseline JSONs are refreshed alongside any watch-set change). Record the baseline regen provenance (run dir, write_baselines exit code, refreshed cells, any deferral lines) in a "## Baseline Regen Provenance" section of docs/promotion_decision.md. If gpt-4o-mini median came in below 0.8, STOP per the docs/baseline_regen.md stop branch — do not write baselines, open a blocker.</action>
  <verify>
    <automated>make eval-gates-check-baselines</automated>
  </verify>
  <acceptance_criteria>
    - `make snapshot-baselines` ran before regen (audit trail preserved)
    - `make write-baselines SUMMARY=eval_reports/{run2_ts}/summary.json RUNS=5` exits 0 against the flag-off Run #2 summary
    - configs/eval_baselines/omakase_mission_open_ended.json and refinement_cheaper.json carry fresh generated_at / generated_by stamps and committed_itinerary_rate is present for gpt-4o-mini, gpt-5-mini, deepseek-reasoner
    - anthropic/gemini cells are NOT regenerated (absent from the arm config / deferred per D-12-09); late_night is not touched (quarantined)
    - `make eval-gates-check-baselines` exits 0; `python scripts/check_baselines_fresh.py origin/main` exits 0 on the final commit
    - docs/promotion_decision.md has a "## Baseline Regen Provenance" section recording the run dir + write_baselines result + refreshed cells
  </acceptance_criteria>
  <done>The runnable baseline cells are regenerated LAST from the honest flag-off Run #2 against final merged code, the staleness gate passes, and the provenance is recorded — with deferred/quarantined cells correctly untouched.</done>
</task>

<task type="auto">
  <name>Task 3: PROMO-03 latency report — honest per-turn decomposition vs ~30s/turn budget</name>
  <files>docs/promotion_decision.md</files>
  <read_first>
    - scripts/eval_agent.py (step_telemetry assembly: llm_call_seconds + tool_exec_seconds + tool_calls_this_step per step; INST-04)
    - the Run #2 run JSONs under eval_reports/{run2_ts}/ (deterministic.step_telemetry per query) AND the existing A2 run dir eval_reports/2026-06-12T07-27-03Z/ (corroborating per-step timings, incl. the ~46s-on-omakase anchor observation)
    - .planning/PROJECT.md (Decision 3: ~30s/turn budget; decisiveness/step-count is the dominant latency lever)
    - .planning/phases/15-gate-promotion-baseline-regen/15-CONTEXT.md (D-15 specifics: decompose honestly, do not flatter; the ~46s observation must appear)
    - docs/promotion_decision.md (sections written so far)
  </read_first>
  <action>Aggregate the PROMO-03 latency report from step_telemetry across the Run #2 (flag-off, prod-config) run JSONs — the prod-driver candidate is gpt-4o-mini. Your discretion whether to aggregate via a throwaway inline python loop or a small helper; do NOT add a committed script unless it earns reuse. For the gpt-4o-mini anchor, decompose per-turn time into LLM-call seconds (sum of step_telemetry[i].llm_call_seconds) vs sequential tool-execution seconds (sum of step_telemetry[i].tool_exec_seconds), and report the per-turn TOTAL explicitly against the ~30s/turn budget for BOTH scenarios. Report HONESTLY: include the inconvenient observation that the anchor's omakase per-turn total is ~46s — already OVER the ~30s budget on the open-ended scenario (corroborate against the A2 run dir eval_reports/2026-06-12T07-27-03Z). Do not report a single flattering number; show the per-scenario decomposition and name decisiveness (step count) as the dominant lever per Decision 3. Write the "## PROMO-03 Latency Report" section in docs/promotion_decision.md with the per-scenario LLM-vs-tool decomposition, the per-turn totals vs budget, the ~46s omakase observation called out, and a written prod-driver recommendation: ratify openai/gpt-4o-mini as the anchor (consistent with the ratified milestone scope), noting the latency reality rather than asserting budget compliance it does not have.</action>
  <verify>
    <automated>grep -qi "Latency" docs/promotion_decision.md && grep -q "30" docs/promotion_decision.md</automated>
  </verify>
  <acceptance_criteria>
    - docs/promotion_decision.md has a "## PROMO-03 Latency Report" section decomposing gpt-4o-mini per-turn time into llm_call_seconds vs tool_exec_seconds for both scenarios
    - The report states the anchor per-turn TOTAL explicitly against the ~30s/turn budget, and CALLS OUT the ~46s-on-omakase over-budget observation (does not flatter)
    - The report names decisiveness / step-count as the dominant latency lever (Decision 3)
    - The report ends with a written prod-driver recommendation to ratify openai/gpt-4o-mini, acknowledging the latency reality honestly
    - The aggregation reads existing run JSON step_telemetry only (zero new live spend)
  </acceptance_criteria>
  <done>The PROMO-03 latency report honestly decomposes the anchor's per-turn time vs the ~30s/turn budget, surfaces the over-budget omakase observation, and records the ratify-gpt-4o-mini prod-driver recommendation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan edits an eval gate config, regenerates committed baseline JSONs from an already-produced summary.json, and aggregates a latency report from existing run JSONs. No new request surface, no external input, no live API spend, no auth/network/secret handling. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-04 | Tampering | configs/eval_baselines/*.json | mitigate | Baselines are written only by scripts/write_baselines.py (D-11-14), which mechanically refuses partial/quarantined cells; snapshot-baselines preserves the prior numbers as an auditable floor; check_baselines_fresh.py enforces freshness on commit. |
| T-15-05 | Repudiation | gate-value provenance | mitigate | D-15-07 provenance honesty: the enforced gate value is sourced from the flag-off Run #2 (the config CI runs), not the FORCED_COMMIT_STEP=6 experiment; rationale fields cite the decision ID and run timestamp. |
| T-15-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan; no `## Package Legitimacy Audit` required. |
</threat_model>

<verification>
- configs/eval_gates.yaml updated with provenance-honest Phase-15 decisions; make eval-gates-check-baselines exits 0
- Runnable baseline cells regenerated from the flag-off Run #2; check_baselines_fresh.py exits 0; deferred/quarantined cells untouched
- PROMO-03 latency report written with the honest ~46s omakase observation and the ratify-anchor recommendation
- No new live spend; no prod-default flip
</verification>

<success_criteria>
All three PROMO requirements are delivered as committed artifacts: gates promoted honestly (PROMO-02), runnable baselines regenerated last against final code (PROMO-01), and the latency report decomposed honestly vs budget with the prod-driver recommendation (PROMO-03).
</success_criteria>

<output>
Create `.planning/phases/15-gate-promotion-baseline-regen/15-03-SUMMARY.md` when done.
</output>
