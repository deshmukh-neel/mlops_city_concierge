---
phase: 17-query-logging-log
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - alembic/versions/  # one new file: *_add_user_query_log.py (revision id auto-generated by make migration)
autonomous: true
requirements: [D-02, D-03, D-04]
must_haves:
  truths:
    - "D-03: A brand-new user_query_log table is created via a single Alembic migration whose down_revision is e0cd7069bc8f; no existing table (place_query_hits) is touched."
    - "D-02: The table has exactly 7 columns — id (uuid pk), message (text not null), requested_primary_types (text[]), num_stops (int null), rag_label (text), created_at (timestamptz not null default now()), session_id (text null)."
    - "D-04: The migration carries a comment documenting that raw message text is stored verbatim with no PII scrubbing because this is a private capstone DB, not a public service."
    - "make migrate applies cleanly (exit 0) and downgrade -1 drops the table and its index without error."
  artifacts:
    - path: "alembic/versions/*_add_user_query_log.py"
      provides: "user_query_log DDL (create table + idx_user_query_log_created_at index) with down_revision e0cd7069bc8f and the raw-store D-04 comment"
      contains: "user_query_log"
  key_links:
    - from: "alembic/versions/*_add_user_query_log.py"
      to: "alembic head e0cd7069bc8f"
      via: "down_revision linkage"
      pattern: "down_revision.*e0cd7069bc8f"
---

<objective>
Create the net-new `user_query_log` table — the demand-side learning signal for the v2.3 adaptive data loop — via a single Alembic migration chained onto the current head `e0cd7069bc8f`.

Purpose: Phase 18 (GAP) will mine this table for under-served neighborhood/cuisine demand. This phase delivers ONLY the schema; the write path is plan 17-02 and the miner repoint is Phase 18. Zero behavior change to the existing loop (D-03).

Output: One new migration file under `alembic/versions/` that, when applied, creates `user_query_log` (7 columns per D-02) plus a `created_at` index, with a comment documenting the raw-store privacy posture (D-04).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-query-logging-log/17-CONTEXT.md
@.planning/phases/17-query-logging-log/17-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate and author the user_query_log migration</name>
  <files>alembic/versions/&lt;auto&gt;_add_user_query_log.py (new; revision id + timestamp auto-generated by `make migration`)</files>
  <read_first>
    - alembic/versions/2026_05_08_1000-34679f77f726_add_places_ingest_query_proposals.py — the EXACT analog to mirror: uuid-style pk via server_default, `sa.Column` style, `sa.TIMESTAMP(timezone=True)` + `server_default=sa.text("NOW()")`, `op.create_index(name, table, [cols])`, matching `op.drop_index(name, table_name=...)` + `op.drop_table(...)` in downgrade.
    - alembic/versions/2026_05_14_1200-e0cd7069bc8f_add_place_relations.py — the CURRENT HEAD; confirm `revision = "e0cd7069bc8f"` (line 28). This is the new migration's `down_revision`.
    - .planning/phases/17-query-logging-log/17-PATTERNS.md § "alembic/versions/...add_user_query_log.py" — the header/DDL pattern to copy verbatim (lines 21-96), including the D-04 raw-store NOTE in the docstring.
    - .planning/phases/17-query-logging-log/17-CONTEXT.md D-02 (schema), D-03 (new table only / down_revision e0cd7069bc8f), D-04 (document raw-store).
    - alembic.ini line 14 — `file_template` (`%(year)d_%(month).2d_%(day).2d_%(hour).2d%(minute).2d-%(rev)s_%(slug)s`); the filename is auto-generated by `make migration`, do not hand-name it.
  </read_first>
  <action>
    Run `make migration MSG="add user_query_log"` to scaffold a new revision file under `alembic/versions/` (revision id is auto-generated; `make migration` auto-sets `down_revision` to the current head `e0cd7069bc8f` — verify it did, do NOT leave an autogenerate diff of unrelated tables in upgrade/downgrade; if `make migration` autogenerated spurious ops, replace the body with the explicit DDL below).
    Author `upgrade()` to `op.create_table("user_query_log", ...)` with EXACTLY these 7 columns per D-02: `id` = `sa.UUID(as_uuid=True)` primary_key with `server_default=sa.text("uuid_generate_v4()")` (the uuid-ossp extension is already enabled repo-wide — do NOT add `op.execute("CREATE EXTENSION...")`); `message` = `sa.Text` nullable=False; `requested_primary_types` = `sa.ARRAY(sa.Text)` nullable=True; `num_stops` = `sa.Integer` nullable=True; `rag_label` = `sa.Text` nullable=True; `created_at` = `sa.TIMESTAMP(timezone=True)` nullable=False with `server_default=sa.text("NOW()")`; `session_id` = `sa.Text` nullable=True. Then `op.create_index("idx_user_query_log_created_at", "user_query_log", ["created_at"])` (Claude's-discretion index endorsed in D-04 / CONTEXT, mirroring place_query_hits time-window indexing for Phase 18's miner).
    Author `downgrade()` to `op.drop_index("idx_user_query_log_created_at", table_name="user_query_log")` then `op.drop_table("user_query_log")`.
    In the module docstring, include a NOTE per D-04 stating raw message text is stored verbatim with no PII scrubbing because the entire value of the table is mining real demand text, and this is a private capstone DB not a public service. Keep `down_revision = "e0cd7069bc8f"`, `branch_labels = None`, `depends_on = None` (analog style). Use `op.create_table`/`op.create_index`, NOT raw `op.execute()` SQL.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && ls alembic/versions/*_add_user_query_log.py && grep -l 'down_revision.*=.*"e0cd7069bc8f"' alembic/versions/*_add_user_query_log.py && grep -q 'idx_user_query_log_created_at' alembic/versions/*_add_user_query_log.py && grep -Eiq 'no PII|verbatim|private capstone' alembic/versions/*_add_user_query_log.py && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - A file matching `alembic/versions/*_add_user_query_log.py` exists.
    - That file contains `down_revision: str | Sequence[str] | None = "e0cd7069bc8f"`.
    - `op.create_table("user_query_log", ...)` declares all 7 D-02 columns with the specified types/nullability/defaults and no extra columns.
    - `op.create_index("idx_user_query_log_created_at", "user_query_log", ["created_at"])` is present; `downgrade()` drops both the index and the table.
    - The docstring contains a D-04 raw-store note (grep matches one of: `no PII`, `verbatim`, `private capstone`).
    - No `op.execute("CREATE EXTENSION` and no autogenerated ops for tables other than `user_query_log`.
  </acceptance_criteria>
  <done>The migration file exists, chains to head e0cd7069bc8f, declares the 7-column user_query_log table + created_at index, downgrades cleanly in source, and documents the raw-store decision.</done>
</task>

<task type="auto">
  <name>Task 2: Apply the migration and verify the live schema round-trips</name>
  <files>alembic/versions/&lt;auto&gt;_add_user_query_log.py (the file authored in Task 1; applied via `make migrate`, no source edits)</files>
  <read_first>
    - CLAUDE.md § Architecture (Alembic) — `make migrate` applies (`alembic upgrade head`); DB URL resolved via `app.db_url.resolve_alembic_database_url()`.
    - Memory gotcha `project_local_postgres_port_collision` — Postgres.app squats host 5432; this project uses 5433 for both prod + sandbox URLs. If applying locally, ensure DATABASE_URL targets the running Docker Postgres (`make db-up`), not Postgres.app on 5432.
    - tests/unit/test_alembic_config.py — confirms the `app.db_url` resolver contract `alembic/env.py` depends on; do not break it.
    - The file authored in Task 1.
  </read_first>
  <action>
    Bring up the local DB if needed (`make db-up`), then run `make migrate` (alembic upgrade head) and confirm exit 0. Inspect the live schema to confirm the 7 columns + index exist (e.g. `psql "$DATABASE_URL" -c '\d user_query_log'` or an `information_schema.columns` query). Then prove the downgrade is reversible: `alembic downgrade -1` (exit 0, table gone), followed by `alembic upgrade head` again (exit 0, table back) so the migration is left applied. Do NOT stamp or hand-edit prod alembic state — this task validates against the LOCAL/Docker DB only (the memory note `project_prod_alembic_w7_divergence` is a prod-deploy concern, out of scope here).
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && make migrate && python -c "import os,psycopg2; from app.db_url import resolve_alembic_database_url; c=psycopg2.connect(resolve_alembic_database_url(env=os.environ)); cur=c.cursor(); cur.execute(\"select column_name from information_schema.columns where table_name='user_query_log' order by 1\"); cols=sorted(r[0] for r in cur.fetchall()); assert cols==['created_at','id','message','num_stops','rag_label','requested_primary_types','session_id'], cols; cur.execute(\"select 1 from pg_indexes where indexname='idx_user_query_log_created_at'\"); assert cur.fetchone(), 'index missing'; print('PASS', cols)"</automated>
  </verify>
  <acceptance_criteria>
    - `make migrate` exits 0.
    - `information_schema.columns` for `user_query_log` returns exactly the 7 column names: created_at, id, message, num_stops, rag_label, requested_primary_types, session_id.
    - `pg_indexes` contains `idx_user_query_log_created_at`.
    - `alembic downgrade -1` then `alembic upgrade head` both exit 0 (migration is reversible and left applied).
  </acceptance_criteria>
  <done>The user_query_log table physically exists in the local DB with all 7 columns and the created_at index, and the migration is proven reversible.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration runner → Cloud SQL / local Postgres | DDL executed by a trusted CI/operator job (`make migrate`); no untrusted input crosses here at migration time. |
| (deferred to 17-02) `/chat` user → user_query_log row | Untrusted raw user `message` is WRITTEN into this table by plan 17-02; this plan only defines the column to hold it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | Tampering | migration `down_revision` linkage | mitigate | Pin `down_revision = "e0cd7069bc8f"` and assert it in Task 1 verify; a wrong/duplicate head would corrupt the migration DAG. |
| T-17-02 | Information disclosure | raw `message` column storing PII | accept | D-04: raw verbatim store, documented in the migration comment; private capstone DB, intrinsically low-PII SF place queries. Not a public service. No high-severity exposure. |
| T-17-03 | Denial of service | unbounded table growth (no retention cap) | accept | D-04: retention/cleanup deferred to Phase 19+; capstone volume is negligible. Index on created_at keeps Phase 18 time-window queries cheap. |
| T-17-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed in this plan (alembic/sqlalchemy/psycopg2 already in pyproject.toml). No package-legitimacy checkpoint required. |
</threat_model>

<verification>
- `make migrate` exits 0 against the local Docker DB.
- `user_query_log` has the 7 D-02 columns + `idx_user_query_log_created_at`.
- Migration `down_revision` is `e0cd7069bc8f`; downgrade/upgrade round-trips cleanly.
- No existing table (`place_query_hits`, `coverage_agent.py`) is modified (D-03 boundary guard).
- `make test` full suite stays green (the alembic-config contract test in tests/unit/test_alembic_config.py still passes).
</verification>

<success_criteria>
- A single new Alembic migration creates `user_query_log` (7 columns, created_at index) chained to head `e0cd7069bc8f`.
- The raw-store privacy decision (D-04) is documented in the migration.
- The migration applies and reverses cleanly on the local DB.
- Zero changes to `scripts/coverage_agent.py`, `scripts/ingest_places_sf.py`, or `place_query_hits` (Phase 18 scope).
</success_criteria>

<output>
Create `.planning/phases/17-query-logging-log/17-01-SUMMARY.md` when done.
</output>
