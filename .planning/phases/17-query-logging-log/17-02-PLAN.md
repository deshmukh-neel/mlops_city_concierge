---
phase: 17-query-logging-log
plan: 02
type: execute
wave: 2
depends_on: ["17-01"]
files_modified:
  - app/query_log.py
  - app/main.py
  - tests/conftest.py
  - tests/unit/test_query_log.py
  - tests/unit/test_chat_endpoint.py
  - tests/integration/test_query_log.py
autonomous: true
requirements: [D-01, D-02, D-04]
must_haves:
  truths:
    - "D-01: /chat schedules the user-query INSERT via FastAPI BackgroundTasks AFTER the ChatResponse is built, on the main graph path, off the synchronous response path — the log fires fire-and-forget and never blocks or delays the reply (it runs after the response is constructed, not on the response-construction contract)."
    - "D-02: log_user_query receives message, requested_primary_types, num_stops, and rag_label as arguments captured in the handler (extracted_types at main.py:724, num_stops hoisted from the inline call at main.py:786, rag_label at main.py:668) — it never re-derives them — and INSERTs them into user_query_log."
    - "D-04: log_user_query swallows ALL exceptions (catch Exception, not just psycopg2.Error) and emits logger.warning(..., exc_info=True) so a logging failure can never affect the reply or leave an unawaited-task warning. Fail-open, fail-quiet."
    - "The INSERT uses psycopg2 %s parameters (never f-string/%-formatted SQL), is unit-tested with the mocked FakeCursor/FakeConnection seam (asserts SQL + params + commit) for BOTH the populated and the empty requested_primary_types=[] case, and round-trips a real row under APP_ENV=integration."
    - "The new BackgroundTask never activates a real DB pool in the existing /chat unit/functional tests: tests/conftest.py neutralizes app.main.log_user_query so no existing /chat test fires a real get_conn() (closing the known full-suite DB-pool-contamination class), and the fixture carries a comment documenting that it suppresses logging globally so any future real-write test must override it."
  artifacts:
    - path: "app/query_log.py"
      provides: "log_user_query(*, message, requested_primary_types, num_stops, rag_label, session_id=None) -> None — sync fire-and-forget INSERT via get_conn(), fail-open"
      exports: ["log_user_query"]
      min_lines: 20
    - path: "tests/conftest.py"
      provides: "autouse fixture neutralizing app.main.log_user_query (no-op spy) so existing /chat TestClient tests never activate a real DB pool via the new BackgroundTask, with a comment documenting the global-suppression contract"
      contains: "log_user_query"
    - path: "tests/unit/test_query_log.py"
      provides: "mocked-conn unit tests: happy-path SQL/params/commit assertion (populated + empty-array cases) + fail-open (raising conn -> no exception, logger.warning called)"
      contains: "log_user_query"
    - path: "tests/integration/test_query_log.py"
      provides: "APP_ENV=integration real-DB INSERT round-trip into user_query_log (skipped by default)"
      contains: "user_query_log"
  key_links:
    - from: "app/main.py chat()"
      to: "app.query_log.log_user_query"
      via: "background_tasks.add_task(...)"
      pattern: "background_tasks\\.add_task\\(\\s*log_user_query"
    - from: "app/query_log.py log_user_query"
      to: "user_query_log table"
      via: "INSERT via get_conn() + conn.commit()"
      pattern: "INSERT INTO user_query_log"
---

<objective>
Add the fire-and-forget write path that logs every main-path `/chat` demand query into `user_query_log` as the loop's demand signal: a sync `log_user_query` helper (D-01 mechanism / D-04 failure posture), its unit + integration tests, the `BackgroundTasks` wiring in `chat()` (D-01 / D-02), and a test-isolation fixture so the new background task never activates a real DB pool in the existing `/chat` suite.

Purpose: turn the loop's learning signal from ingestion hits into real user demand. The write is a side-channel — it must never delay or break the `/chat` reply.

Output: `app/query_log.py` (new module), unit + integration tests, a `tests/conftest.py` autouse fixture that neutralizes `app.main.log_user_query` for the existing `/chat` tests, and a one-line `BackgroundTasks` scheduling call on the main graph path in `app/main.py`. No changes to the `/chat` request/response contract (LOG is a side-channel write).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-query-logging-log/17-CONTEXT.md
@.planning/phases/17-query-logging-log/17-PATTERNS.md
@.planning/phases/17-query-logging-log/17-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create app/query_log.py with fail-open log_user_query + unit tests</name>
  <files>app/query_log.py, tests/unit/test_query_log.py</files>
  <read_first>
    - app/db.py — `get_conn()` (lines 36-46): the sanctioned pooled-connection context manager; rolls back on exit but does NOT auto-commit, so the writer must call `conn.commit()`.
    - scripts/coverage_agent.py lines 247-258 — the INSERT analog (`with get_conn() as conn, conn.cursor() as cur: cur.execute(sql, [params]); conn.commit()`).
    - app/main.py lines 480-499 (`_place_is_open_now`) — the fail-open + `logger.warning(..., exc_info=True)` precedent. NOTE: that one catches `psycopg2.Error`; log_user_query must catch `Exception` (broader) because `get_conn()` itself can raise `RuntimeError("Missing DATABASE_URL...")` from db_pool._ensure_db_pool() (PATTERNS.md § "Exception swallowing").
    - .planning/phases/17-query-logging-log/17-PATTERNS.md § "app/query_log.py" (lines 100-191) — the full function shape (signature, INSERT SQL, `except Exception`), and § "Shared Patterns" (lines 283-304) for the commit + test-seam guidance.
    - tests/unit/test_tools_retrieval.py lines 46-92 — the `FakeCursor` / `FakeConnection` / `patch_get_conn(mocker)` fixture to COPY into the new test file. NOTE: `FakeConnection` there has no `commit()` method and is not itself the context manager (`get_conn` is) — add a `commit_called` flag to the copied `FakeConnection` and have `fake_get_conn` yield it through a `@contextmanager`.
    - tests/unit/test_db.py lines 39-53 — the error-swallow + `logger.warning` assertion pattern (mocker.patch the dependency to raise; assert no propagation + warning called).
    - .planning/phases/17-query-logging-log/17-CONTEXT.md D-02 (which slots), D-04 (fail-open/fail-quiet, catch all), Claude's-Discretion (session_id may be null; helper decomposition).
  </read_first>
  <behavior>
    - Happy path (populated): calling log_user_query(message="date night in North Beach", requested_primary_types=["restaurant","bar"], num_stops=3, rag_label="openai:gpt-4o-mini") executes one INSERT INTO user_query_log with %s params bound to [message, requested_primary_types, num_stops, rag_label, session_id] (session_id defaults None) and calls conn.commit() exactly once.
    - Happy path (empty array — the COMMON free-text case): calling log_user_query(message="find me good tacos", requested_primary_types=[], num_stops=None, rag_label="openai:gpt-4o-mini") binds requested_primary_types as `[]` (an empty list, NOT null/None) and num_stops as `None`, and still calls conn.commit() exactly once. This is the dominant real-traffic shape (most /chat free-text leaves extracted_types empty and names no stop count).
    - SQL safety: the INSERT statement string is a fixed template using %s placeholders; the raw user `message` is passed as a bound param, never interpolated into the SQL string (no f-string / %-format of values into SQL).
    - Fail-open: when get_conn() raises (e.g. RuntimeError("db down")) OR cur.execute raises, log_user_query returns None WITHOUT raising and logs exactly one logger.warning(..., exc_info=True).
    - session_id is an optional keyword arg defaulting to None (Claude's discretion: leaving it null is acceptable per D-02 note; if a cheap per-turn marker is derived in Task 2's wiring it is passed through, otherwise None).
  </behavior>
  <action>
    Create `app/query_log.py` exporting a single public function `log_user_query(*, message: str, requested_primary_types: list[str], num_stops: int | None, rag_label: str, session_id: str | None = None) -> None`. Use a module-level `logger = logging.getLogger(__name__)` and `from .db import get_conn`. Body: a `try:` wrapping `with get_conn() as conn, conn.cursor() as cur:` that runs `cur.execute("INSERT INTO user_query_log (message, requested_primary_types, num_stops, rag_label, session_id) VALUES (%s, %s, %s, %s, %s)", [message, requested_primary_types, num_stops, rag_label, session_id])` then `conn.commit()`; an `except Exception:` branch that calls `logger.warning("log_user_query failed; skipping", exc_info=True)` and returns (NEVER re-raise — this runs in a BackgroundTask threadpool and must not surface). Keep the docstring noting fire-and-forget / fail-open / fail-quiet per D-01 + D-04. Optional internal decomposition (e.g. a thin `_insert(...)` helper) is Claude's discretion as long as the public signature and fail-open contract hold.
    Create `tests/unit/test_query_log.py`: copy the `FakeCursor`/`FakeConnection`/`patch_get_conn` seam from tests/unit/test_tools_retrieval.py, extending `FakeConnection` with a `commit_called: bool` flag set True in a `commit()` method, and patching `app.query_log.get_conn` (NOT app.tools.retrieval.get_conn). Tests: (1) happy-path populated — assert `cur.executed_sql` contains `INSERT INTO user_query_log`, `cur.executed_params == [message, ["restaurant","bar"], num_stops, rag_label, None]`, and `conn.commit_called is True`; (2) happy-path empty-array — call `log_user_query(message="find me good tacos", requested_primary_types=[], num_stops=None, rag_label="openai:gpt-4o-mini")` and assert `cur.executed_params == ["find me good tacos", [], None, "openai:gpt-4o-mini", None]` (the empty list binds as `[]`, NOT None — assert `cur.executed_params[1] == []` and `cur.executed_params[2] is None`) and `conn.commit_called is True`; (3) param-safety — assert the executed SQL string contains five `%s` placeholders and does NOT contain the literal message text (proves parameterization, not interpolation); (4) fail-open via get_conn raising — `mocker.patch("app.query_log.get_conn", side_effect=RuntimeError("db down"))`, assert `log_user_query(...)` returns None without raising and a patched logger.warning was called once with `exc_info=True`; (5) fail-open via cur.execute raising — FakeCursor.execute raises, same no-propagation + warning assertion.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && poetry run pytest tests/unit/test_query_log.py -v && grep -q 'INSERT INTO user_query_log' app/query_log.py && grep -q 'except Exception' app/query_log.py && grep -q 'exc_info=True' app/query_log.py && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `app/query_log.py` exists and exports `log_user_query` with the exact keyword-only signature above.
    - `pytest tests/unit/test_query_log.py` exits 0 with all five tests passing (populated happy-path, empty-array happy-path, param-safety, two fail-open paths).
    - The empty-array test asserts `requested_primary_types=[]` binds as an empty list (`executed_params[1] == []`) and `num_stops=None` binds as `None` (`executed_params[2] is None`), and `conn.commit_called is True`.
    - `grep 'INSERT INTO user_query_log' app/query_log.py` matches and the SQL uses five `%s` placeholders.
    - The INSERT uses bound params, never f-string/%-format/string-concat of `message` into SQL (param-safety unit test asserts the message literal is absent from the executed SQL string).
    - `grep 'except Exception' app/query_log.py` and `grep 'exc_info=True' app/query_log.py` both match (D-04 catch-all + traceback).
  </acceptance_criteria>
  <done>log_user_query is a tested, fail-open sync INSERT helper: happy path writes the 5 D-02 fields + commits for BOTH the populated and the empty-array case, and any DB/connection error is swallowed with a single warning, never propagating.</done>
</task>

<task type="auto">
  <name>Task 2: Wire BackgroundTasks logging into chat() + neutralize it in the existing /chat tests</name>
  <files>app/main.py, tests/conftest.py</files>
  <read_first>
    - app/main.py line 13 — `from fastapi import Depends, FastAPI, HTTPException, Request`; add `BackgroundTasks` to this import (PATTERNS.md lines 238-244).
    - app/main.py lines 662-805 — the `chat()` endpoint. Specifically: signature `async def chat(req: ChatRequest, request: Request)` at 663 (add the `background_tasks: BackgroundTasks` param — FastAPI injects it automatically); `rag_label` local at 668; `extracted_types` local at 724; the inline `explicit_num_stops_from_conversation(req.history, req.message)` at 786 (NOT a local — hoist it to a local `num_stops` before the `ItineraryState(...)` build and reuse it there); the main-path `return ChatResponse(...)` at 800.
    - app/main.py lines 685-696 — the closure-decision early-returns: `_try_accept_path` accept early-return at 689-691, `_decline_path` return at 695. These fire BEFORE `extracted_types`/`num_stops` are computed (they short-circuit before the `with trace_request(...)` block).
    - .planning/phases/17-query-logging-log/17-PATTERNS.md § "app/main.py — modified chat()" (lines 195-279) — the BackgroundTasks idiom + where add_task goes + the exit-paths decision.
    - .planning/phases/17-query-logging-log/17-CONTEXT.md D-01 (paths note: main graph path mandatory; closure early-returns are Claude's discretion, low demand-signal), D-02 (pass captured slots as args, do not re-derive).
    - tests/conftest.py — the existing `_patch_env` autouse fixture (lines 25-43) sets `DATABASE_URL` to a localhost target; `app/db_pool.py` line 93 (`_ensure_db_pool`) lazily builds a REAL ThreadedConnectionPool on first `get_conn()`. This is the seam the isolation fixture below plugs.
    - app/db_pool.py around line 93 — confirms `get_conn()` lazily initializes a real pool the first time it is called. Under TestClient, BackgroundTasks run synchronously before the POST returns, so an unpatched `log_user_query` WOULD call this in every existing /chat test.
    - tests/unit/test_chat_endpoint.py and tests/unit/test_chat_functional.py — the existing /chat tests (all use `TestClient(app)`); they must stay green AND must NOT activate a real DB pool via the new background task. Memory `project_full_suite_db_pool_contamination` — this is exactly the contamination class to prevent.
  </read_first>
  <action>
    PART A — wire the log. Add `BackgroundTasks` to the fastapi import on line 13 and `from .query_log import log_user_query` to the app/main.py imports. Add a `background_tasks: BackgroundTasks` parameter to `chat()` (FastAPI auto-injects it; placing it after `request: Request` is fine). Hoist the num_stops computation: before constructing `ItineraryState` at ~786, assign `num_stops = explicit_num_stops_from_conversation(req.history, req.message)` to a local and pass that local into `UserConstraints(num_stops=num_stops, ...)` (no behavior change, just capture). After the main-path `ChatResponse(...)` is built at ~800, call `background_tasks.add_task(log_user_query, message=req.message, requested_primary_types=extracted_types, num_stops=num_stops, rag_label=rag_label)` and then return the response — schedule, then return (D-01: fires AFTER the response is constructed, off the synchronous response path, never delaying the reply). DECISION (per D-01 Claude's discretion): log ONLY the main graph path; do NOT log the `_try_accept_path` accept early-return or `_decline_path` early-return — those are short accept/decline closure replies (NOT new demand queries) with low demand-signal, and they short-circuit before `extracted_types`/`num_stops` exist, so logging them would require re-deriving slots (violating D-02's "pass captured args, don't re-derive"). Document this main-path-only scope choice in a short code comment at the add_task call. Do NOT add any field to `ChatRequest`/`ChatResponse` (LOG is a side-channel write — boundary guard).
    PART B — isolate the existing /chat tests (HARD requirement, not optional). Add an autouse fixture to `tests/conftest.py` (e.g. `_neutralize_query_log`) that does `mocker`/`monkeypatch`-style patches `app.main.log_user_query` with a no-op spy (e.g. `monkeypatch.setattr("app.main.log_user_query", lambda **kwargs: None)`, or a `MagicMock`) so the BackgroundTask scheduled by `chat()` never calls the real `get_conn()` / activates a real ThreadedConnectionPool during the existing /chat unit + functional tests. This MUST be autouse so every existing TestClient-based /chat test (test_chat_endpoint.py, test_chat_functional.py) is covered without per-test edits. Keep the existing `_patch_env` autouse fixture intact (do not merge or remove it). Because the new dedicated scheduling-assertion test below installs its OWN local spy via `mocker.patch("app.main.log_user_query", ...)`, the conftest fixture must use a mechanism that a per-test `mocker.patch` cleanly overrides (monkeypatch/setattr on the module attribute satisfies this — the later in-test patch wins for that test). DOCUMENT THE GLOBAL-SUPPRESSION CONTRACT: add a short code comment (or docstring) on the autouse fixture stating that it suppresses `app.main.log_user_query` globally so the BackgroundTask never activates a real DB pool in unit/functional tests, AND that any FUTURE test that needs the REAL write to run (a "real /chat writes a DB row" test) MUST override/disable this fixture — e.g. re-patch `app.main.log_user_query` locally with `mocker.patch(...)` or use the APP_ENV=integration test. The comment must contain the literal word `override` so the contract is greppable.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && python -c "import ast; src=open('app/main.py').read(); assert 'BackgroundTasks' in src and 'from .query_log import log_user_query' in src and 'background_tasks.add_task' in src; t=ast.parse(src); fn=[n for n in ast.walk(t) if isinstance(n,(ast.AsyncFunctionDef,ast.FunctionDef)) and n.name=='chat'][0]; params=[a.arg for a in fn.args.args]; assert 'background_tasks' in params, params; print('PASS', params)" && grep -q 'log_user_query' tests/conftest.py && grep -q 'override' tests/conftest.py && poetry run pytest tests/unit/test_chat_endpoint.py tests/unit/test_chat_functional.py -v && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `app/main.py` imports `BackgroundTasks` from fastapi and `log_user_query` from `.query_log`.
    - The `chat()` function signature includes a `background_tasks` parameter (AST-checked).
    - `num_stops` is captured as a local and passed both into `UserConstraints(...)` and into `add_task(...)` (no double-derivation).
    - `background_tasks.add_task(log_user_query, message=req.message, requested_primary_types=extracted_types, num_stops=num_stops, rag_label=rag_label)` appears AFTER the main-path ChatResponse is built and BEFORE its return.
    - Only the main graph path logs; the accept/decline early-returns are NOT instrumented (documented by comment).
    - `tests/conftest.py` contains an autouse fixture that patches `app.main.log_user_query` to a no-op spy (grep `log_user_query` in tests/conftest.py matches), so the existing /chat TestClient tests never activate a real DB pool via the new BackgroundTask.
    - The autouse fixture carries a comment/docstring documenting the global-suppression contract — that it suppresses logging so the BackgroundTask never opens a real DB pool in unit/functional tests, and that any future real-write test must override it (`grep -q 'override' tests/conftest.py` matches).
    - `pytest tests/unit/test_chat_endpoint.py tests/unit/test_chat_functional.py` exits 0 — no change to the request/response contract, no DB-pool activation warnings from the background task.
  </acceptance_criteria>
  <done>chat() schedules log_user_query via BackgroundTasks on the main graph path with captured slots, off the synchronous response path, the existing /chat tests still pass with no contract change, and an autouse conftest fixture (with a documented override contract) guarantees those tests never activate a real DB pool through the new background task.</done>
</task>

<task type="auto">
  <name>Task 3: Scheduling-assertion test + integration round-trip + full-suite verification</name>
  <files>tests/unit/test_chat_endpoint.py, tests/integration/test_query_log.py</files>
  <read_first>
    - tests/unit/test_chat_endpoint.py lines 39-91 (`test_chat_endpoint_returns_reply_places_raglabel`) — the canonical happy-path /chat TestClient test to model the scheduling-assertion test on (stubs `load_registered_rag_chain` + `build_agent_graph`, POSTs a free-text message, asserts 200). The new test follows this exact setup but additionally installs a LOCAL spy.
    - tests/conftest.py — the autouse `_neutralize_query_log` fixture from Task 2 patches `app.main.log_user_query` to a no-op. The scheduling-assertion test OVERRIDES it within the test body via `mocker.patch("app.main.log_user_query", <spy>)` so it can assert the exact kwargs the BackgroundTask scheduled (the later in-test patch wins).
    - tests/integration/test_db.py and tests/integration/test_booking.py — the integration-test gate pattern: `pytestmark = pytest.mark.skipif(os.getenv("APP_ENV", "test") != "integration", reason="...")` at the top, real `get_conn()`/DB usage below with `# noqa: E402` imports after the skipif.
    - app/query_log.py (from Task 1) and app/db.py get_conn().
    - The migration from plan 17-01 (the `user_query_log` table must exist — this test depends on 17-01 being applied via `make migrate`).
    - Memory gotcha `project_full_suite_db_pool_contamination` — real-graph/real-DB tests can leak a live DB pool into the full suite; verification MUST run the FULL suite (`make test`), not just the changed file. Memory `project_local_postgres_port_collision` — use the Docker DB on 5433, not Postgres.app on 5432.
    - .planning/phases/17-query-logging-log/17-CONTEXT.md D-01 (main-path logging), D-02 (the columns to assert) + the test-layering requirement (unit + integration both required for a new write module).
  </read_first>
  <action>
    PART A — scheduling-assertion test (proves the wire AND doubles as the isolation seam). In `tests/unit/test_chat_endpoint.py`, add a test (e.g. `test_chat_endpoint_schedules_query_log_with_captured_slots`) modeled on `test_chat_endpoint_returns_reply_places_raglabel`: stub `app.main.load_registered_rag_chain` + `app.main.build_agent_graph` (free-text message, returns a normal final state), install a LOCAL spy via `spy = mocker.patch("app.main.log_user_query")` (this overrides the autouse conftest no-op for this test), POST a free-text `/chat` message, assert 200, then assert `spy` was called EXACTLY ONCE with the captured slots as keyword args — `spy.assert_called_once_with(message=<the posted message>, requested_primary_types=<expected extracted_types, e.g. [] for a plain free-text message>, num_stops=<expected, e.g. None>, rag_label="openai:gpt-4o-mini")` (use the kwargs that the free-text fixture actually produces; if extracted_types is `[]` for a plain free-text message, assert `requested_primary_types=[]`). Because TestClient runs BackgroundTasks synchronously before `client.post(...)` returns, the spy is guaranteed called by the time the assertion runs. This test is the concrete proof that the add_task wiring schedules log_user_query with the exact D-02 kwargs (message, requested_primary_types, num_stops, rag_label).
    PART B — integration round-trip. Create `tests/integration/test_query_log.py` gated by `pytestmark = pytest.mark.skipif(os.getenv("APP_ENV", "test") != "integration", reason="Set APP_ENV=integration and provide a real DATABASE_URL to run integration tests.")` (copy the exact skipif from tests/integration/test_db.py). The test calls `log_user_query(message="integration test query", requested_primary_types=["restaurant"], num_stops=2, rag_label="test-label", session_id="it-marker-<unique>")` (use a unique marker per run, e.g. a uuid suffix), then opens its own `with get_conn() as conn, conn.cursor() as cur:` to `SELECT message, requested_primary_types, num_stops, rag_label, session_id FROM user_query_log WHERE session_id = %s` for that marker and asserts exactly one row with the round-tripped values (message verbatim, requested_primary_types == ["restaurant"], num_stops == 2, rag_label == "test-label"). ALSO assert the dominant empty-array shape adapts in a real DB: in the same test (or a second assertion block) call `log_user_query(message="real-db empty-array case", requested_primary_types=[], num_stops=None, rag_label="test-label", session_id="it-empty-<unique>")` and SELECT it back, asserting `requested_primary_types == []` (an empty Postgres text[] array, NOT None) — this proves psycopg2/Postgres adapts the common free-text `[]` case end-to-end. Clean up the inserted rows in a try/finally (DELETE WHERE session_id IN the markers, committed) so repeated runs stay idempotent. Use `# noqa: E402` on imports placed after the skipif, matching the analog.
    After the new tests pass (the scheduling-assertion test under the default env, the integration test under `APP_ENV=integration`), run the FULL suite (`make test`) — not just the new files — per the known full-suite DB-pool-contamination gotcha; the suite must stay green.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && grep -q 'log_user_query' tests/unit/test_chat_endpoint.py && grep -q 'assert_called_once_with' tests/unit/test_chat_endpoint.py && grep -q 'APP_ENV' tests/integration/test_query_log.py && grep -q 'skipif' tests/integration/test_query_log.py && grep -q 'user_query_log' tests/integration/test_query_log.py && poetry run pytest tests/unit/test_chat_endpoint.py -v && APP_ENV=integration poetry run pytest tests/integration/test_query_log.py -v && make test && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `tests/unit/test_chat_endpoint.py` has a test that installs a local `mocker.patch("app.main.log_user_query")` spy and asserts it was scheduled EXACTLY ONCE with the exact kwargs `message`, `requested_primary_types`, `num_stops`, `rag_label` (`assert_called_once_with`), proving the add_task wiring passes the captured D-02 slots.
    - `tests/integration/test_query_log.py` exists with the `pytest.mark.skipif(... APP_ENV ...)` gate so it is SKIPPED by default and only runs under `APP_ENV=integration`.
    - Under `APP_ENV=integration` (with the 17-01 migration applied), the test inserts via `log_user_query` and SELECTs back exactly one row with the verbatim message + round-tripped slots, then deletes it.
    - The integration test also round-trips the empty-array case (`requested_primary_types=[]`) and asserts it reads back as `[]` (not None), proving psycopg2/Postgres adaptation for the dominant free-text shape.
    - `make test` (full suite) exits 0 with no DB-pool leakage — verification uses the full suite, not just the changed files.
  </acceptance_criteria>
  <done>A unit test patches app.main.log_user_query with a spy and proves it is scheduled once with the captured D-02 kwargs; a real INSERT through log_user_query round-trips a row into user_query_log under APP_ENV=integration (including the empty-array case, skipped otherwise); and the full suite stays green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `/chat` user → `log_user_query` → user_query_log | The raw user `message` (untrusted) crosses into a SQL write here. This is the primary boundary for this plan. |
| `/chat` request path → BackgroundTask threadpool | The log runs out-of-band after the response; a failure in the threadpool must not propagate to the request. |
| existing /chat test suite → real DB pool | The new BackgroundTask, run synchronously by TestClient, could activate a real ThreadedConnectionPool in tests that previously never touched the DB. The conftest isolation fixture is the guard at this boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-04 | Tampering | SQL injection via raw `message` at write time | mitigate | The INSERT uses psycopg2 `%s` bound parameters; `message` is passed as a param, never string-formatted into SQL. Enforced by Task 1 param-safety unit test (asserts the message literal never appears in the executed SQL string). |
| T-17-05 | Denial of service | a DB failure in logging blocking/breaking `/chat` | mitigate | D-01 fire-and-forget via BackgroundTasks (runs after the response is constructed, off the synchronous response path) + D-04 swallow-all-exceptions (`except Exception` + logger.warning, never re-raise). A logging failure cannot delay or break the reply. Verified by the fail-open unit tests (get_conn raising AND cur.execute raising). |
| T-17-06 | Information disclosure | PII in raw stored `message` | accept | D-04: raw verbatim store is an explicitly accepted risk (private capstone DB, low-PII SF place queries). Documented in the 17-01 migration comment. Not a blocker. |
| T-17-07 | Repudiation / Info disclosure | over-capture (full reply, committed place_ids) | mitigate | D-02 scope: only message + extracted slots + lightweight metadata are logged; full reply / place_ids are intentionally NOT captured (overlaps MLflow trace_request). No field added to ChatRequest/ChatResponse. |
| T-17-08 | Denial of service | new BackgroundTask activates a real DB pool in the existing /chat test suite (known full-suite DB-pool-contamination class) | mitigate | Autouse `tests/conftest.py` fixture neutralizes `app.main.log_user_query` (no-op spy) so existing TestClient /chat tests never fire a real `get_conn()`; the fixture comment documents that future real-write tests must override it. The dedicated scheduling-assertion test installs its own local spy to assert kwargs without touching the DB. Verified by `make test` (full suite green, no pool-leak). |
| T-17-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed (fastapi `BackgroundTasks`, psycopg2, pytest-mock all already in pyproject.toml). No package-legitimacy checkpoint required. |
</threat_model>

<verification>
- `pytest tests/unit/test_query_log.py` exits 0 (populated + empty-array happy-paths SQL/params/commit + param-safety + two fail-open paths).
- `app/main.py chat()` has a `background_tasks: BackgroundTasks` param and schedules `log_user_query` on the main graph path only (AST + grep checked); existing /chat tests stay green with no contract change.
- `tests/conftest.py` has an autouse fixture neutralizing `app.main.log_user_query` so the existing /chat TestClient tests never activate a real DB pool via the new BackgroundTask, with a comment documenting that future real-write tests must override it (T-17-08).
- A unit test patches `app.main.log_user_query` with a spy and asserts `assert_called_once_with` the captured D-02 kwargs (message, requested_primary_types, num_stops, rag_label).
- `tests/integration/test_query_log.py` round-trips a real row under `APP_ENV=integration` (including the empty-array case; skipped by default).
- The INSERT is parameterized (`%s`), never string-interpolated (T-17-04).
- The log is fire-and-forget and fail-open (T-17-05): a DB error neither raises nor delays the reply.
- `make test` (FULL suite) exits 0 — required because of the known full-suite DB-pool-contamination gotcha; running only the changed file is insufficient.
- No modification to `scripts/coverage_agent.py`, `scripts/ingest_places_sf.py`, `place_query_hits`, or the `/chat` request/response schema (boundary guard).
</verification>

<success_criteria>
- `/chat` logs every real main-path demand query into `user_query_log` via BackgroundTasks, off the synchronous response path so the log never delays the reply (D-01). The accept/decline closure early-returns are intentionally NOT logged (Claude's-discretion under D-01: short closure replies, not new demand queries, and slots are not yet derived there).
- The logged record captures message + extracted_types + num_stops + rag_label, passed as captured handler args (D-02).
- The write fails open and quiet (catch-all + warning), so a logging failure can never affect the reply (D-04).
- The new background task is fully isolated from the existing /chat test suite via an autouse conftest fixture (with a documented override contract), so it cannot activate a real DB pool there (closes the known full-suite contamination class).
- The write fn is unit-tested (mocked conn: SQL/params/commit for populated + empty-array + param-safety + fail-open), scheduling-asserted (spy on app.main.log_user_query), AND integration-tested (real-DB round-trip including the empty-array case), and the full suite stays green.
</success_criteria>

<output>
Create `.planning/phases/17-query-logging-log/17-02-SUMMARY.md` when done.
</output>
